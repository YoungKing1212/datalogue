# 项目记忆

## 开发标准记忆

- 默认中文沟通；用户为 yangkai / KenYang。
- 新增代码文件必须包含用途或关键职责注释，注释尽量使用中文。
- 新功能开工前先标记 `TODO`；功能完成后删除对应临时 `TODO`。
- 每完成一个功能，都要在本文件记录完成情况。
- 文档记录文件名尽量使用中文。
- 完成功能记录按时间顺序排列，使用 `YYYY-MM-DD HH:mm` 格式；较早记录在前，较晚记录在后。
- 完成功能记录应包含：完成时间、功能名称、涉及文件、关键改动、验证方式、残留风险或后续事项。
- 新增 Python 文件时必须使用项目指定文件头注释模板；创建时可保留模板 `TODO`，完成文件职责说明后替换为实际中文描述。

## 功能完成记录

### 2026-06-05 18:58 · 隐藏语义治理助手界面

- 涉及文件：`datalogue-web/src/components/datasets.jsx`
- 关键改动：移除数据集页面中 `DatasetAssistantPanel` 的 import 和挂载点，保留组件文件以便后续恢复。
- 验证方式：执行 `npm run lint` 和 `npm run build`，均通过；仅保留项目已有 warning 和 Vite 大包提示。
- 残留风险：`datalogue-web/src/components/dataset-assistant-panel.jsx` 仍保留但当前未引用，后续若长期不用可再评估删除依赖和样式。

### 2026-06-05 19:01 · 优化数据集数据预览组件

- 涉及文件：`datalogue-web/src/components/datasets.jsx`、`datalogue-web/src/styles.css`
- 关键改动：将数据预览区域抽成 `DataPreviewPanel`，增加表名摘要、字段数/样例行统计、刷新按钮、加载/空状态、粘性表头、横向滚动和单元格溢出处理。
- 验证方式：执行 `npm run lint` 和 `npm run build`，均通过；仅保留项目已有 warning 和 Vite 大包提示。
- 残留风险：本地 Vite dev server 在沙箱内首次监听端口失败，已通过授权启动过；浏览器可视化验收因随后切换到开发标准任务未继续执行。

### 2026-06-05 19:02 · 建立项目开发标准记忆

- 涉及文件：`AGENTS.md`、`.codex/项目记忆.md`
- 关键改动：新增项目级协作和开发标准，明确中文注释、TODO 生命周期、功能完成文档记录要求。
- 验证方式：文档类改动，无需运行构建或测试。
- 残留风险：后续每个功能完成后需要持续维护本文件，否则记录会失效。

### 2026-06-05 19:02 · 调整项目记忆文档规则

- 涉及文件：`AGENTS.md`、`.codex/项目记忆.md`
- 关键改动：将项目记忆文件改为中文文件名，明确功能完成记录按时间顺序排列，并统一时间格式为 `YYYY-MM-DD HH:mm`。
- 验证方式：文档类改动，无需运行构建或测试。
- 残留风险：`AGENTS.md` 属于 Agent 约定文件名，仍需保留英文名称。

### 2026-06-05 19:06 · 增加 Python 文件头注释模板

- 涉及文件：`AGENTS.md`、`.codex/项目记忆.md`
- 关键改动：新增 Python 文件头注释模板，明确 `${NAME}`、`${USER}`、`${DATE}` 占位含义，并要求完成文件职责说明后将模板 `TODO` 替换为实际中文描述。
- 验证方式：文档类改动，无需运行构建或测试。
- 残留风险：后续创建 Python 文件时需要人工或 Agent 严格套用该模板。

### 2026-06-08 10:20 · 问数入口意图分类与路由策略

- 涉及文件：`datalogue-api/app/graph/state.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`
- 关键改动：新增 QueryGraph 前置入口分类节点，扩展 `AgentState` 和 SSE final/step 元数据，支持识别指标查询、明细查询、分析蓝图、知识库问答、澄清追问和权限拒答；命中已发布分析蓝图时返回 `blueprint_id` 与匹配详情。
- 验证方式：2026-06-08 13:27 复核执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_chat.py tests/test_analysis_blueprint.py`，44 passed、3 skipped；执行 `.venv/bin/pytest`，120 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：分析蓝图已接入真实执行器；知识库问答当前仍以业务术语定义直答为主，尚未扩展为完整知识库检索链路。

### 2026-06-08 10:30 · 接入分析蓝图真实执行器

- 涉及文件：`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：新增分析蓝图执行服务，支持参数默认值和问题日期提取、只读 SQL 校验、参数绑定执行、结果结构化、用量日志写入和 `usage_count` 更新；聊天入口命中分析蓝图后进入真实执行节点并继续报告生成；蓝图管理的试运行接口由 mock 返回改为真实执行 SQL 模板。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，6 passed；执行 `.venv/bin/pytest tests/test_chat.py`，34 passed、3 skipped；执行 `.venv/bin/pytest`，116 passed、4 skipped；执行 `.venv/bin/python -m compileall app tests` 和 `git diff --check`，均通过。
- 残留风险：执行器当前只允许 `SELECT/WITH` 只读 SQL，不支持 `CREATE PROCEDURE` 或数据库存储过程定义本身；参数抽取仍是确定性规则，复杂自然语言参数后续需要接入更强的抽取器；`.venv` 中仍未安装 `ruff`。

### 2026-06-08 10:39 · 替换分析蓝图 SQL 分析 mock

- 涉及文件：`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：删除 `_mock_analyze_sql` 固定返回逻辑，新增真实 SQL 草稿分析服务；`analyze-sql` 与 `re-analyze` 现在基于输入 SQL 提取过程参数、占位符参数、最终 SELECT 输出列、来源表、触发词、触发示例、步骤、归因提示、整体置信度和低置信字段，结果继续兼容现有前端任务查询协议。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，7 passed；执行 `.venv/bin/pytest tests/test_chat.py`，34 passed、3 skipped；执行 `.venv/bin/pytest`，117 passed、4 skipped；执行 `.venv/bin/python -m compileall app tests` 和 `git diff --check`，均通过。
- 残留风险：当前 SQL 分析为确定性解析流程，不依赖 LLM；复杂嵌套 SQL、数据库特定语法和存储过程完整 AST 解析仍可能需要后续引入专业 SQL parser 或 LLM 审核增强；`.venv` 中仍未安装 `ruff`。

### 2026-06-08 10:48 · 分析蓝图 SQL 分析改为 AI 主流程

- 涉及文件：`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：`analyze_sql_for_blueprint` 改为调用项目统一 LLM 客户端 `get_llm`，由 AI 理解 SQL 草稿并生成蓝图名称、业务描述、参数口径、输出字段语义、步骤、触发示例、归因提示和低置信字段；原确定性 SQL 解析只作为 AI 输入参考和字段补齐兜底，不再作为主分析逻辑。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，7 passed；执行 `.venv/bin/pytest tests/test_chat.py`，34 passed、3 skipped；执行 `.venv/bin/pytest`，117 passed、4 skipped；执行 `.venv/bin/python -m compileall app tests` 和 `git diff --check`，均通过。
- 残留风险：测试中通过 mock LLM 验证调用路径，未访问真实外部模型；线上效果取决于 `OPENAI_API_KEY`、`OPENAI_BASE_URL` 和 `LLM_MODEL` 配置；`.venv` 中仍未安装 `ruff`。

### 2026-06-08 10:55 · 分析蓝图 SQL 变量占位保护

- 涉及文件：`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：AI 分析前先删除 `SET` / `DECLARE` 变量赋值语句，并将 `@var`、`${var}`、`#{var}`、`{{var}}` 等变量统一替换为 `:var` 占位符；AI 返回后再次净化 `raw_sql`、`call_template` 和参数 `default_expr`，避免变量实际值进入分析结果。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，8 passed；执行 `.venv/bin/pytest tests/test_chat.py`，34 passed、3 skipped；执行 `.venv/bin/pytest`，118 passed、4 skipped；执行 `.venv/bin/python -m compileall app tests` 和 `git diff --check`，均通过。
- 残留风险：当前变量识别覆盖常见 SQL/模板写法，复杂脚本变量或方言特有语法后续可能需要补充解析规则；`.venv` 中仍未安装 `ruff`。

### 2026-06-08 11:24 · 优化分析蓝图 AI 分析接口响应速度

- 涉及文件：`datalogue-api/app/api/dataset.py`、`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/app/graph/llm.py`、`datalogue-api/app/core/config.py`、`datalogue-api/.env.example`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-web/src/components/analysis-blueprints.jsx`
- 关键改动：`analyze-sql` 与 `re-analyze` 改为后台任务执行，接口先返回 `running` 和 `task_id`，前端按任务接口轮询最终结果；AI 分析服务记录预处理、提示词构建、LLM 调用、结果合并和总耗时；LLM 客户端改为进程内缓存，代理和超时改由 `OPENAI_PROXY_URL`、`LLM_TIMEOUT_SECONDS` 配置；压缩系统预解析参考，降低提示词体积；`.env.example` 中的 API Key 示例改为占位符。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，8 passed；执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest`，118 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：真实 AI 响应耗时仍主要取决于外部模型服务和网络；任务状态当前保存在进程内存，服务重启或多进程部署会丢失未完成任务，后续生产化需要迁移到数据库或队列。

### 2026-06-08 11:32 · 增强分析蓝图 AI 阶段耗时日志

- 涉及文件：`datalogue-api/app/api/dataset.py`、`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/app/graph/llm.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：后台任务日志新增 `task_id`、排队等待、SQL 长度、提示词长度、任务总耗时；AI 分析内部将耗时拆分为 SQL 预处理、提示词构建、LLM 客户端获取、LLM 请求、JSON 解析、结果合并和总耗时；LLM 工厂在缓存未命中时记录模型、base_url、代理启用状态和超时配置；测试补充耗时字段存在性断言。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行真实模型分析任务，结果为 `total=34785ms`、`llm_invoke=34608ms`、`llm_client=170ms`、`preprocess=4ms`、`prompt_build=0ms`、`parse_json=0ms`、`merge=0ms`；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，8 passed；执行 `.venv/bin/pytest`，118 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：当前主要耗时集中在外部模型请求阶段，后续若继续优化，需要从模型选择、非流式调用、输出 token 控制、超时/重试策略或供应商网络链路入手。

### 2026-06-08 11:38 · 分析蓝图 AI 分析改为单次同步请求

- 涉及文件：`datalogue-api/app/api/dataset.py`、`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：取消分析蓝图前端轮询任务结果的交互方式，避免模型耗时较长时持续请求后端；`analyze-sql` 与 `re-analyze` 现在在单次 POST 请求内同步执行 AI 分析并返回 `done` 或 `failed`，保留 `task_id` 和任务查询接口仅用于兼容历史调用；前端移除 `waitAnalyzeTask`、轮询间隔和 `getBlueprintAnalyzeTask` 引用。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，8 passed；执行 `.venv/bin/pytest`，118 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：单次请求会占用一个 HTTP 连接直到模型返回，若真实模型仍保持 30 秒以上耗时，需要关注网关、浏览器或反向代理超时；接口频次问题已通过取消轮询解决。

### 2026-06-08 11:47 · 防止分析蓝图推荐内容泄露 SQL 参数

- 涉及文件：`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：AI 分析提示词明确禁止在蓝图自然语言字段中输出 SQL 参数名、占位符或变量值；服务端合并 AI 输出时构造参数保护上下文，将原 SQL 中的 `@var`、`:var`、`${var}` 等参数名以及 `SET/DECLARE` 变量值纳入禁止集合；对 `name`、`description`、`trigger_keywords`、`trigger_examples`、`when_to_use`、`steps`、`attribution_hints`、`low_confidence_fields` 统一清理或过滤，避免推荐问法、触发词和步骤说明带出输入 SQL 的参数痕迹。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，8 passed；执行 `.venv/bin/pytest`，118 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：结构化 `parameters` 字段仍保留参数名，用于蓝图执行器参数绑定；本次清理针对用户可读的推荐问法、触发词、描述和步骤等自然语言字段。

### 2026-06-08 12:09 · 简化分析蓝图发布流程

- 涉及文件：`datalogue-api/app/api/dataset.py`、`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：移除发布分析蓝图前必须先测试成功的后端强校验，发布只负责将蓝图状态改为 `active` 并创建版本快照；前端向导移除“发布前必须先完成一次测试”的拦截提示；测试用例改为验证未测试蓝图也能发布并生成版本，同时保留试运行接口作为可选校验能力。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，9 passed；执行 `.venv/bin/pytest`，119 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：发布不再保证 SQL 可执行，运行时失败会通过蓝图执行日志体现；后续 UI 可用“未测试/上次测试失败”状态提醒，但不应阻塞发布。

### 2026-06-08 12:18 · 增加分析蓝图手动创建 AI 草稿

- 涉及文件：`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/app/schemas/__init__.py`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-web/src/api/client.js`、`datalogue-web/src/components/analysis-blueprints.jsx`
- 关键改动：新增 `BlueprintAnalyzeDescriptionPayload` 和 `/api/dataset/{ds_id}/blueprints/analyze-description` 接口，支持根据蓝图名称、业务场景、用户可能问法、希望输出和数据口径约束生成 `semantic_plan` 蓝图草稿；后端会把当前数据集指标、维度和业务术语作为 AI 参考；前端手动创建模式改为结构化业务场景表单，不再复用 SQL 粘贴输入；生成后沿用既有 AI 草稿审核、高级配置、保存和发布流程。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，10 passed；执行 `.venv/bin/pytest`，120 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：手动创建蓝图默认 `implementation_type=semantic_plan`，不包含可执行 SQL；发布后如果进入现有 SQL 执行器会因缺少执行 SQL 而失败，后续需要让运行时支持语义计划蓝图或在发布前关联执行逻辑。

### 2026-06-08 12:24 · 优化手动创建蓝图填写提示

- 涉及文件：`datalogue-web/src/components/analysis-blueprints.jsx`
- 关键改动：手动创建蓝图表单新增“填写示例”区域，展示业务场景、希望输出、用户问法和口径约束的完整样例；蓝图名称、用户可能问法、业务场景、希望输出、数据口径约束字段补充 placeholder 与字段提示；手动创建表单和示例区域改为自适应网格，降低窄屏下文案和输入框拥挤风险。
- 验证方式：执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过。
- 残留风险：当前示例为通用零售销售波动场景，后续可根据数据集行业或已有业务术语动态生成更贴近当前数据集的示例。

### 2026-06-08 12:46 · 重构分析蓝图创建向导 UI

- 涉及文件：`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-web/src/styles.css`
- 关键改动：参考 assistant-ui 的 headless primitives 组合思路，将新建分析蓝图弹窗重构为“左侧创建方式与流程导航、右侧步骤工作区、底部动作栏”的向导结构；手动创建模式新增 AI 输入摘要侧栏和“套用示例”动作，帮助用户理解 AI 将读取的业务语义；SQL 模式改为更清晰的代码输入 composer；第 2-4 步统一为审核、配置、测试面板；补充桌面与移动端响应式样式，移动端保留简洁模式与步骤名称，避免长说明挤压表单。
- 验证方式：执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；使用本地 Vite 页面检查桌面和移动宽度下弹窗渲染、手动创建切换、套用示例与 AI 输入摘要联动。
- 残留风险：当前是项目内自定义 UI 组件，并未直接接入 `@assistant-ui/react` 运行时；若后续要把蓝图创建过程做成真实对话式 Copilot，需要进一步接入 assistant-ui runtime 和工具调用 UI。

### 2026-06-08 13:10 · 修复分析蓝图详情功能节点

- 涉及文件：`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-web/src/styles.css`
- 关键改动：将分析蓝图详情卡片中的“概览、参数、输出列、业务逻辑、测试、使用记录”改为真实可切换子页面；参数和输出列统计卡可跳转对应页面；测试页支持按蓝图参数填写测试值并调用真实试运行接口；使用记录页接入真实统计和日志接口；详情动作区接入发布、弃用和重新启用状态流转，已发布蓝图禁用重复发布；补充字符串和对象两类参数、输出列、步骤结构兼容，避免历史数据或 AI 生成字段显示成“未命名”。
- 验证方式：执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，10 passed；执行 `git diff --check`，通过；使用本地 Vite 页面验证 6 个详情子页可切换，参数、输出列、业务逻辑、测试参数、使用统计和测试/发布/弃用按钮均正常展示。
- 残留风险：浏览器验收未点击真实“弃用/重新启用”，避免改变当前演示数据状态；接口链路由后端蓝图测试覆盖。发布操作仍不强制要求测试成功，运行失败会落到使用日志中。

### 2026-06-08 16:29 · 修复入口分类、蓝图执行和 SSE 序列化问题

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：增强 `_extract_node_output`，递归兼容 LangGraph `astream_events(version="v2")` 的扁平输出、`{节点名: 输出}`、多层 `output` 包装和图级最终状态输出；只有提取到真实 `AgentState` 字段时才标记节点完成，避免 `entry_intent_classification` 和 `analysis_blueprint_execute` 的输出被空事件吞掉，导致 final 走通用兜底；新增 `_sse_data`，所有 SSE step/token/final payload 统一经 `jsonable_encoder` 后再 `json.dumps`，避免蓝图结果行中的 `datetime`、`date`、`Decimal` 导致 `Object of type datetime is not JSON serializable`；蓝图执行参数抽取新增中文年份/月度范围和“某人的日报/任务/记录”姓名识别，支持“我要查询2024年杨凯的日报”提取 `person_name=杨凯`、`start_date=2024-01-01`、`end_date=2024-12-31`。
- 验证方式：执行 `.venv/bin/pytest tests/test_chat.py`，37 passed、3 skipped；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py tests/test_chat.py`，50 passed、3 skipped；执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest`，126 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：当前兼容 LangGraph 常见输出形态；如果后续升级 LangGraph 引入新的事件包装层，需要继续扩展 `_extract_node_output`。姓名抽取只覆盖常见中文姓名问法，复杂组织/多人条件仍需要更强的参数抽取器。

### 2026-06-08 16:56 · 手动分析蓝图接入语义计划执行链路

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`
- 关键改动：`semantic_plan` 类型的手动分析蓝图不再进入 SQL 模板执行器，也不再要求用户配置可执行 SQL；蓝图执行节点会将名称、适用场景、业务描述、触发问法、参数、输出列、业务步骤和归因提示整理为 `blueprint_context`，再由工作流路由回 `schema_recall`，作为数据集级 LLM 约束进入 QueryGraph/NL2SQL；SQL 导入蓝图仍继续使用原有真实 SQL 执行器。
- 验证方式：执行 `.venv/bin/pytest tests/test_chat.py tests/test_analysis_blueprint.py`，53 passed、3 skipped；执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest`，129 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：语义计划蓝图的实际查询质量仍取决于当前数据集语义层、所选表结构和 LLM 生成 SQL 的能力；如果蓝图步骤需要多 SQL、多阶段计算或临时表，后续还需要实现更完整的计划执行器。

### 2026-06-08 17:17 · 数据集级 SQL 查询约束配置

- 涉及文件：`datalogue-api/app/models/dataset.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/app/utils/query_constraints.py`、`datalogue-api/app/utils/prompt.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/alembic/versions/b2c4d6e8f901_add_dataset_query_constraints.py`、`datalogue-api/tests/test_dataset.py`、`datalogue-api/tests/test_dataset_prompt_instructions.py`、`datalogue-web/src/components/datasets.jsx`
- 关键改动：为数据集新增 `query_constraints` JSON 配置，默认启用“未指定时间查最近 30 天、默认 LIMIT 100、最大 LIMIT 1000”；数据集创建和更新接口会归一化并裁剪配置；Schema prompt、真实 Schema SQL 生成、推断 SQL 生成、语义 DSL 生成和 DSL 编译器统一读取该配置；前端数据集右键菜单新增“查询约束”入口，弹窗支持开关、默认时间范围、默认返回条数、最大返回条数，并保留原有自定义 LLM 约束；新建数据集默认开启查询约束。
- 验证方式：执行 `.venv/bin/pytest tests/test_dataset.py tests/test_dataset_prompt_instructions.py tests/test_chat.py`，77 passed、3 skipped；执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest`，136 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `.venv/bin/alembic heads`，返回单一 head `b2c4d6e8f901`；执行 `git diff --check`，通过。
- 残留风险：该配置约束的是 AI 生成和语义 DSL 编译路径；用户手写 SQL、分析蓝图 SQL 模板或历史已保存 SQL 不会被强制改写，后续如要覆盖这几类路径，需要在 SQL 执行前增加统一重写/审计层。

### 2026-06-09 11:12 · T-015 升级 NL2DSL 资产引用 Schema

- 涉及文件：`datalogue-api/app/schemas/dsl.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/docs/NL2DSL资产引用Schema.md`、`datalogue-api/tests/test_dsl_schema.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/types/nl2dsl.d.ts`、`datalogue-web/src/assistant/chat-adapter.js`
- 关键改动：新增 NL2DSL v2 资产引用模型，统一表达 `asset_type`、`asset_id`、`confidence` 和 `ambiguities`；新增 `normalize_dsl` 兼容旧版 `metrics: ["gmv"]`、`dimensions: ["region"]` 字符串 DSL；语义层 Schema 召回补充指标、维度、业务术语和已发布分析蓝图的资产 ID；DSL 生成提示词升级为资产引用格式；DSL 校验和编译器改为新旧双读；SSE final payload 和前端 assistant metadata 透出最终 DSL；前端新增 `nl2dsl.d.ts` 类型声明。
- 验证方式：执行 `.venv/bin/pytest tests/test_dsl_schema.py tests/test_chat.py -q`，46 passed、3 skipped；执行 `.venv/bin/pytest -q`，141 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过。
- 残留风险：当前编译器仍只使用资产引用中的 `name` 生成 SQL，`asset_id` 主要用于审计、前端展示和后续澄清；`ambiguities` 已结构化保留但暂不触发自动澄清路由，后续可以在入口分类或 DSL 校验后增加歧义确认节点。

### 2026-06-09 11:38 · 分析蓝图补充创建来源标记

- 涉及文件：`datalogue-api/app/models/dataset.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/alembic/versions/c9d1e2f3a4b5_add_blueprint_creation_source.py`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-web/src/styles.css`
- 关键改动：为分析蓝图新增 `creation_source`、`ai_generated`、`ai_generation_type`，用于稳定区分 `manual`、`sql_ai_analysis` 和 `manual_ai_draft`；SQL AI 分析任务返回 `sql_ai_analysis/sql_analysis`，业务描述 AI 草稿返回 `manual_ai_draft/description_analysis`；创建、列表、详情、版本快照和回滚均保留来源字段；前端蓝图列表和详情头部展示“AI SQL分析 / AI业务草稿 / 手动填写”来源标签。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py -q`，13 passed；执行 `.venv/bin/pytest -q`，142 passed、4 skipped；执行 `.venv/bin/alembic heads`，返回单一 head `c9d1e2f3a4b5`；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过。
- 残留风险：历史蓝图迁移后默认标记为 `manual`，无法追溯历史创建时是否经过 AI；如果需要修正历史数据，需要额外基于操作日志或字段特征做一次人工确认脚本。

### 2026-06-09 12:04 · T-016 语义资产解析器替换指标解析节点

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`、`datalogue-web/src/components/chat-page.jsx`
- 关键改动：新增 `semantic_asset_resolution_node` 并替换工作流中的 `metric_resolution_node`，统一解析业务术语、指标、维度、源字段和分析蓝图资产；Schema 召回补充 `fields` 结构化字段资产和业务术语 `asset_links`；解析结果输出 `assets/terms/metrics/dimensions/fields/blueprints/ambiguities/unresolved`，包含 `asset_id`、`asset_type`、`confidence`、`match_type`；命中业务术语后自动扩展显式关联资产；保留旧 `metric_resolution` 输出和兼容包装函数，供 DSL 生成、SQL 审计和历史前端继续使用；SSE、final payload 和前端 reasoning 展示新增“语义资产解析”节点。
- 验证方式：执行 `.venv/bin/python -m py_compile app/graph/nodes.py app/api/chat.py app/graph/workflow.py app/graph/state.py`，通过；执行 `.venv/bin/pytest tests/test_chat.py -q`，47 passed、3 skipped；执行 `.venv/bin/pytest -q`，146 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过。
- 残留风险：当前解析器是确定性规则和轻量上下文偏置，不额外调用 LLM；歧义会进入 `semantic_asset_resolution.ambiguities` 并写入 DSL prompt，但还没有在工作流中强制打断并向用户发起澄清。

### 2026-06-09 12:38 · T-004 业务术语运行时归一化

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`
- 关键改动：在 QueryGraph 中新增 `term_normalize_node`，工作流调整为 `schema_recall -> term_normalize_node -> semantic_asset_resolution_node -> dsl_generate`；术语节点基于业务术语 `name/display_name/aliases` 做确定性匹配，将命中的同义词注入 `entities.terms` 和 `term_normalization`；Schema 结构化术语补充 `forbidden_aliases`；DSL 生成和 SQL 审计 prompt 注入业务术语归一化结果；当同一个名称或同义词命中多个业务术语时，节点直接返回 `term_conflict_clarification`，进入澄清而不是继续生成 SQL；前端推理链新增“术语归一化”节点展示命中数和冲突数。
- 验证方式：执行 `.venv/bin/python -m py_compile app/graph/nodes.py app/graph/workflow.py app/graph/state.py app/api/chat.py`，通过；执行 `.venv/bin/pytest tests/test_chat.py -q`，51 passed、3 skipped；执行 `.venv/bin/pytest -q`，150 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过。
- 残留风险：术语归一化当前只做确定性文本匹配，不做 LLM 语义改写；冲突澄清已在后端 route payload 中返回，前端暂沿用通用回答展示，后续可做专门的术语冲突确认交互。

### 2026-06-09 13:29 · T-003 真实蓝图测试执行增强

- 涉及文件：`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/app/models/dataset.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/alembic/versions/d4e5f6a7b8c9_add_blueprint_usage_log_details.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：分析蓝图真实执行器读取 `timeout_seconds`，默认 30 秒，并按 Postgres、MySQL/MariaDB、SQLite 和支持 `call_timeout` 的驱动 best-effort 设置数据库级超时；执行结果返回前统一脱敏手机号、邮箱、身份证和 token/key/secret/password 等敏感字段和值；缺参、非只读 SQL、超时和执行异常统一返回 `diagnosis.code/title/detail/suggested_action`；测试接口透出 `row_count`、`diagnosis`、`masking_summary`；`blueprint_usage_log` 新增 `row_count` 和 `diagnosis`，成功/失败测试均保留日志但 `count_usage=False` 不增加 `usage_count`。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py -q`，17 passed；执行 `.venv/bin/pytest -q`，154 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：发布流程仍按产品决策保持“测试作为发布前可选校验”，不恢复“未测试成功不能发布”的强阻塞；数据库级超时是 best-effort，具体可用性取决于数据源方言和驱动支持。

### 2026-06-09 13:47 · T-017 数据集问数上下文组装服务

- 涉及文件：`datalogue-api/app/services/dataset_context.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_dataset_context.py`、`datalogue-api/tests/test_chat.py`
- 关键改动：新增 `build_dataset_query_context` 服务，集中组装 SQL 生成所需的数据集上下文，包含数据集说明、查询约束、已选表字段、字段样例、指标、维度、业务术语、分析蓝图和权限信息；`schema_recall_node` 改为调用该服务并继续透出 `schema_context`、`schema_structured`、`ddl_context`、`query_constraints`、`dataset_prompt_instructions` 等兼容字段；上下文 prompt 支持 `token_budget` 裁剪，问题文本或语义解析显式命中的资产会被优先保留；新增 `dataset_context_debug`，记录资产总数、保留数量、预算、估算 token、被裁剪资产和命中资产摘要，并在 SSE final payload 中透出。
- 验证方式：执行 `.venv/bin/pytest tests/test_dataset_context.py tests/test_chat.py -q`，53 passed、3 skipped；执行 `.venv/bin/pytest -q`，156 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：预算估算采用轻量字符折算，不是模型 tokenizer 的精确 token；权限信息当前为“未配置数据集级权限策略”的占位摘要，后续接入真实权限模型后需要由本服务统一渲染。

### 2026-06-09 16:24 · T-019 SQL 静态安全校验与方言规范化

- 涉及文件：`datalogue-api/app/utils/sql_guard.py`、`datalogue-api/app/utils/__init__.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/tests/test_sql_guard.py`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-api/tests/test_chat.py`
- 关键改动：新增统一 SQL Guard，执行前静态拦截空 SQL、多语句、非 `SELECT/WITH`、DML/DDL/DCL/TCL 关键字、危险函数和文件导出语法；注释和字符串内容会被屏蔽后再扫描，避免误杀展示文本；按数据源方言补齐或裁剪结果行数限制，Oracle 使用 `FETCH FIRST ... ROWS ONLY`，MySQL/Postgres/SQLite 使用 `LIMIT`；问数 `direct_sql`、真实 Schema SQL、语义 DSL 编译 SQL、最终 SQL 执行节点和分析蓝图 SQL 模板执行统一接入 Guard，并透出结构化 `sql_guard` 信息和明确错误原因。
- 验证方式：执行 `.venv/bin/python -m py_compile app/utils/sql_guard.py app/graph/nodes.py app/services/analysis_blueprint.py tests/test_sql_guard.py tests/test_analysis_blueprint.py tests/test_chat.py`，通过；执行 `.venv/bin/pytest tests/test_sql_guard.py tests/test_analysis_blueprint.py tests/test_chat.py -q`，86 passed、3 skipped；执行 `.venv/bin/pytest -q`，174 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：首版方言规范化聚焦在执行前安全、注释/字符串感知扫描、`NULL` 比较修正和 LIMIT/FETCH 行数限制；复杂跨方言函数转换仍需要后续增强。

### 2026-06-09 17:12 · T-019 引入 SQLGlot AST 解析

- 涉及文件：`datalogue-api/app/utils/sql_guard.py`、`datalogue-api/pyproject.toml`、`datalogue-api/requirements.txt`、`datalogue-api/uv.lock`、`datalogue-api/tests/test_sql_guard.py`、`datalogue-api/tests/test_chat.py`
- 关键改动：引入 `sqlglot==30.9.0`，SQL Guard 改为先做注释/字符串感知的高风险扫描，再用 SQLGlot 按目标方言解析 AST；通过 AST 判断单语句、只读 Query、DML/DDL/命令类表达式和危险函数，并使用 SQLGlot AST 生成目标方言 SQL；外层 LIMIT/FETCH 由 AST 统一补齐或裁剪，Oracle 输出 `FETCH FIRST ... ROWS ONLY`，MySQL/Postgres/SQLite 输出 `LIMIT`。
- 验证方式：执行 `.venv/bin/python -m py_compile app/utils/sql_guard.py app/graph/nodes.py app/services/analysis_blueprint.py tests/test_sql_guard.py tests/test_analysis_blueprint.py tests/test_chat.py`，通过；执行 `.venv/bin/pytest tests/test_sql_guard.py tests/test_analysis_blueprint.py tests/test_chat.py -q`，86 passed、3 skipped；执行 `.venv/bin/pytest -q`，174 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：SQLGlot 已覆盖 AST 解析和基础方言输出，但业务上更复杂的跨方言函数等价转换、数据库私有函数白名单和更细粒度的子查询限制策略仍需按真实数据源错误继续补齐。

### 2026-06-09 17:42 · T-020 SQL 执行失败诊断节点

- 涉及文件：`datalogue-api/app/utils/sql_diagnosis.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/models/conversation.py`、`datalogue-api/app/models/__init__.py`、`datalogue-api/app/prompts/sql_audit.py`、`datalogue-api/alembic/versions/f6a7b8c9d0e1_add_sql_diagnosis_log.py`、`datalogue-api/tests/test_sql_audit.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`
- 关键改动：新增 `sql_diagnosis` 确定性诊断工具，稳定分类字段不存在、表未选、表不存在、聚合错误、类型错误、权限不足、语法/方言错误、超时和未知执行错误；`sql_audit_node` 改为“确定性分类优先 + LLM 补充解释”，保留旧 `root_cause/wrong_field/suggested_fix/severity` 字段并新增 `code/category/title/detail/suggested_action/retryable/evidence/original_error`；工作流路由改为按 `retryable` 和 `retry_count < 3` 决定是否重试；新增 `sql_diagnosis_log` 表落库诊断；聊天 SSE、final payload、历史 `step_trace` 和前端 reasoning 透出 SQL 诊断摘要与 metadata。
- 验证方式：执行 `.venv/bin/python -m py_compile app/utils/sql_diagnosis.py app/graph/nodes.py app/api/chat.py app/graph/workflow.py app/models/conversation.py tests/test_sql_audit.py tests/test_chat.py`，通过；执行 `.venv/bin/pytest tests/test_sql_audit.py tests/test_chat.py -q`，83 passed、3 skipped；执行 `.venv/bin/pytest -q`，187 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `.venv/bin/alembic heads`，返回单一 head `f6a7b8c9d0e1`；执行 `git diff --check`，通过。
- 残留风险：诊断日志本轮只保证落库，不提供查询接口和治理看板；确定性分类已覆盖主流错误文本，但不同数据库驱动的错误文案仍需随真实线上样本继续补充规则。

### 2026-06-09 17:51 · 修复 SQL 诊断日志迁移撞表

- 涉及文件：`datalogue-api/alembic/versions/f6a7b8c9d0e1_add_sql_diagnosis_log.py`
- 关键改动：`sql_diagnosis_log` 迁移改为使用 SQLAlchemy inspector 做存在性检查；当目标库中表已存在但 Alembic revision 尚未升级时，跳过建表并补齐缺失索引，避免 `psycopg2.errors.DuplicateTable: relation "sql_diagnosis_log" already exists`；downgrade 同样检查表和索引是否存在，避免对象缺失时报错。
- 验证方式：执行 `.venv/bin/python -m py_compile alembic/versions/f6a7b8c9d0e1_add_sql_diagnosis_log.py`，通过；执行 `.venv/bin/alembic upgrade head`，通过并升级到 `f6a7b8c9d0e1`；执行 `.venv/bin/alembic current`，返回 `f6a7b8c9d0e1 (head)`；执行 `.venv/bin/pytest tests/test_sql_audit.py -q`，24 passed。
- 残留风险：如果既有 `sql_diagnosis_log` 表缺少列或外键，本迁移只补索引，不做列级补齐；当前报错场景是表已存在导致撞表，后续如遇历史手工表结构不完整，需要再加列级检查。

### 2026-06-09 17:57 · T-021 SQL 自动修复与重试闭环

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_sql_audit.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`
- 关键改动：新增 `sql_retry_trace` 状态字段，记录每次自动修复重试的 `attempt/max_attempts/original_sql/repair_reason/diagnosis_code/repaired_sql/status/result/error/row_count`；`sql_audit_node` 在可修复且未超过 `max_retry_count` 时登记待重试记录，并把结构化 `retry_decision` 写入诊断；SQL 执行、DSL 校验和 DSL 编译失败会回填最近一次重试结果，成功执行会记录返回行数；工作流把最大重试次数从硬编码 3 收敛为 `max_retry_count`，默认仍为 3；SSE/final payload、前端 reasoning 和 assistant metadata 透出自动修复重试链路，达到上限后最终回答会解释无法继续安全重试。
- 验证方式：执行 `.venv/bin/python -m py_compile app/graph/nodes.py app/graph/workflow.py app/graph/state.py app/api/chat.py tests/test_sql_audit.py tests/test_chat.py`，通过；执行 `.venv/bin/pytest tests/test_sql_audit.py tests/test_chat.py -q`，86 passed、3 skipped；执行 `.venv/bin/pytest -q`，190 passed、4 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过。
- 残留风险：本轮自动修复仍以“重新生成 DSL/SQL + SQL Guard 安全校验”为边界，不做直接字符串 patch 执行；若重试在真实数据库外部异常或数据源连接层失败，仍会按诊断结果终止并解释原因。

### 2026-06-09 18:08 · T-022 回答解释与低置信确认

- 涉及文件：`datalogue-api/app/services/answer_explanation.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/models/conversation.py`、`datalogue-api/app/schemas/conversation.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/prompts/report_generate.py`、`datalogue-api/alembic/versions/0f1e2d3c4b5a_add_message_response_metadata.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_conversation.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：新增 `answer_explanation` 解释包，统一汇总口径说明、数据来源、SQL 摘要、置信度、风险提示和低置信确认提示；解释包优先读取语义资产、DSL、蓝图路由、SQL 诊断和数据集上下文裁剪信息，并用 SQLGlot 解析 SQL 表字段作为兜底；聊天 final 回答追加确定性 Markdown，同时 SSE final 和 assistant metadata 透出结构化解释包；`message` 新增 `response_metadata` JSON 字段并通过 conversation API 历史恢复；前端消息气泡新增“口径与可信度”信息区，低置信时展示确认提示。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_chat.py tests/test_conversation.py`，68 passed、4 skipped；执行 `.venv/bin/pytest`，195 passed、4 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：解释包为确定性规则生成，低置信阈值暂定 `0.75`；实际生产中不同数据源方言或复杂 SQL 可能让 SQL 摘要降级为预览，后续可根据真实问数日志继续细化置信度扣分和风险分类。

### 2026-06-09 18:16 · T-022 解释信息改为气泡折叠栏

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：取消将解释包 Markdown 直接追加到回答正文，避免口径说明、数据来源、SQL 摘要、置信度和风险提示在正文中展开重复展示；前端“口径与可信度”改为消息气泡内的可点击折叠栏，默认折叠，只展示置信度、风险数量和是否需要确认，点击后展开数据来源、SQL 摘要、风险提示和低置信确认。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_chat.py tests/test_conversation.py`，68 passed、4 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：当前折叠状态为单条消息本地状态，刷新后默认仍折叠；如果后续需要记住用户展开/折叠偏好，需要额外加入前端持久化。

### 2026-06-09 18:17 · SQL 查询结果默认折叠

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：查询结果表格改为消息气泡内的可点击折叠栏，默认折叠，只展示“查询结果”和返回行数；点击后展开完整表格；折叠头部增加 hover 状态和箭头旋转，和“口径与可信度”折叠栏保持一致。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：折叠状态仍为单条消息本地状态，刷新后默认折叠；如果后续需要记住用户展开结果表的偏好，需要加入前端持久化。

### 2026-06-09 18:19 · 生成 SQL 气泡支持复制

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：生成 SQL 折叠气泡头部新增复制按钮，点击只复制当前 SQL，不触发展开/收起；复制成功后短暂显示“已复制”；外层折叠头保持可点击和键盘 Enter/空格切换，避免嵌套 button 造成无效 DOM。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：复制能力依赖浏览器 `navigator.clipboard`；若在不支持剪贴板 API 的环境中，会在控制台记录错误但不影响 SQL 展示。

### 2026-06-09 18:20 · 风险提示按序号逐行展示

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：“口径与可信度”折叠栏展开后，风险提示从分号拼接文本改为有序列表；存在多条风险时按 `1. 2. 3.` 序号逐行列出，无风险时继续显示“未发现明显风险”；补充列表缩进、行高和换行样式，避免长风险文案溢出气泡。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：风险条目顺序沿用后端解释包生成顺序，前端不额外按严重程度排序。

### 2026-06-09 18:22 · 生成 SQL 气泡轻量化

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：生成 SQL 折叠气泡新增 `sql-collapse` 专用样式，避免继续套用较厚重的通用折叠块；头部改为更低高度、更窄 padding、更弱边框和更轻背景，标题从“生成的 SQL”收敛为“SQL”；复制按钮压缩为轻量工具按钮；展开后的 SQL 代码区减少内边距、字号和行高，让 SQL 组件更适合消息流 UI。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：当前只轻量化消息气泡里的 SQL 折叠块，不影响 AgentPanel 和其他页面的 SQL 预览组件。

### 2026-06-09 18:26 · 修复 SQL 按钮尺寸污染

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：SQL 折叠按钮的箭头图标从通用 `chev` 类切换为 `sql-collapse-chev`，避免被页面其它 `.chev` 样式污染导致图标异常放大；SQL 气泡宽度保持 `100%`，与上方“口径与可信度”和“查询结果”折叠栏同宽，只收紧内部高度、padding、字号和复制按钮尺寸；补充独立箭头尺寸和旋转样式，适配当前 assistant-ui 消息流组件风格。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：如果后续继续新增全局短类名样式，消息内小组件仍需优先使用局部命名类，避免再次出现样式污染。

### 2026-06-09 19:49 · 修复历史会话数据集绑定丢失

- 涉及文件：`datalogue-api/app/models/conversation.py`、`datalogue-api/app/schemas/conversation.py`、`datalogue-api/app/api/conversation.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/alembic/versions/a1b2c3d4e5f6_add_dataset_id_to_conversation.py`、`datalogue-api/tests/test_conversation.py`、`datalogue-web/src/components/chat-page.jsx`、`datalogue-web/src/api/client.js`、`datalogue-web/src/assistant/thread-list-adapter.js`
- 关键改动：`Conversation` 新增 nullable `dataset_id` 并补 Alembic 迁移；chat 流在新建或继续会话时写入/更新会话绑定数据集，请求未带 `dataset_id` 时优先沿用会话绑定值；conversation 详情接口返回 `dataset_id`；ChatPage 切换 `/chat/:id` 历史会话时读取会话详情并恢复 `selectedDs`，同步给 `datasetIdRef`，保证后续问数继续携带对应数据集。
- 验证方式：执行 `.venv/bin/python -m py_compile app/api/chat.py app/api/conversation.py app/models/conversation.py app/schemas/conversation.py alembic/versions/a1b2c3d4e5f6_add_dataset_id_to_conversation.py tests/test_conversation.py`，通过；执行 `.venv/bin/alembic upgrade head`，成功从 `0f1e2d3c4b5a` 升级到 `a1b2c3d4e5f6`；执行 `.venv/bin/pytest tests/test_conversation.py -q`，5 passed、1 skipped；执行 `.venv/bin/pytest -q`，202 passed、5 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：旧会话如果没有 `Conversation.dataset_id`，无法自动恢复数据集，需要用户手动选择一次并在后续问数时写回会话绑定。

### 2026-06-09 22:24 · T-023 语义验证增强：覆盖术语和蓝图

- 涉及文件：`datalogue-api/app/models/dataset.py`、`datalogue-api/app/models/__init__.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/app/schemas/__init__.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/alembic/versions/b7c8d9e0f1a2_add_semantic_validation_case.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-web/src/api/client.js`、`datalogue-web/src/components/datasets.jsx`
- 关键改动：新增 `semantic_validation_case` 表和数据集验证用例列表/保存 API；数据集语义验证 Tab 汇总展示入口路由、普通问数路径、术语命中、蓝图命中、生成 SQL、失败原因、置信度风险和节点轨迹，并支持保存/查看最近验证用例；DSL 校验节点补充对 `terms` 和 `blueprints` 的资产名称/ID 校验，避免报告把不存在的术语或蓝图引用判为有效；修复数据集页面默认展示首个数据集但未写入 `activeDsId` 导致语义验证工作台不渲染的问题。
- 验证方式：执行 `.venv/bin/python -m py_compile app/models/dataset.py app/models/__init__.py app/schemas/dataset.py app/schemas/__init__.py app/api/dataset.py app/graph/nodes.py tests/test_chat.py tests/test_analysis_blueprint.py alembic/versions/b7c8d9e0f1a2_add_semantic_validation_case.py`，通过；执行 `.venv/bin/pytest tests/test_chat.py tests/test_analysis_blueprint.py -q`，88 passed、3 skipped；执行 `.venv/bin/pytest -q`，202 passed、4 skipped；执行 `.venv/bin/alembic heads`，返回单一 head `b7c8d9e0f1a2`；执行 `.venv/bin/alembic upgrade head`，成功从 `a1b2c3d4e5f6` 升级到 `b7c8d9e0f1a2`；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；通过内置浏览器打开 `http://127.0.0.1:5173/`，进入数据集页并确认“语义层验证”和“最近保存用例”区域渲染。
- 残留风险：本轮保存的是验证结果快照，尚未实现批量回放调度、用例断言规则和定时评测；真实试问仍依赖当前问数链路和 LLM/数据源可用性。

### 2026-06-10 10:32 · T-018 术语冲突澄清回复闭环

- 涉及文件：`datalogue-api/app/models/conversation.py`、`datalogue-api/app/models/__init__.py`、`datalogue-api/app/schemas/chat.py`、`datalogue-api/app/schemas/__init__.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/alembic/versions/c1d2e3f4a5b6_add_pending_clarification.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：新增 `pending_clarification` 持久化表，按会话和数据集记录术语冲突澄清态、候选项、原问题、过期时间和解决结果；`ChatRequest` 支持 `clarification_response`；QueryGraph 入口新增 `clarification_resolution_node`，可解析结构化按钮选择、自然语言“第一个/1”和术语名称，成功后恢复原问题并携带 `selected_term_id` 直接进入 schema recall 后续链路；`term_normalize_node` 支持已选术语口径，过滤冲突候选并避免再次打断；SSE final 和 assistant metadata 透出 `clarification_id/candidates/expires_at`；前端 AI 消息展示术语候选卡片，点击候选后填入输入框并随下一轮请求携带一次性结构化澄清回复。
- 验证方式：执行 `.venv/bin/python -m py_compile app/models/conversation.py app/models/__init__.py app/schemas/chat.py app/schemas/__init__.py app/graph/state.py app/graph/nodes.py app/graph/workflow.py app/api/chat.py alembic/versions/c1d2e3f4a5b6_add_pending_clarification.py`，通过；执行 `.venv/bin/pytest tests/test_chat.py -k 'clarification or term_normalize' -q`，13 passed、67 deselected；执行 `.venv/bin/pytest tests/test_chat.py -q`，77 passed、3 skipped；执行 `.venv/bin/pytest -q`，210 passed、4 skipped；执行 `.venv/bin/alembic heads`，返回单一 head `c1d2e3f4a5b6`；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过。
- 残留风险：澄清态采用下一轮读取时惰性过期，不含后台清理任务；当前只处理业务术语冲突，不覆盖蓝图参数缺失等其他澄清类型；前端候选点击依赖当前输入框提交路径，不使用 assistant-ui 私有 programmatic send API。

### 2026-06-10 11:23 · 数据源支持能力增强

- 涉及文件：`datalogue-api/app/services/datasource.py`、`datalogue-api/app/models/datasource.py`、`datalogue-api/app/schemas/datasource.py`、`datalogue-api/app/api/datasource.py`、`datalogue-api/app/services/dataset_context.py`、`datalogue-api/app/utils/sql_guard.py`、`datalogue-api/app/utils/sql_dialect.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/alembic/versions/d2e3f4a5b6c7_add_datasource_capability_fields.py`、`datalogue-api/tests/test_datasource.py`、`datalogue-api/tests/test_sql_guard.py`、`datalogue-api/tests/test_dataset_context.py`、`datalogue-web/src/api/client.js`、`datalogue-web/src/components/datasources.jsx`
- 关键改动：新增 SQLAlchemy 优先的数据源适配器注册表，注册 MySQL、PostgreSQL、SQLite、Oracle、Hive、ClickHouse、SQL Server、Trino、Presto、BigQuery 能力；可选企业驱动采用延迟 import，缺失时返回 `DRIVER_MISSING` 诊断而不阻断应用启动；`Datasource` 新增 `dialect/driver/default_schema/connection_options/connect_timeout_seconds/query_timeout_seconds/last_test_result/last_error_code/last_error_message`，并补能力接口 `/api/datasource/capabilities`；连接测试、Schema 拉取、表字段同步和预览统一返回诊断结构；数据集问数上下文新增 `datasource_context`，DSL 编译、SQL 执行、SQL 审计和蓝图执行统一读取方言和授权表；SQL Guard 增加表白名单校验和多方言别名；前端数据源表单改为后端能力驱动，并按类型展示 Oracle/Hive/Trino/Presto/SQL Server/BigQuery 扩展选项，DDL 同步页展示 skipped/errors。
- 验证方式：执行 `.venv/bin/python -m py_compile app/models/datasource.py app/schemas/datasource.py app/services/datasource.py app/api/datasource.py app/services/dataset_context.py app/utils/sql_guard.py app/utils/sql_dialect.py app/graph/nodes.py app/api/chat.py app/services/analysis_blueprint.py app/utils/sample_data.py tests/test_datasource.py tests/test_sql_guard.py tests/test_dataset_context.py`，通过；执行 `.venv/bin/pytest tests/test_datasource.py tests/test_sql_guard.py tests/test_dataset_context.py tests/test_chat.py -q`，101 passed、3 skipped；执行 `.venv/bin/pytest -q`，214 passed、4 skipped；执行 `.venv/bin/alembic upgrade head`，成功升级到 `d2e3f4a5b6c7`；执行 `.venv/bin/alembic current`，返回 `d2e3f4a5b6c7 (head)`；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过。
- 残留风险：Oracle/Hive/ClickHouse/SQL Server/Trino/Presto/BigQuery 驱动按可选依赖处理，本轮以能力注册和 mock/契约诊断为主，未连接真实企业库验收；SQLGlot 对复杂跨方言函数转换仍是 best-effort，后续需要结合真实库报错补方言规则和函数白名单；前端 lint 仍保留既有 warning，并新增 `datasources.jsx` 的 `useEffect` 依赖 warning，不影响构建。

### 2026-06-10 11:52 · 企业数据源驱动离线交付方案

- 涉及文件：`.gitignore`、`datalogue-api/README.md`、`datalogue-api/requirements-enterprise.txt`、`datalogue-api/pyproject.toml`、`datalogue-api/scripts/download_enterprise_wheels.sh`、`datalogue-api/docs/企业数据源驱动离线部署.md`、`datalogue-api/app/services/datasource.py`、`datalogue-api/app/schemas/datasource.py`、`datalogue-api/tests/test_datasource.py`、`datalogue-web/src/components/datasources.jsx`
- 关键改动：新增企业数据源可选驱动清单，覆盖 Oracle、Hive/Presto、Trino、SQL Server、ClickHouse、BigQuery，并同步登记到 `pyproject.toml` 的 `enterprise` 可选依赖；新增离线 wheelhouse 下载脚本，供有网构建机提前下载企业驱动包，再随制品进入纯内网；新增部署文档，说明 `requirements.txt` 与 `requirements-enterprise.txt` 的职责、内网 `pip install --no-index --find-links` 安装方式、系统 ODBC/Kerberos/服务账号等注意事项；README 增加企业驱动离线安装入口；`.gitignore` 忽略 wheelhouse 目录；数据源能力接口新增 `driver_module/driver_status/install_hint`，前端数据源管理页在概览和创建抽屉展示“内置驱动/驱动已安装/驱动未安装”状态，驱动缺失时保留配置保存但明确提示离线安装步骤。
- 验证方式：执行 `.venv/bin/python -m py_compile app/services/datasource.py app/schemas/datasource.py tests/test_datasource.py`，通过；执行 `.venv/bin/pytest tests/test_datasource.py -q`，9 passed；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：脚本只负责下载 Python wheel，不处理 SQL Server ODBC Driver、Kerberos 客户端、Oracle thick client、BigQuery 服务账号等系统级或密钥类依赖；如果目标平台没有对应 wheel，需要在同架构构建机编译或由企业制品库提供已验证 wheel。

### 2026-06-10 12:08 · 修复企业驱动下载脚本 Python 命令兼容

- 涉及文件：`datalogue-api/scripts/download_enterprise_wheels.sh`、`datalogue-api/docs/企业数据源驱动离线部署.md`、`datalogue-api/README.md`
- 关键改动：修复 `download_enterprise_wheels.sh` 在只有 `python3`、没有 `python` 命令的环境中报错的问题；脚本现在按 `PYTHON` 环境变量、项目 `.venv/bin/python`、`python3`、`python` 的顺序选择解释器，并在输出中显示实际使用的 Python；文档和 README 的内网安装示例改为 `python3 -m pip`，并补充 `PYTHON=/path/to/python` 显式指定方式。
- 验证方式：执行 `bash -n datalogue-api/scripts/download_enterprise_wheels.sh`，通过；执行 `git diff --check`，通过。
- 残留风险：本次只做脚本兼容性和语法检查，没有实际联网下载 wheel；真实下载仍依赖构建机网络、Python 版本和目标平台 wheel 可用性。

### 2026-06-10 12:13 · 修复企业驱动下载脚本 venv 无 pip 场景

- 涉及文件：`datalogue-api/scripts/download_enterprise_wheels.sh`、`datalogue-api/docs/企业数据源驱动离线部署.md`
- 关键改动：进一步修复项目 `.venv/bin/python` 存在但没有 `pip` 模块时脚本误选该解释器的问题；脚本新增 `has_pip` 检查，自动发现模式只选择能执行 `python -m pip` 的解释器，显式 `PYTHON=...` 缺 pip 时给出清晰错误；文档补充离线 wheel 与 Python 版本、操作系统和 CPU 架构相关，建议使用与内网目标环境一致且带 pip 的 Python 下载。
- 验证方式：执行 `bash -n datalogue-api/scripts/download_enterprise_wheels.sh`，通过；执行 `python3 -m pip --version`，确认系统 `python3` 可用；执行 `datalogue-api/.venv/bin/python -m pip --version`，复现 `.venv` 无 pip；执行 `git diff --check`，通过。
- 残留风险：仍未实际联网下载 wheel；如果系统 `python3` 版本与内网目标 Python 版本不一致，下载到的部分平台 wheel 可能不适用于目标环境，需要用目标同版本 Python 重新生成 wheelhouse。

### 2026-06-10 13:20 · LiteLLM 前端配置化接入

- 涉及文件：`datalogue-api/app/models/llm.py`、`datalogue-api/app/schemas/llm.py`、`datalogue-api/app/api/llm.py`、`datalogue-api/app/services/llm_config.py`、`datalogue-api/app/graph/llm.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/services/annotation.py`、`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/alembic/versions/e3f4a5b6c7d8_add_llm_model_config.py`、`datalogue-api/tests/test_llm_config.py`、`datalogue-web/src/components/settings.jsx`、`datalogue-web/src/styles.css`
- 关键改动：新增 LLM 模型配置和角色绑定表，前端系统设置页支持新增、编辑、启停、删除、测试模型，并按 `default/intent/dsl/sql_audit/report/annotation/blueprint` 绑定任务模型；API Key 复用 AES 加密落库，接口只返回 `api_key_set`；`get_llm` 保留 `ChatOpenAI` 作为 OpenAI-compatible 客户端，按数据库角色配置优先、`.env` 兜底创建模型客户端；问数链路、字段标注和蓝图分析调用均标注对应角色。
- 验证方式：执行 `.venv/bin/python -m py_compile app/models/llm.py app/schemas/llm.py app/services/llm_config.py app/api/llm.py app/graph/llm.py app/graph/nodes.py app/graph/workflow.py app/services/annotation.py app/services/blueprint_analyzer.py tests/test_llm_config.py`，通过；执行 `.venv/bin/alembic heads`，返回单一 head `e3f4a5b6c7d8`；执行 `.venv/bin/alembic upgrade head`，成功升级到 `e3f4a5b6c7d8`；执行 `.venv/bin/alembic current`，返回 `e3f4a5b6c7d8 (head)`；执行 `.venv/bin/pytest tests/test_llm_config.py tests/test_chat.py tests/test_sql_audit.py tests/test_analysis_blueprint.py -q`，128 passed、3 skipped；执行 `.venv/bin/pytest -q`，219 passed、4 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；通过内置浏览器打开 `http://localhost:5174/settings` 并切换到“LLM 模型”，确认角色绑定、模型表单和空列表渲染，浏览器控制台无 error。
- 残留风险：模型连接测试依赖真实 LiteLLM Proxy 或 OpenAI-compatible 网关可用；本轮不引入 LiteLLM Python SDK，也不实现多租户权限隔离或密钥轮换审计；本地 uvicorn 启动因沙箱阻止连接 `localhost:5432` 未完成真实后端页面联调，但后端 API 已由 TestClient 覆盖。

### 2026-06-10 13:43 · 设计开发方案 docx 补充 LiteLLM 配置化接入

- 涉及文件：`assets/documents/Datalogue_设计开发方案_v2.0_更新版_20260610.docx`、`scripts/update_litellm_docx.py`
- 关键改动：基于 20260605 版 docx 生成 20260610 更新版；封面更新为 v2.0.2；补充 `4.4 LLM 多来源配置` 的当前实现状态、验收结果和剩余边界；同步更新技术选型、双模部署、核心数据库设计、API 设计、P1 路线图、附录差距矩阵和后续任务 `T-014 LLM 故障降级与用量审计`。
- 验证方式：执行更新脚本生成 docx；执行关键内容抽取检查，确认 v2.0.2、`/api/llm/models`、`llm_model_config`、`T-014` 均已写入；执行 `unzip -t assets/documents/Datalogue_设计开发方案_v2.0_更新版_20260610.docx`，无压缩结构错误；执行 python-docx 打开检查，返回 `paragraphs=401 tables=39`。
- 残留风险：按文档处理规范尝试 `render_docx.py` 渲染页面图片，但 bundled LibreOffice 缺少 `liblcms2.2.dylib`，无法完成视觉 PNG QA；本轮完成结构级校验，后续如本机补齐 LibreOffice 依赖可再做页面级视觉验收。

### 2026-06-10 13:50 · LLM 配置表单下拉与自动填充

- 涉及文件：`datalogue-web/src/components/settings.jsx`、`datalogue-web/src/styles.css`
- 关键改动：系统设置的 LLM 模型表单新增接入模板下拉，内置 LiteLLM Proxy、OpenAI、DeepSeek、通义千问、MiniMax、Claude via LiteLLM 和自定义 OpenAI-compatible；供应商和模型名改为下拉选择，模板切换会自动填充供应商、Base URL、描述、默认模型和自动名称；模型选择会同步更新模型名，选择“自定义模型”时展示手动输入框；编辑已有模型时按模型名和 Base URL 反推模板，API Key 继续只支持写入或覆盖，不展示明文。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；通过内置浏览器打开 `http://localhost:5173/settings`，切换到“LLM 模型”，验证模板和模型下拉渲染，选择“通义千问 DashScope”后自动填充 Base URL、供应商、描述和名称，选择“自定义模型”后出现模型名输入框，浏览器控制台无 error。
- 残留风险：模板清单为前端内置常用值，后续如供应商模型命名变化需要同步更新；本轮只验证前端表单交互和构建，不连接真实模型网关做连接测试。

### 2026-06-10 13:55 · LLM 表单增加测试连接按钮

- 涉及文件：`datalogue-web/src/components/settings.jsx`、`datalogue-web/src/styles.css`
- 关键改动：在 LLM 模型编辑表单的保存区域新增“测试连接”按钮，复用已落地的 `/api/llm/models/{id}/test` 后端接口；未保存的新配置点击时给出“请先保存模型配置，再测试 LLM 连通性”提示，避免把未入库的 API Key 明文带入测试请求；列表行原有测试图标标题同步调整为“测试连接”；补充按钮组布局样式，保证“测试连接”和“保存”并排展示且窄宽度可换行。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；通过内置浏览器打开 `http://localhost:5173/settings`，切换到“LLM 模型”，确认表单操作区显示“测试连接 / 保存”，未保存配置点击“测试连接”后展示先保存提示，浏览器控制台无 error。
- 残留风险：真实连通性测试仍依赖已保存模型配置、后端服务和目标 OpenAI-compatible 网关可用；本轮仅验证前端按钮、提示和构建。
