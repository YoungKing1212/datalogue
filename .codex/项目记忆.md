# 项目记忆

## 开发标准记忆

- 默认中文沟通；用户为 yangkai / KenYang。
- 新增代码文件必须包含用途或关键职责注释，注释尽量使用中文。
- 新功能开工前先标记 `TODO`；功能完成后删除对应临时 `TODO`。
- 每完成一个功能，都要在本文件记录完成情况。
- 文档记录文件名尽量使用中文。
- 完成功能记录按时间顺序排列，使用 `YYYY-MM-DD HH:mm` 格式；较早记录在前，较晚记录在后。
- 完成功能记录应包含：完成时间、功能名称、涉及文件、关键改动、验证方式、残留风险或后续事项。
- 新增 Python 文件时必须使用项目指定文件头注释模板；创建时可保留模板 `TODO`，完成文件职责说明后替换为实际中文描述。

## 功能完成记录

### 2026-06-11 12:46 · 查询审计与内嵌 Trace 渲染

- 涉及文件：`datalogue-api/app/services/observability/traces.py`、`datalogue-api/app/api/observability.py`、`datalogue-api/tests/test_observability.py`、`datalogue-web/src/api/client.js`、`datalogue-web/src/components/audit-query.jsx`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/components/agent-panel.jsx`、`datalogue-web/src/styles.css`
- 关键改动：新增查询审计 trace 列表和详情 API，详情优先通过 Langfuse SDK v4 拉取 trace、observations、scores 和 metrics，失败时降级读取本地 `observability_trace_index`、消息 `response_metadata` 与 `step_trace`；前端“查询审计”页改为真实数据驱动，内嵌展示 trace 瀑布、输入输出、SQL、分数、失败原因和本地 fallback 步骤；消息下方“查看链路”和右侧 Trace 面板不再跳转 Langfuse，统一进入 Datalogue 内部 `/audit-query?trace_id=...`。
- 验证方式：执行 `./.venv/bin/python -m pytest tests/test_observability.py tests/test_conversation.py tests/test_chat.py -q`，87 passed、4 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；使用浏览器打开 `http://127.0.0.1:5173/audit-query`，确认查询审计页可加载真实 trace 列表和详情，控制台无新增错误。
- 残留风险：Langfuse 明细拉取依赖本地 API 进程已读取正确的 `LANGFUSE_PROJECT_ID/public_key/secret_key` 配置；Langfuse 不可用时页面只能展示本地索引和 `step_trace`，不会有完整 token/cost/observation 明细；Annotation、生产采样 eval 和成本治理仍在后续阶段任务中。

### 2026-06-11 12:38 · Langfuse Trace 深链直达

- 涉及文件：`datalogue-api/.env`、`datalogue-api/.env.example`、`datalogue-api/app/core/config.py`、`datalogue-api/app/services/observability/context.py`、`datalogue-api/app/services/observability/tracer.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/api/conversation.py`、`datalogue-api/tests/test_observability.py`、`datalogue-api/tests/test_conversation.py`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/components/agent-panel.jsx`、`datalogue-web/src/styles.css`
- 关键改动：新增 `LANGFUSE_PROJECT_ID` 配置，后端按 `{base_url}/project/{project_id}/traces/{trace_id}` 生成 `trace_url`，并写入 chat final payload、assistant message `response_metadata.langfuse` 与 `response_metadata.observability`；本地 `.env` 已根据 Langfuse Postgres 中 `api_keys.public_key` 查到的项目写入 `LANGFUSE_PROJECT_ID`；`/api/conversation/{id}` 返回历史消息时会动态补齐旧消息缺失的 `project_id/trace_url`，无需先做数据迁移；前端消息“查看链路”和右侧 Trace 面板均优先打开 `trace_url`，无深链时才降级打开 Langfuse 控制台；前端对旧历史消息可用 `base_url + project_id + trace_id` 兜底生成深链。
- 验证方式：执行 `./.venv/bin/python -m pytest tests/test_conversation.py tests/test_observability.py tests/test_chat.py -q`，86 passed、4 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；本地 Python 配置检查确认示例 trace URL 形态为 `http://localhost:3000/project/<project_id>/traces/trace-demo`。
- 残留风险：已有 Datalogue API 进程需要重启才能读取新增的 `LANGFUSE_PROJECT_ID`；旧历史消息如果当时没有保存 `trace_id`，仍无法生成 Langfuse 详情链接。

### 2026-06-11 12:04 · Langfuse Trace 前端面板接入与飞书后续任务

- 涉及文件：`datalogue-api/app/services/observability/context.py`、`datalogue-api/app/services/observability/tracer.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/components/chat-page.jsx`、`datalogue-web/src/components/agent-panel.jsx`、`datalogue-web/src/styles.css`
- 关键改动：后端 final `observability` 增加 Langfuse `base_url`；前端 chat adapter 在收到 final 后继续派发 `datalogue:trace`，ChatPage 记录当前回答的 `traceId/sessionId/messageId/observability`；右侧“Agent 执行过程”面板新增 Langfuse Trace 概览，展示上报状态、环境、版本、Prompt label、Trace ID、Session ID，并支持复制 ID 和打开 Langfuse 控制台；同步修正澄清解析节点直测断言，匹配内部状态字段 `clarification_resolution_result`；飞书创建 `Datalogue Langfuse 后续阶段 TODO` 清单并写入 Phase 2-4 的 9 个后续任务。
- 验证方式：执行 `./.venv/bin/python -m pytest tests/test_observability.py tests/test_chat.py -q`，81 passed、3 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；飞书任务清单创建成功，清单 GUID 为 `50372079-1e48-4d79-abd1-9c56e506017f`。
- 残留风险：前端当前只提供 Langfuse 控制台入口和 Trace/Session ID 复制，未生成 trace 深链，因为 Langfuse 深链需要 project id；真实 trace 上报仍依赖 Langfuse 项目 public/secret key 配置并重启 Datalogue API；飞书 CLI 提示当前 1.0.49 可升级到 1.0.51，尚未执行升级。

### 2026-06-11 12:14 · 修复历史消息 Trace 链路不可见

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/assistant/thread-list-adapter.js`、`datalogue-web/src/components/chat-page.jsx`、`datalogue-web/src/styles.css`
- 关键改动：修复历史消息看不到 Langfuse 链路的问题；后端保存 assistant message metadata 时补齐 `observability`、`base_url`、`enabled`、`active`；历史消息加载优先读取完整 `response_metadata.observability`，并兼容旧的 `response_metadata.langfuse`；AI 消息正文下方新增常显“查看链路”小条，展示 Trace ID、状态、环境和版本，支持复制 Trace ID、打开 Langfuse 控制台；点击“查看链路”会派发 trace final 事件并自动打开右侧“推理过程”面板，把历史消息的 Trace 元数据回填进去；右侧面板对旧历史 metadata 做兼容，只有明确 `enabled=false` 才显示“未启用”，已有 trace/session 的旧记录显示“可追踪”。
- 验证方式：执行 `./.venv/bin/python -m pytest tests/test_observability.py tests/test_chat.py -q`，81 passed、3 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：旧历史消息如果当时未保存 `base_url/enabled/active`，仍只能展示 Trace ID/Session ID，无法打开 Langfuse 控制台；真实后端链路上报还需要 `LANGFUSE_ENABLED=true` 且配置有效 public/secret key。

### 2026-06-11 11:25 · Langfuse 可观测第一阶段集成与本地部署

- 涉及文件：`datalogue-api/app/services/observability/`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/api/messages.py`、`datalogue-api/app/api/observability.py`、`datalogue-api/app/models/conversation.py`、`datalogue-api/alembic/versions/f1a2b3c4d5e6_add_observability_tables.py`、`datalogue-api/docker-compose.yml`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`
- 关键改动：新增基于 Langfuse Python SDK v4 的 `DatalogueTracer` 封装，提供 no-op 降级、contextvars、脱敏、Prompt Manager 兜底、feedback score 和本地观测报表聚合；`/api/chat/stream` 创建 trace/session，节点 step 映射 span，final 返回 `langfuse_trace_id/langfuse_session_id/observability/message_id` 并写入 assistant message metadata；新增 `POST /api/messages/{message_id}/feedback` 和 `/api/observability/summary|costs|quality|failures`；新增 `observability_trace_index` 与 `trace_annotation_candidate` 表；前端 AI 消息接收 trace metadata 并让点赞/点踩提交到后端；Docker Compose 增加 Langfuse Web/Worker、Postgres、ClickHouse、Redis、MinIO，独立于 Datalogue 业务库部署。
- 验证方式：执行 `python3 -m py_compile` 覆盖新增后端模块，通过；执行 `python3 -m pytest -q`，227 passed、74 warnings；执行 `python3 -m pytest tests/test_observability.py tests/test_chat.py -q`，84 passed、25 warnings；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；执行 `python3 -m alembic upgrade head && python3 -m alembic current`，业务库升级到 `f1a2b3c4d5e6 (head)`；执行 `docker compose -f datalogue-api/docker-compose.yml up -d langfuse-web langfuse-worker`，Langfuse 依赖容器 healthy，`curl -I http://localhost:3000` 返回 HTTP 200。
- 残留风险：Langfuse UI 已部署但尚未创建项目 API Key，`LANGFUSE_ENABLED` 默认仍为 `false`；真实 trace 上报需要在 Langfuse UI 创建 public/secret key 后写入 `.env` 并重启 Datalogue API；当前 Cost 先按本地 token 聚合占位，真实费用归因需后续补模型价格或直接读 Langfuse Metrics API；Prompt Manager 首批覆盖 `sql_audit` 和 `report_generate`，其他 prompt 仍待逐步迁移。

### 2026-06-11 11:35 · 修复 Langfuse SDK 运行环境缺失

- 涉及文件：`datalogue-api/pyproject.toml`、`datalogue-api/requirements.txt`、`datalogue-api/uv.lock`、`datalogue-api/tests/test_observability.py`
- 关键改动：使用 `uv sync` 将 `langfuse>=4,<5` 安装到项目 `.venv`，当前解析版本为 `langfuse 4.7.1`；更新 `uv.lock`，确保后续按 uv 安装时包含 SDK v4；测试中通过 monkeypatch 隔离 Langfuse score 调用，避免本机 `.env` 开启 Langfuse 时单测依赖真实服务。
- 验证方式：执行 `./.venv/bin/python -c "from langfuse import Langfuse; import langfuse; print(langfuse.__version__)"`，输出 `4.7.1`；执行 `./.venv/bin/python -m pytest tests/test_observability.py -q`，4 passed；执行 `./.venv/bin/python -m pytest tests/test_observability.py tests/test_chat.py -q`，80 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：如果 Datalogue API 不是通过 `datalogue-api/.venv/bin/uvicorn` 启动，而是用系统 `python3` 启动，仍需要切换到项目 `.venv` 或在对应解释器安装 `langfuse`；已运行的 API 进程建议重启一次，确保新依赖和最新配置生效。

### 2026-06-05 18:58 · 隐藏语义治理助手界面

- 涉及文件：`datalogue-web/src/components/datasets.jsx`
- 关键改动：移除数据集页面中 `DatasetAssistantPanel` 的 import 和挂载点，保留组件文件以便后续恢复。
- 验证方式：执行 `npm run lint` 和 `npm run build`，均通过；仅保留项目已有 warning 和 Vite 大包提示。
- 残留风险：`datalogue-web/src/components/dataset-assistant-panel.jsx` 仍保留但当前未引用，后续若长期不用可再评估删除依赖和样式。

### 2026-06-05 19:01 · 优化数据集数据预览组件

- 涉及文件：`datalogue-web/src/components/datasets.jsx`、`datalogue-web/src/styles.css`
- 关键改动：将数据预览区域抽成 `DataPreviewPanel`，增加表名摘要、字段数/样例行统计、刷新按钮、加载/空状态、粘性表头、横向滚动和单元格溢出处理。
- 验证方式：执行 `npm run lint` 和 `npm run build`，均通过；仅保留项目已有 warning 和 Vite 大包提示。
- 残留风险：本地 Vite dev server 在沙箱内首次监听端口失败，已通过授权启动过；浏览器可视化验收因随后切换到开发标准任务未继续执行。

### 2026-06-05 19:02 · 建立项目开发标准记忆

- 涉及文件：`AGENTS.md`、`.codex/项目记忆.md`
- 关键改动：新增项目级协作和开发标准，明确中文注释、TODO 生命周期、功能完成文档记录要求。
- 验证方式：文档类改动，无需运行构建或测试。
- 残留风险：后续每个功能完成后需要持续维护本文件，否则记录会失效。

### 2026-06-05 19:02 · 调整项目记忆文档规则

- 涉及文件：`AGENTS.md`、`.codex/项目记忆.md`
- 关键改动：将项目记忆文件改为中文文件名，明确功能完成记录按时间顺序排列，并统一时间格式为 `YYYY-MM-DD HH:mm`。
- 验证方式：文档类改动，无需运行构建或测试。
- 残留风险：`AGENTS.md` 属于 Agent 约定文件名，仍需保留英文名称。

### 2026-06-05 19:06 · 增加 Python 文件头注释模板

- 涉及文件：`AGENTS.md`、`.codex/项目记忆.md`
- 关键改动：新增 Python 文件头注释模板，明确 `${NAME}`、`${USER}`、`${DATE}` 占位含义，并要求完成文件职责说明后将模板 `TODO` 替换为实际中文描述。
- 验证方式：文档类改动，无需运行构建或测试。
- 残留风险：后续创建 Python 文件时需要人工或 Agent 严格套用该模板。

### 2026-06-08 10:20 · 问数入口意图分类与路由策略

- 涉及文件：`datalogue-api/app/graph/state.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`
- 关键改动：新增 QueryGraph 前置入口分类节点，扩展 `AgentState` 和 SSE final/step 元数据，支持识别指标查询、明细查询、分析蓝图、知识库问答、澄清追问和权限拒答；命中已发布分析蓝图时返回 `blueprint_id` 与匹配详情。
- 验证方式：2026-06-08 13:27 复核执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_chat.py tests/test_analysis_blueprint.py`，44 passed、3 skipped；执行 `.venv/bin/pytest`，120 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：分析蓝图已接入真实执行器；知识库问答当前仍以业务术语定义直答为主，尚未扩展为完整知识库检索链路。

### 2026-06-08 10:30 · 接入分析蓝图真实执行器

- 涉及文件：`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：新增分析蓝图执行服务，支持参数默认值和问题日期提取、只读 SQL 校验、参数绑定执行、结果结构化、用量日志写入和 `usage_count` 更新；聊天入口命中分析蓝图后进入真实执行节点并继续报告生成；蓝图管理的试运行接口由 mock 返回改为真实执行 SQL 模板。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，6 passed；执行 `.venv/bin/pytest tests/test_chat.py`，34 passed、3 skipped；执行 `.venv/bin/pytest`，116 passed、4 skipped；执行 `.venv/bin/python -m compileall app tests` 和 `git diff --check`，均通过。
- 残留风险：执行器当前只允许 `SELECT/WITH` 只读 SQL，不支持 `CREATE PROCEDURE` 或数据库存储过程定义本身；参数抽取仍是确定性规则，复杂自然语言参数后续需要接入更强的抽取器；`.venv` 中仍未安装 `ruff`。

### 2026-06-08 10:39 · 替换分析蓝图 SQL 分析 mock

- 涉及文件：`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：删除 `_mock_analyze_sql` 固定返回逻辑，新增真实 SQL 草稿分析服务；`analyze-sql` 与 `re-analyze` 现在基于输入 SQL 提取过程参数、占位符参数、最终 SELECT 输出列、来源表、触发词、触发示例、步骤、归因提示、整体置信度和低置信字段，结果继续兼容现有前端任务查询协议。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，7 passed；执行 `.venv/bin/pytest tests/test_chat.py`，34 passed、3 skipped；执行 `.venv/bin/pytest`，117 passed、4 skipped；执行 `.venv/bin/python -m compileall app tests` 和 `git diff --check`，均通过。
- 残留风险：当前 SQL 分析为确定性解析流程，不依赖 LLM；复杂嵌套 SQL、数据库特定语法和存储过程完整 AST 解析仍可能需要后续引入专业 SQL parser 或 LLM 审核增强；`.venv` 中仍未安装 `ruff`。

### 2026-06-08 10:48 · 分析蓝图 SQL 分析改为 AI 主流程

- 涉及文件：`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：`analyze_sql_for_blueprint` 改为调用项目统一 LLM 客户端 `get_llm`，由 AI 理解 SQL 草稿并生成蓝图名称、业务描述、参数口径、输出字段语义、步骤、触发示例、归因提示和低置信字段；原确定性 SQL 解析只作为 AI 输入参考和字段补齐兜底，不再作为主分析逻辑。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，7 passed；执行 `.venv/bin/pytest tests/test_chat.py`，34 passed、3 skipped；执行 `.venv/bin/pytest`，117 passed、4 skipped；执行 `.venv/bin/python -m compileall app tests` 和 `git diff --check`，均通过。
- 残留风险：测试中通过 mock LLM 验证调用路径，未访问真实外部模型；线上效果取决于 `OPENAI_API_KEY`、`OPENAI_BASE_URL` 和 `LLM_MODEL` 配置；`.venv` 中仍未安装 `ruff`。

### 2026-06-08 10:55 · 分析蓝图 SQL 变量占位保护

- 涉及文件：`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：AI 分析前先删除 `SET` / `DECLARE` 变量赋值语句，并将 `@var`、`${var}`、`#{var}`、`{{var}}` 等变量统一替换为 `:var` 占位符；AI 返回后再次净化 `raw_sql`、`call_template` 和参数 `default_expr`，避免变量实际值进入分析结果。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，8 passed；执行 `.venv/bin/pytest tests/test_chat.py`，34 passed、3 skipped；执行 `.venv/bin/pytest`，118 passed、4 skipped；执行 `.venv/bin/python -m compileall app tests` 和 `git diff --check`，均通过。
- 残留风险：当前变量识别覆盖常见 SQL/模板写法，复杂脚本变量或方言特有语法后续可能需要补充解析规则；`.venv` 中仍未安装 `ruff`。

### 2026-06-08 11:24 · 优化分析蓝图 AI 分析接口响应速度

- 涉及文件：`datalogue-api/app/api/dataset.py`、`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/app/graph/llm.py`、`datalogue-api/app/core/config.py`、`datalogue-api/.env.example`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-web/src/components/analysis-blueprints.jsx`
- 关键改动：`analyze-sql` 与 `re-analyze` 改为后台任务执行，接口先返回 `running` 和 `task_id`，前端按任务接口轮询最终结果；AI 分析服务记录预处理、提示词构建、LLM 调用、结果合并和总耗时；LLM 客户端改为进程内缓存，代理和超时改由 `OPENAI_PROXY_URL`、`LLM_TIMEOUT_SECONDS` 配置；压缩系统预解析参考，降低提示词体积；`.env.example` 中的 API Key 示例改为占位符。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，8 passed；执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest`，118 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：真实 AI 响应耗时仍主要取决于外部模型服务和网络；任务状态当前保存在进程内存，服务重启或多进程部署会丢失未完成任务，后续生产化需要迁移到数据库或队列。

### 2026-06-08 11:32 · 增强分析蓝图 AI 阶段耗时日志

- 涉及文件：`datalogue-api/app/api/dataset.py`、`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/app/graph/llm.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：后台任务日志新增 `task_id`、排队等待、SQL 长度、提示词长度、任务总耗时；AI 分析内部将耗时拆分为 SQL 预处理、提示词构建、LLM 客户端获取、LLM 请求、JSON 解析、结果合并和总耗时；LLM 工厂在缓存未命中时记录模型、base_url、代理启用状态和超时配置；测试补充耗时字段存在性断言。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行真实模型分析任务，结果为 `total=34785ms`、`llm_invoke=34608ms`、`llm_client=170ms`、`preprocess=4ms`、`prompt_build=0ms`、`parse_json=0ms`、`merge=0ms`；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，8 passed；执行 `.venv/bin/pytest`，118 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：当前主要耗时集中在外部模型请求阶段，后续若继续优化，需要从模型选择、非流式调用、输出 token 控制、超时/重试策略或供应商网络链路入手。

### 2026-06-08 11:38 · 分析蓝图 AI 分析改为单次同步请求

- 涉及文件：`datalogue-api/app/api/dataset.py`、`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：取消分析蓝图前端轮询任务结果的交互方式，避免模型耗时较长时持续请求后端；`analyze-sql` 与 `re-analyze` 现在在单次 POST 请求内同步执行 AI 分析并返回 `done` 或 `failed`，保留 `task_id` 和任务查询接口仅用于兼容历史调用；前端移除 `waitAnalyzeTask`、轮询间隔和 `getBlueprintAnalyzeTask` 引用。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，8 passed；执行 `.venv/bin/pytest`，118 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：单次请求会占用一个 HTTP 连接直到模型返回，若真实模型仍保持 30 秒以上耗时，需要关注网关、浏览器或反向代理超时；接口频次问题已通过取消轮询解决。

### 2026-06-08 11:47 · 防止分析蓝图推荐内容泄露 SQL 参数

- 涉及文件：`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：AI 分析提示词明确禁止在蓝图自然语言字段中输出 SQL 参数名、占位符或变量值；服务端合并 AI 输出时构造参数保护上下文，将原 SQL 中的 `@var`、`:var`、`${var}` 等参数名以及 `SET/DECLARE` 变量值纳入禁止集合；对 `name`、`description`、`trigger_keywords`、`trigger_examples`、`when_to_use`、`steps`、`attribution_hints`、`low_confidence_fields` 统一清理或过滤，避免推荐问法、触发词和步骤说明带出输入 SQL 的参数痕迹。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，8 passed；执行 `.venv/bin/pytest`，118 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：结构化 `parameters` 字段仍保留参数名，用于蓝图执行器参数绑定；本次清理针对用户可读的推荐问法、触发词、描述和步骤等自然语言字段。

### 2026-06-08 12:09 · 简化分析蓝图发布流程

- 涉及文件：`datalogue-api/app/api/dataset.py`、`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：移除发布分析蓝图前必须先测试成功的后端强校验，发布只负责将蓝图状态改为 `active` 并创建版本快照；前端向导移除“发布前必须先完成一次测试”的拦截提示；测试用例改为验证未测试蓝图也能发布并生成版本，同时保留试运行接口作为可选校验能力。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，9 passed；执行 `.venv/bin/pytest`，119 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：发布不再保证 SQL 可执行，运行时失败会通过蓝图执行日志体现；后续 UI 可用“未测试/上次测试失败”状态提醒，但不应阻塞发布。

### 2026-06-08 12:18 · 增加分析蓝图手动创建 AI 草稿

- 涉及文件：`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/app/schemas/__init__.py`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-web/src/api/client.js`、`datalogue-web/src/components/analysis-blueprints.jsx`
- 关键改动：新增 `BlueprintAnalyzeDescriptionPayload` 和 `/api/dataset/{ds_id}/blueprints/analyze-description` 接口，支持根据蓝图名称、业务场景、用户可能问法、希望输出和数据口径约束生成 `semantic_plan` 蓝图草稿；后端会把当前数据集指标、维度和业务术语作为 AI 参考；前端手动创建模式改为结构化业务场景表单，不再复用 SQL 粘贴输入；生成后沿用既有 AI 草稿审核、高级配置、保存和发布流程。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，10 passed；执行 `.venv/bin/pytest`，120 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：手动创建蓝图默认 `implementation_type=semantic_plan`，不包含可执行 SQL；发布后如果进入现有 SQL 执行器会因缺少执行 SQL 而失败，后续需要让运行时支持语义计划蓝图或在发布前关联执行逻辑。

### 2026-06-08 12:24 · 优化手动创建蓝图填写提示

- 涉及文件：`datalogue-web/src/components/analysis-blueprints.jsx`
- 关键改动：手动创建蓝图表单新增“填写示例”区域，展示业务场景、希望输出、用户问法和口径约束的完整样例；蓝图名称、用户可能问法、业务场景、希望输出、数据口径约束字段补充 placeholder 与字段提示；手动创建表单和示例区域改为自适应网格，降低窄屏下文案和输入框拥挤风险。
- 验证方式：执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过。
- 残留风险：当前示例为通用零售销售波动场景，后续可根据数据集行业或已有业务术语动态生成更贴近当前数据集的示例。

### 2026-06-08 12:46 · 重构分析蓝图创建向导 UI

- 涉及文件：`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-web/src/styles.css`
- 关键改动：参考 assistant-ui 的 headless primitives 组合思路，将新建分析蓝图弹窗重构为“左侧创建方式与流程导航、右侧步骤工作区、底部动作栏”的向导结构；手动创建模式新增 AI 输入摘要侧栏和“套用示例”动作，帮助用户理解 AI 将读取的业务语义；SQL 模式改为更清晰的代码输入 composer；第 2-4 步统一为审核、配置、测试面板；补充桌面与移动端响应式样式，移动端保留简洁模式与步骤名称，避免长说明挤压表单。
- 验证方式：执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；使用本地 Vite 页面检查桌面和移动宽度下弹窗渲染、手动创建切换、套用示例与 AI 输入摘要联动。
- 残留风险：当前是项目内自定义 UI 组件，并未直接接入 `@assistant-ui/react` 运行时；若后续要把蓝图创建过程做成真实对话式 Copilot，需要进一步接入 assistant-ui runtime 和工具调用 UI。

### 2026-06-08 13:10 · 修复分析蓝图详情功能节点

- 涉及文件：`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-web/src/styles.css`
- 关键改动：将分析蓝图详情卡片中的“概览、参数、输出列、业务逻辑、测试、使用记录”改为真实可切换子页面；参数和输出列统计卡可跳转对应页面；测试页支持按蓝图参数填写测试值并调用真实试运行接口；使用记录页接入真实统计和日志接口；详情动作区接入发布、弃用和重新启用状态流转，已发布蓝图禁用重复发布；补充字符串和对象两类参数、输出列、步骤结构兼容，避免历史数据或 AI 生成字段显示成“未命名”。
- 验证方式：执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py`，10 passed；执行 `git diff --check`，通过；使用本地 Vite 页面验证 6 个详情子页可切换，参数、输出列、业务逻辑、测试参数、使用统计和测试/发布/弃用按钮均正常展示。
- 残留风险：浏览器验收未点击真实“弃用/重新启用”，避免改变当前演示数据状态；接口链路由后端蓝图测试覆盖。发布操作仍不强制要求测试成功，运行失败会落到使用日志中。

### 2026-06-08 16:29 · 修复入口分类、蓝图执行和 SSE 序列化问题

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：增强 `_extract_node_output`，递归兼容 LangGraph `astream_events(version="v2")` 的扁平输出、`{节点名: 输出}`、多层 `output` 包装和图级最终状态输出；只有提取到真实 `AgentState` 字段时才标记节点完成，避免 `entry_intent_classification` 和 `analysis_blueprint_execute` 的输出被空事件吞掉，导致 final 走通用兜底；新增 `_sse_data`，所有 SSE step/token/final payload 统一经 `jsonable_encoder` 后再 `json.dumps`，避免蓝图结果行中的 `datetime`、`date`、`Decimal` 导致 `Object of type datetime is not JSON serializable`；蓝图执行参数抽取新增中文年份/月度范围和“某人的日报/任务/记录”姓名识别，支持“我要查询2024年杨凯的日报”提取 `person_name=杨凯`、`start_date=2024-01-01`、`end_date=2024-12-31`。
- 验证方式：执行 `.venv/bin/pytest tests/test_chat.py`，37 passed、3 skipped；执行 `.venv/bin/pytest tests/test_analysis_blueprint.py tests/test_chat.py`，50 passed、3 skipped；执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest`，126 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：当前兼容 LangGraph 常见输出形态；如果后续升级 LangGraph 引入新的事件包装层，需要继续扩展 `_extract_node_output`。姓名抽取只覆盖常见中文姓名问法，复杂组织/多人条件仍需要更强的参数抽取器。

### 2026-06-08 16:56 · 手动分析蓝图接入语义计划执行链路

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`
- 关键改动：`semantic_plan` 类型的手动分析蓝图不再进入 SQL 模板执行器，也不再要求用户配置可执行 SQL；蓝图执行节点会将名称、适用场景、业务描述、触发问法、参数、输出列、业务步骤和归因提示整理为 `blueprint_context`，再由工作流路由回 `schema_recall`，作为数据集级 LLM 约束进入 QueryGraph/NL2SQL；SQL 导入蓝图仍继续使用原有真实 SQL 执行器。
- 验证方式：执行 `.venv/bin/pytest tests/test_chat.py tests/test_analysis_blueprint.py`，53 passed、3 skipped；执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest`，129 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：语义计划蓝图的实际查询质量仍取决于当前数据集语义层、所选表结构和 LLM 生成 SQL 的能力；如果蓝图步骤需要多 SQL、多阶段计算或临时表，后续还需要实现更完整的计划执行器。

### 2026-06-08 17:17 · 数据集级 SQL 查询约束配置

- 涉及文件：`datalogue-api/app/models/dataset.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/app/utils/query_constraints.py`、`datalogue-api/app/utils/prompt.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/alembic/versions/b2c4d6e8f901_add_dataset_query_constraints.py`、`datalogue-api/tests/test_dataset.py`、`datalogue-api/tests/test_dataset_prompt_instructions.py`、`datalogue-web/src/components/datasets.jsx`
- 关键改动：为数据集新增 `query_constraints` JSON 配置，默认启用“未指定时间查最近 30 天、默认 LIMIT 100、最大 LIMIT 1000”；数据集创建和更新接口会归一化并裁剪配置；Schema prompt、真实 Schema SQL 生成、推断 SQL 生成、语义 DSL 生成和 DSL 编译器统一读取该配置；前端数据集右键菜单新增“查询约束”入口，弹窗支持开关、默认时间范围、默认返回条数、最大返回条数，并保留原有自定义 LLM 约束；新建数据集默认开启查询约束。
- 验证方式：执行 `.venv/bin/pytest tests/test_dataset.py tests/test_dataset_prompt_instructions.py tests/test_chat.py`，77 passed、3 skipped；执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest`，136 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `.venv/bin/alembic heads`，返回单一 head `b2c4d6e8f901`；执行 `git diff --check`，通过。
- 残留风险：该配置约束的是 AI 生成和语义 DSL 编译路径；用户手写 SQL、分析蓝图 SQL 模板或历史已保存 SQL 不会被强制改写，后续如要覆盖这几类路径，需要在 SQL 执行前增加统一重写/审计层。

### 2026-06-09 11:12 · T-015 升级 NL2DSL 资产引用 Schema

- 涉及文件：`datalogue-api/app/schemas/dsl.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/docs/NL2DSL资产引用Schema.md`、`datalogue-api/tests/test_dsl_schema.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/types/nl2dsl.d.ts`、`datalogue-web/src/assistant/chat-adapter.js`
- 关键改动：新增 NL2DSL v2 资产引用模型，统一表达 `asset_type`、`asset_id`、`confidence` 和 `ambiguities`；新增 `normalize_dsl` 兼容旧版 `metrics: ["gmv"]`、`dimensions: ["region"]` 字符串 DSL；语义层 Schema 召回补充指标、维度、业务术语和已发布分析蓝图的资产 ID；DSL 生成提示词升级为资产引用格式；DSL 校验和编译器改为新旧双读；SSE final payload 和前端 assistant metadata 透出最终 DSL；前端新增 `nl2dsl.d.ts` 类型声明。
- 验证方式：执行 `.venv/bin/pytest tests/test_dsl_schema.py tests/test_chat.py -q`，46 passed、3 skipped；执行 `.venv/bin/pytest -q`，141 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过。
- 残留风险：当前编译器仍只使用资产引用中的 `name` 生成 SQL，`asset_id` 主要用于审计、前端展示和后续澄清；`ambiguities` 已结构化保留但暂不触发自动澄清路由，后续可以在入口分类或 DSL 校验后增加歧义确认节点。

### 2026-06-09 11:38 · 分析蓝图补充创建来源标记

- 涉及文件：`datalogue-api/app/models/dataset.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/alembic/versions/c9d1e2f3a4b5_add_blueprint_creation_source.py`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-web/src/styles.css`
- 关键改动：为分析蓝图新增 `creation_source`、`ai_generated`、`ai_generation_type`，用于稳定区分 `manual`、`sql_ai_analysis` 和 `manual_ai_draft`；SQL AI 分析任务返回 `sql_ai_analysis/sql_analysis`，业务描述 AI 草稿返回 `manual_ai_draft/description_analysis`；创建、列表、详情、版本快照和回滚均保留来源字段；前端蓝图列表和详情头部展示“AI SQL分析 / AI业务草稿 / 手动填写”来源标签。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py -q`，13 passed；执行 `.venv/bin/pytest -q`，142 passed、4 skipped；执行 `.venv/bin/alembic heads`，返回单一 head `c9d1e2f3a4b5`；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过。
- 残留风险：历史蓝图迁移后默认标记为 `manual`，无法追溯历史创建时是否经过 AI；如果需要修正历史数据，需要额外基于操作日志或字段特征做一次人工确认脚本。

### 2026-06-09 12:04 · T-016 语义资产解析器替换指标解析节点

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`、`datalogue-web/src/components/chat-page.jsx`
- 关键改动：新增 `semantic_asset_resolution_node` 并替换工作流中的 `metric_resolution_node`，统一解析业务术语、指标、维度、源字段和分析蓝图资产；Schema 召回补充 `fields` 结构化字段资产和业务术语 `asset_links`；解析结果输出 `assets/terms/metrics/dimensions/fields/blueprints/ambiguities/unresolved`，包含 `asset_id`、`asset_type`、`confidence`、`match_type`；命中业务术语后自动扩展显式关联资产；保留旧 `metric_resolution` 输出和兼容包装函数，供 DSL 生成、SQL 审计和历史前端继续使用；SSE、final payload 和前端 reasoning 展示新增“语义资产解析”节点。
- 验证方式：执行 `.venv/bin/python -m py_compile app/graph/nodes.py app/api/chat.py app/graph/workflow.py app/graph/state.py`，通过；执行 `.venv/bin/pytest tests/test_chat.py -q`，47 passed、3 skipped；执行 `.venv/bin/pytest -q`，146 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过。
- 残留风险：当前解析器是确定性规则和轻量上下文偏置，不额外调用 LLM；歧义会进入 `semantic_asset_resolution.ambiguities` 并写入 DSL prompt，但还没有在工作流中强制打断并向用户发起澄清。

### 2026-06-09 12:38 · T-004 业务术语运行时归一化

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`
- 关键改动：在 QueryGraph 中新增 `term_normalize_node`，工作流调整为 `schema_recall -> term_normalize_node -> semantic_asset_resolution_node -> dsl_generate`；术语节点基于业务术语 `name/display_name/aliases` 做确定性匹配，将命中的同义词注入 `entities.terms` 和 `term_normalization`；Schema 结构化术语补充 `forbidden_aliases`；DSL 生成和 SQL 审计 prompt 注入业务术语归一化结果；当同一个名称或同义词命中多个业务术语时，节点直接返回 `term_conflict_clarification`，进入澄清而不是继续生成 SQL；前端推理链新增“术语归一化”节点展示命中数和冲突数。
- 验证方式：执行 `.venv/bin/python -m py_compile app/graph/nodes.py app/graph/workflow.py app/graph/state.py app/api/chat.py`，通过；执行 `.venv/bin/pytest tests/test_chat.py -q`，51 passed、3 skipped；执行 `.venv/bin/pytest -q`，150 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过。
- 残留风险：术语归一化当前只做确定性文本匹配，不做 LLM 语义改写；冲突澄清已在后端 route payload 中返回，前端暂沿用通用回答展示，后续可做专门的术语冲突确认交互。

### 2026-06-09 13:29 · T-003 真实蓝图测试执行增强

- 涉及文件：`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/app/models/dataset.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/alembic/versions/d4e5f6a7b8c9_add_blueprint_usage_log_details.py`、`datalogue-api/tests/test_analysis_blueprint.py`
- 关键改动：分析蓝图真实执行器读取 `timeout_seconds`，默认 30 秒，并按 Postgres、MySQL/MariaDB、SQLite 和支持 `call_timeout` 的驱动 best-effort 设置数据库级超时；执行结果返回前统一脱敏手机号、邮箱、身份证和 token/key/secret/password 等敏感字段和值；缺参、非只读 SQL、超时和执行异常统一返回 `diagnosis.code/title/detail/suggested_action`；测试接口透出 `row_count`、`diagnosis`、`masking_summary`；`blueprint_usage_log` 新增 `row_count` 和 `diagnosis`，成功/失败测试均保留日志但 `count_usage=False` 不增加 `usage_count`。
- 验证方式：执行 `.venv/bin/pytest tests/test_analysis_blueprint.py -q`，17 passed；执行 `.venv/bin/pytest -q`，154 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：发布流程仍按产品决策保持“测试作为发布前可选校验”，不恢复“未测试成功不能发布”的强阻塞；数据库级超时是 best-effort，具体可用性取决于数据源方言和驱动支持。

### 2026-06-09 13:47 · T-017 数据集问数上下文组装服务

- 涉及文件：`datalogue-api/app/services/dataset_context.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_dataset_context.py`、`datalogue-api/tests/test_chat.py`
- 关键改动：新增 `build_dataset_query_context` 服务，集中组装 SQL 生成所需的数据集上下文，包含数据集说明、查询约束、已选表字段、字段样例、指标、维度、业务术语、分析蓝图和权限信息；`schema_recall_node` 改为调用该服务并继续透出 `schema_context`、`schema_structured`、`ddl_context`、`query_constraints`、`dataset_prompt_instructions` 等兼容字段；上下文 prompt 支持 `token_budget` 裁剪，问题文本或语义解析显式命中的资产会被优先保留；新增 `dataset_context_debug`，记录资产总数、保留数量、预算、估算 token、被裁剪资产和命中资产摘要，并在 SSE final payload 中透出。
- 验证方式：执行 `.venv/bin/pytest tests/test_dataset_context.py tests/test_chat.py -q`，53 passed、3 skipped；执行 `.venv/bin/pytest -q`，156 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：预算估算采用轻量字符折算，不是模型 tokenizer 的精确 token；权限信息当前为“未配置数据集级权限策略”的占位摘要，后续接入真实权限模型后需要由本服务统一渲染。

### 2026-06-09 16:24 · T-019 SQL 静态安全校验与方言规范化

- 涉及文件：`datalogue-api/app/utils/sql_guard.py`、`datalogue-api/app/utils/__init__.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/tests/test_sql_guard.py`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-api/tests/test_chat.py`
- 关键改动：新增统一 SQL Guard，执行前静态拦截空 SQL、多语句、非 `SELECT/WITH`、DML/DDL/DCL/TCL 关键字、危险函数和文件导出语法；注释和字符串内容会被屏蔽后再扫描，避免误杀展示文本；按数据源方言补齐或裁剪结果行数限制，Oracle 使用 `FETCH FIRST ... ROWS ONLY`，MySQL/Postgres/SQLite 使用 `LIMIT`；问数 `direct_sql`、真实 Schema SQL、语义 DSL 编译 SQL、最终 SQL 执行节点和分析蓝图 SQL 模板执行统一接入 Guard，并透出结构化 `sql_guard` 信息和明确错误原因。
- 验证方式：执行 `.venv/bin/python -m py_compile app/utils/sql_guard.py app/graph/nodes.py app/services/analysis_blueprint.py tests/test_sql_guard.py tests/test_analysis_blueprint.py tests/test_chat.py`，通过；执行 `.venv/bin/pytest tests/test_sql_guard.py tests/test_analysis_blueprint.py tests/test_chat.py -q`，86 passed、3 skipped；执行 `.venv/bin/pytest -q`，174 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：首版方言规范化聚焦在执行前安全、注释/字符串感知扫描、`NULL` 比较修正和 LIMIT/FETCH 行数限制；复杂跨方言函数转换仍需要后续增强。

### 2026-06-09 17:12 · T-019 引入 SQLGlot AST 解析

- 涉及文件：`datalogue-api/app/utils/sql_guard.py`、`datalogue-api/pyproject.toml`、`datalogue-api/requirements.txt`、`datalogue-api/uv.lock`、`datalogue-api/tests/test_sql_guard.py`、`datalogue-api/tests/test_chat.py`
- 关键改动：引入 `sqlglot==30.9.0`，SQL Guard 改为先做注释/字符串感知的高风险扫描，再用 SQLGlot 按目标方言解析 AST；通过 AST 判断单语句、只读 Query、DML/DDL/命令类表达式和危险函数，并使用 SQLGlot AST 生成目标方言 SQL；外层 LIMIT/FETCH 由 AST 统一补齐或裁剪，Oracle 输出 `FETCH FIRST ... ROWS ONLY`，MySQL/Postgres/SQLite 输出 `LIMIT`。
- 验证方式：执行 `.venv/bin/python -m py_compile app/utils/sql_guard.py app/graph/nodes.py app/services/analysis_blueprint.py tests/test_sql_guard.py tests/test_analysis_blueprint.py tests/test_chat.py`，通过；执行 `.venv/bin/pytest tests/test_sql_guard.py tests/test_analysis_blueprint.py tests/test_chat.py -q`，86 passed、3 skipped；执行 `.venv/bin/pytest -q`，174 passed、4 skipped；执行 `git diff --check`，通过。
- 残留风险：SQLGlot 已覆盖 AST 解析和基础方言输出，但业务上更复杂的跨方言函数等价转换、数据库私有函数白名单和更细粒度的子查询限制策略仍需按真实数据源错误继续补齐。

### 2026-06-09 17:42 · T-020 SQL 执行失败诊断节点

- 涉及文件：`datalogue-api/app/utils/sql_diagnosis.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/models/conversation.py`、`datalogue-api/app/models/__init__.py`、`datalogue-api/app/prompts/sql_audit.py`、`datalogue-api/alembic/versions/f6a7b8c9d0e1_add_sql_diagnosis_log.py`、`datalogue-api/tests/test_sql_audit.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`
- 关键改动：新增 `sql_diagnosis` 确定性诊断工具，稳定分类字段不存在、表未选、表不存在、聚合错误、类型错误、权限不足、语法/方言错误、超时和未知执行错误；`sql_audit_node` 改为“确定性分类优先 + LLM 补充解释”，保留旧 `root_cause/wrong_field/suggested_fix/severity` 字段并新增 `code/category/title/detail/suggested_action/retryable/evidence/original_error`；工作流路由改为按 `retryable` 和 `retry_count < 3` 决定是否重试；新增 `sql_diagnosis_log` 表落库诊断；聊天 SSE、final payload、历史 `step_trace` 和前端 reasoning 透出 SQL 诊断摘要与 metadata。
- 验证方式：执行 `.venv/bin/python -m py_compile app/utils/sql_diagnosis.py app/graph/nodes.py app/api/chat.py app/graph/workflow.py app/models/conversation.py tests/test_sql_audit.py tests/test_chat.py`，通过；执行 `.venv/bin/pytest tests/test_sql_audit.py tests/test_chat.py -q`，83 passed、3 skipped；执行 `.venv/bin/pytest -q`，187 passed、4 skipped；执行 `npm run lint`，0 errors、21 warnings；执行 `npm run build`，通过；执行 `.venv/bin/alembic heads`，返回单一 head `f6a7b8c9d0e1`；执行 `git diff --check`，通过。
- 残留风险：诊断日志本轮只保证落库，不提供查询接口和治理看板；确定性分类已覆盖主流错误文本，但不同数据库驱动的错误文案仍需随真实线上样本继续补充规则。

### 2026-06-09 17:51 · 修复 SQL 诊断日志迁移撞表

- 涉及文件：`datalogue-api/alembic/versions/f6a7b8c9d0e1_add_sql_diagnosis_log.py`
- 关键改动：`sql_diagnosis_log` 迁移改为使用 SQLAlchemy inspector 做存在性检查；当目标库中表已存在但 Alembic revision 尚未升级时，跳过建表并补齐缺失索引，避免 `psycopg2.errors.DuplicateTable: relation "sql_diagnosis_log" already exists`；downgrade 同样检查表和索引是否存在，避免对象缺失时报错。
- 验证方式：执行 `.venv/bin/python -m py_compile alembic/versions/f6a7b8c9d0e1_add_sql_diagnosis_log.py`，通过；执行 `.venv/bin/alembic upgrade head`，通过并升级到 `f6a7b8c9d0e1`；执行 `.venv/bin/alembic current`，返回 `f6a7b8c9d0e1 (head)`；执行 `.venv/bin/pytest tests/test_sql_audit.py -q`，24 passed。
- 残留风险：如果既有 `sql_diagnosis_log` 表缺少列或外键，本迁移只补索引，不做列级补齐；当前报错场景是表已存在导致撞表，后续如遇历史手工表结构不完整，需要再加列级检查。

### 2026-06-09 17:57 · T-021 SQL 自动修复与重试闭环

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_sql_audit.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`
- 关键改动：新增 `sql_retry_trace` 状态字段，记录每次自动修复重试的 `attempt/max_attempts/original_sql/repair_reason/diagnosis_code/repaired_sql/status/result/error/row_count`；`sql_audit_node` 在可修复且未超过 `max_retry_count` 时登记待重试记录，并把结构化 `retry_decision` 写入诊断；SQL 执行、DSL 校验和 DSL 编译失败会回填最近一次重试结果，成功执行会记录返回行数；工作流把最大重试次数从硬编码 3 收敛为 `max_retry_count`，默认仍为 3；SSE/final payload、前端 reasoning 和 assistant metadata 透出自动修复重试链路，达到上限后最终回答会解释无法继续安全重试。
- 验证方式：执行 `.venv/bin/python -m py_compile app/graph/nodes.py app/graph/workflow.py app/graph/state.py app/api/chat.py tests/test_sql_audit.py tests/test_chat.py`，通过；执行 `.venv/bin/pytest tests/test_sql_audit.py tests/test_chat.py -q`，86 passed、3 skipped；执行 `.venv/bin/pytest -q`，190 passed、4 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过。
- 残留风险：本轮自动修复仍以“重新生成 DSL/SQL + SQL Guard 安全校验”为边界，不做直接字符串 patch 执行；若重试在真实数据库外部异常或数据源连接层失败，仍会按诊断结果终止并解释原因。

### 2026-06-09 18:08 · T-022 回答解释与低置信确认

- 涉及文件：`datalogue-api/app/services/answer_explanation.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/models/conversation.py`、`datalogue-api/app/schemas/conversation.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/prompts/report_generate.py`、`datalogue-api/alembic/versions/0f1e2d3c4b5a_add_message_response_metadata.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_conversation.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：新增 `answer_explanation` 解释包，统一汇总口径说明、数据来源、SQL 摘要、置信度、风险提示和低置信确认提示；解释包优先读取语义资产、DSL、蓝图路由、SQL 诊断和数据集上下文裁剪信息，并用 SQLGlot 解析 SQL 表字段作为兜底；聊天 final 回答追加确定性 Markdown，同时 SSE final 和 assistant metadata 透出结构化解释包；`message` 新增 `response_metadata` JSON 字段并通过 conversation API 历史恢复；前端消息气泡新增“口径与可信度”信息区，低置信时展示确认提示。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_chat.py tests/test_conversation.py`，68 passed、4 skipped；执行 `.venv/bin/pytest`，195 passed、4 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：解释包为确定性规则生成，低置信阈值暂定 `0.75`；实际生产中不同数据源方言或复杂 SQL 可能让 SQL 摘要降级为预览，后续可根据真实问数日志继续细化置信度扣分和风险分类。

### 2026-06-09 18:16 · T-022 解释信息改为气泡折叠栏

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：取消将解释包 Markdown 直接追加到回答正文，避免口径说明、数据来源、SQL 摘要、置信度和风险提示在正文中展开重复展示；前端“口径与可信度”改为消息气泡内的可点击折叠栏，默认折叠，只展示置信度、风险数量和是否需要确认，点击后展开数据来源、SQL 摘要、风险提示和低置信确认。
- 验证方式：执行 `.venv/bin/python -m compileall app tests`，通过；执行 `.venv/bin/pytest tests/test_chat.py tests/test_conversation.py`，68 passed、4 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：当前折叠状态为单条消息本地状态，刷新后默认仍折叠；如果后续需要记住用户展开/折叠偏好，需要额外加入前端持久化。

### 2026-06-09 18:17 · SQL 查询结果默认折叠

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：查询结果表格改为消息气泡内的可点击折叠栏，默认折叠，只展示“查询结果”和返回行数；点击后展开完整表格；折叠头部增加 hover 状态和箭头旋转，和“口径与可信度”折叠栏保持一致。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：折叠状态仍为单条消息本地状态，刷新后默认折叠；如果后续需要记住用户展开结果表的偏好，需要加入前端持久化。

### 2026-06-09 18:19 · 生成 SQL 气泡支持复制

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：生成 SQL 折叠气泡头部新增复制按钮，点击只复制当前 SQL，不触发展开/收起；复制成功后短暂显示“已复制”；外层折叠头保持可点击和键盘 Enter/空格切换，避免嵌套 button 造成无效 DOM。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：复制能力依赖浏览器 `navigator.clipboard`；若在不支持剪贴板 API 的环境中，会在控制台记录错误但不影响 SQL 展示。

### 2026-06-09 18:20 · 风险提示按序号逐行展示

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：“口径与可信度”折叠栏展开后，风险提示从分号拼接文本改为有序列表；存在多条风险时按 `1. 2. 3.` 序号逐行列出，无风险时继续显示“未发现明显风险”；补充列表缩进、行高和换行样式，避免长风险文案溢出气泡。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：风险条目顺序沿用后端解释包生成顺序，前端不额外按严重程度排序。

### 2026-06-09 18:22 · 生成 SQL 气泡轻量化

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：生成 SQL 折叠气泡新增 `sql-collapse` 专用样式，避免继续套用较厚重的通用折叠块；头部改为更低高度、更窄 padding、更弱边框和更轻背景，标题从“生成的 SQL”收敛为“SQL”；复制按钮压缩为轻量工具按钮；展开后的 SQL 代码区减少内边距、字号和行高，让 SQL 组件更适合消息流 UI。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：当前只轻量化消息气泡里的 SQL 折叠块，不影响 AgentPanel 和其他页面的 SQL 预览组件。

### 2026-06-09 18:26 · 修复 SQL 按钮尺寸污染

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：SQL 折叠按钮的箭头图标从通用 `chev` 类切换为 `sql-collapse-chev`，避免被页面其它 `.chev` 样式污染导致图标异常放大；SQL 气泡宽度保持 `100%`，与上方“口径与可信度”和“查询结果”折叠栏同宽，只收紧内部高度、padding、字号和复制按钮尺寸；补充独立箭头尺寸和旋转样式，适配当前 assistant-ui 消息流组件风格。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：如果后续继续新增全局短类名样式，消息内小组件仍需优先使用局部命名类，避免再次出现样式污染。

### 2026-06-09 19:49 · 修复历史会话数据集绑定丢失

- 涉及文件：`datalogue-api/app/models/conversation.py`、`datalogue-api/app/schemas/conversation.py`、`datalogue-api/app/api/conversation.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/alembic/versions/a1b2c3d4e5f6_add_dataset_id_to_conversation.py`、`datalogue-api/tests/test_conversation.py`、`datalogue-web/src/components/chat-page.jsx`、`datalogue-web/src/api/client.js`、`datalogue-web/src/assistant/thread-list-adapter.js`
- 关键改动：`Conversation` 新增 nullable `dataset_id` 并补 Alembic 迁移；chat 流在新建或继续会话时写入/更新会话绑定数据集，请求未带 `dataset_id` 时优先沿用会话绑定值；conversation 详情接口返回 `dataset_id`；ChatPage 切换 `/chat/:id` 历史会话时读取会话详情并恢复 `selectedDs`，同步给 `datasetIdRef`，保证后续问数继续携带对应数据集。
- 验证方式：执行 `.venv/bin/python -m py_compile app/api/chat.py app/api/conversation.py app/models/conversation.py app/schemas/conversation.py alembic/versions/a1b2c3d4e5f6_add_dataset_id_to_conversation.py tests/test_conversation.py`，通过；执行 `.venv/bin/alembic upgrade head`，成功从 `0f1e2d3c4b5a` 升级到 `a1b2c3d4e5f6`；执行 `.venv/bin/pytest tests/test_conversation.py -q`，5 passed、1 skipped；执行 `.venv/bin/pytest -q`，202 passed、5 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：旧会话如果没有 `Conversation.dataset_id`，无法自动恢复数据集，需要用户手动选择一次并在后续问数时写回会话绑定。

### 2026-06-09 22:24 · T-023 语义验证增强：覆盖术语和蓝图

- 涉及文件：`datalogue-api/app/models/dataset.py`、`datalogue-api/app/models/__init__.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/app/schemas/__init__.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/alembic/versions/b7c8d9e0f1a2_add_semantic_validation_case.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-web/src/api/client.js`、`datalogue-web/src/components/datasets.jsx`
- 关键改动：新增 `semantic_validation_case` 表和数据集验证用例列表/保存 API；数据集语义验证 Tab 汇总展示入口路由、普通问数路径、术语命中、蓝图命中、生成 SQL、失败原因、置信度风险和节点轨迹，并支持保存/查看最近验证用例；DSL 校验节点补充对 `terms` 和 `blueprints` 的资产名称/ID 校验，避免报告把不存在的术语或蓝图引用判为有效；修复数据集页面默认展示首个数据集但未写入 `activeDsId` 导致语义验证工作台不渲染的问题。
- 验证方式：执行 `.venv/bin/python -m py_compile app/models/dataset.py app/models/__init__.py app/schemas/dataset.py app/schemas/__init__.py app/api/dataset.py app/graph/nodes.py tests/test_chat.py tests/test_analysis_blueprint.py alembic/versions/b7c8d9e0f1a2_add_semantic_validation_case.py`，通过；执行 `.venv/bin/pytest tests/test_chat.py tests/test_analysis_blueprint.py -q`，88 passed、3 skipped；执行 `.venv/bin/pytest -q`，202 passed、4 skipped；执行 `.venv/bin/alembic heads`，返回单一 head `b7c8d9e0f1a2`；执行 `.venv/bin/alembic upgrade head`，成功从 `a1b2c3d4e5f6` 升级到 `b7c8d9e0f1a2`；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；通过内置浏览器打开 `http://127.0.0.1:5173/`，进入数据集页并确认“语义层验证”和“最近保存用例”区域渲染。
- 残留风险：本轮保存的是验证结果快照，尚未实现批量回放调度、用例断言规则和定时评测；真实试问仍依赖当前问数链路和 LLM/数据源可用性。

### 2026-06-10 10:32 · T-018 术语冲突澄清回复闭环

- 涉及文件：`datalogue-api/app/models/conversation.py`、`datalogue-api/app/models/__init__.py`、`datalogue-api/app/schemas/chat.py`、`datalogue-api/app/schemas/__init__.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/alembic/versions/c1d2e3f4a5b6_add_pending_clarification.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/styles.css`
- 关键改动：新增 `pending_clarification` 持久化表，按会话和数据集记录术语冲突澄清态、候选项、原问题、过期时间和解决结果；`ChatRequest` 支持 `clarification_response`；QueryGraph 入口新增 `clarification_resolution_node`，可解析结构化按钮选择、自然语言“第一个/1”和术语名称，成功后恢复原问题并携带 `selected_term_id` 直接进入 schema recall 后续链路；`term_normalize_node` 支持已选术语口径，过滤冲突候选并避免再次打断；SSE final 和 assistant metadata 透出 `clarification_id/candidates/expires_at`；前端 AI 消息展示术语候选卡片，点击候选后填入输入框并随下一轮请求携带一次性结构化澄清回复。
- 验证方式：执行 `.venv/bin/python -m py_compile app/models/conversation.py app/models/__init__.py app/schemas/chat.py app/schemas/__init__.py app/graph/state.py app/graph/nodes.py app/graph/workflow.py app/api/chat.py alembic/versions/c1d2e3f4a5b6_add_pending_clarification.py`，通过；执行 `.venv/bin/pytest tests/test_chat.py -k 'clarification or term_normalize' -q`，13 passed、67 deselected；执行 `.venv/bin/pytest tests/test_chat.py -q`，77 passed、3 skipped；执行 `.venv/bin/pytest -q`，210 passed、4 skipped；执行 `.venv/bin/alembic heads`，返回单一 head `c1d2e3f4a5b6`；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过。
- 残留风险：澄清态采用下一轮读取时惰性过期，不含后台清理任务；当前只处理业务术语冲突，不覆盖蓝图参数缺失等其他澄清类型；前端候选点击依赖当前输入框提交路径，不使用 assistant-ui 私有 programmatic send API。

### 2026-06-10 11:23 · 数据源支持能力增强

- 涉及文件：`datalogue-api/app/services/datasource.py`、`datalogue-api/app/models/datasource.py`、`datalogue-api/app/schemas/datasource.py`、`datalogue-api/app/api/datasource.py`、`datalogue-api/app/services/dataset_context.py`、`datalogue-api/app/utils/sql_guard.py`、`datalogue-api/app/utils/sql_dialect.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/alembic/versions/d2e3f4a5b6c7_add_datasource_capability_fields.py`、`datalogue-api/tests/test_datasource.py`、`datalogue-api/tests/test_sql_guard.py`、`datalogue-api/tests/test_dataset_context.py`、`datalogue-web/src/api/client.js`、`datalogue-web/src/components/datasources.jsx`
- 关键改动：新增 SQLAlchemy 优先的数据源适配器注册表，注册 MySQL、PostgreSQL、SQLite、Oracle、Hive、ClickHouse、SQL Server、Trino、Presto、BigQuery 能力；可选企业驱动采用延迟 import，缺失时返回 `DRIVER_MISSING` 诊断而不阻断应用启动；`Datasource` 新增 `dialect/driver/default_schema/connection_options/connect_timeout_seconds/query_timeout_seconds/last_test_result/last_error_code/last_error_message`，并补能力接口 `/api/datasource/capabilities`；连接测试、Schema 拉取、表字段同步和预览统一返回诊断结构；数据集问数上下文新增 `datasource_context`，DSL 编译、SQL 执行、SQL 审计和蓝图执行统一读取方言和授权表；SQL Guard 增加表白名单校验和多方言别名；前端数据源表单改为后端能力驱动，并按类型展示 Oracle/Hive/Trino/Presto/SQL Server/BigQuery 扩展选项，DDL 同步页展示 skipped/errors。
- 验证方式：执行 `.venv/bin/python -m py_compile app/models/datasource.py app/schemas/datasource.py app/services/datasource.py app/api/datasource.py app/services/dataset_context.py app/utils/sql_guard.py app/utils/sql_dialect.py app/graph/nodes.py app/api/chat.py app/services/analysis_blueprint.py app/utils/sample_data.py tests/test_datasource.py tests/test_sql_guard.py tests/test_dataset_context.py`，通过；执行 `.venv/bin/pytest tests/test_datasource.py tests/test_sql_guard.py tests/test_dataset_context.py tests/test_chat.py -q`，101 passed、3 skipped；执行 `.venv/bin/pytest -q`，214 passed、4 skipped；执行 `.venv/bin/alembic upgrade head`，成功升级到 `d2e3f4a5b6c7`；执行 `.venv/bin/alembic current`，返回 `d2e3f4a5b6c7 (head)`；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过。
- 残留风险：Oracle/Hive/ClickHouse/SQL Server/Trino/Presto/BigQuery 驱动按可选依赖处理，本轮以能力注册和 mock/契约诊断为主，未连接真实企业库验收；SQLGlot 对复杂跨方言函数转换仍是 best-effort，后续需要结合真实库报错补方言规则和函数白名单；前端 lint 仍保留既有 warning，并新增 `datasources.jsx` 的 `useEffect` 依赖 warning，不影响构建。

### 2026-06-10 11:52 · 企业数据源驱动离线交付方案

- 涉及文件：`.gitignore`、`datalogue-api/README.md`、`datalogue-api/requirements-enterprise.txt`、`datalogue-api/pyproject.toml`、`datalogue-api/scripts/download_enterprise_wheels.sh`、`datalogue-api/docs/企业数据源驱动离线部署.md`、`datalogue-api/app/services/datasource.py`、`datalogue-api/app/schemas/datasource.py`、`datalogue-api/tests/test_datasource.py`、`datalogue-web/src/components/datasources.jsx`
- 关键改动：新增企业数据源可选驱动清单，覆盖 Oracle、Hive/Presto、Trino、SQL Server、ClickHouse、BigQuery，并同步登记到 `pyproject.toml` 的 `enterprise` 可选依赖；新增离线 wheelhouse 下载脚本，供有网构建机提前下载企业驱动包，再随制品进入纯内网；新增部署文档，说明 `requirements.txt` 与 `requirements-enterprise.txt` 的职责、内网 `pip install --no-index --find-links` 安装方式、系统 ODBC/Kerberos/服务账号等注意事项；README 增加企业驱动离线安装入口；`.gitignore` 忽略 wheelhouse 目录；数据源能力接口新增 `driver_module/driver_status/install_hint`，前端数据源管理页在概览和创建抽屉展示“内置驱动/驱动已安装/驱动未安装”状态，驱动缺失时保留配置保存但明确提示离线安装步骤。
- 验证方式：执行 `.venv/bin/python -m py_compile app/services/datasource.py app/schemas/datasource.py tests/test_datasource.py`，通过；执行 `.venv/bin/pytest tests/test_datasource.py -q`，9 passed；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：脚本只负责下载 Python wheel，不处理 SQL Server ODBC Driver、Kerberos 客户端、Oracle thick client、BigQuery 服务账号等系统级或密钥类依赖；如果目标平台没有对应 wheel，需要在同架构构建机编译或由企业制品库提供已验证 wheel。

### 2026-06-10 12:08 · 修复企业驱动下载脚本 Python 命令兼容

- 涉及文件：`datalogue-api/scripts/download_enterprise_wheels.sh`、`datalogue-api/docs/企业数据源驱动离线部署.md`、`datalogue-api/README.md`
- 关键改动：修复 `download_enterprise_wheels.sh` 在只有 `python3`、没有 `python` 命令的环境中报错的问题；脚本现在按 `PYTHON` 环境变量、项目 `.venv/bin/python`、`python3`、`python` 的顺序选择解释器，并在输出中显示实际使用的 Python；文档和 README 的内网安装示例改为 `python3 -m pip`，并补充 `PYTHON=/path/to/python` 显式指定方式。
- 验证方式：执行 `bash -n datalogue-api/scripts/download_enterprise_wheels.sh`，通过；执行 `git diff --check`，通过。
- 残留风险：本次只做脚本兼容性和语法检查，没有实际联网下载 wheel；真实下载仍依赖构建机网络、Python 版本和目标平台 wheel 可用性。

### 2026-06-10 12:13 · 修复企业驱动下载脚本 venv 无 pip 场景

- 涉及文件：`datalogue-api/scripts/download_enterprise_wheels.sh`、`datalogue-api/docs/企业数据源驱动离线部署.md`
- 关键改动：进一步修复项目 `.venv/bin/python` 存在但没有 `pip` 模块时脚本误选该解释器的问题；脚本新增 `has_pip` 检查，自动发现模式只选择能执行 `python -m pip` 的解释器，显式 `PYTHON=...` 缺 pip 时给出清晰错误；文档补充离线 wheel 与 Python 版本、操作系统和 CPU 架构相关，建议使用与内网目标环境一致且带 pip 的 Python 下载。
- 验证方式：执行 `bash -n datalogue-api/scripts/download_enterprise_wheels.sh`，通过；执行 `python3 -m pip --version`，确认系统 `python3` 可用；执行 `datalogue-api/.venv/bin/python -m pip --version`，复现 `.venv` 无 pip；执行 `git diff --check`，通过。
- 残留风险：仍未实际联网下载 wheel；如果系统 `python3` 版本与内网目标 Python 版本不一致，下载到的部分平台 wheel 可能不适用于目标环境，需要用目标同版本 Python 重新生成 wheelhouse。

### 2026-06-10 13:20 · LiteLLM 前端配置化接入

- 涉及文件：`datalogue-api/app/models/llm.py`、`datalogue-api/app/schemas/llm.py`、`datalogue-api/app/api/llm.py`、`datalogue-api/app/services/llm_config.py`、`datalogue-api/app/graph/llm.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/services/annotation.py`、`datalogue-api/app/services/blueprint_analyzer.py`、`datalogue-api/alembic/versions/e3f4a5b6c7d8_add_llm_model_config.py`、`datalogue-api/tests/test_llm_config.py`、`datalogue-web/src/components/settings.jsx`、`datalogue-web/src/styles.css`
- 关键改动：新增 LLM 模型配置和角色绑定表，前端系统设置页支持新增、编辑、启停、删除、测试模型，并按 `default/intent/dsl/sql_audit/report/annotation/blueprint` 绑定任务模型；API Key 复用 AES 加密落库，接口只返回 `api_key_set`；`get_llm` 保留 `ChatOpenAI` 作为 OpenAI-compatible 客户端，按数据库角色配置优先、`.env` 兜底创建模型客户端；问数链路、字段标注和蓝图分析调用均标注对应角色。
- 验证方式：执行 `.venv/bin/python -m py_compile app/models/llm.py app/schemas/llm.py app/services/llm_config.py app/api/llm.py app/graph/llm.py app/graph/nodes.py app/graph/workflow.py app/services/annotation.py app/services/blueprint_analyzer.py tests/test_llm_config.py`，通过；执行 `.venv/bin/alembic heads`，返回单一 head `e3f4a5b6c7d8`；执行 `.venv/bin/alembic upgrade head`，成功升级到 `e3f4a5b6c7d8`；执行 `.venv/bin/alembic current`，返回 `e3f4a5b6c7d8 (head)`；执行 `.venv/bin/pytest tests/test_llm_config.py tests/test_chat.py tests/test_sql_audit.py tests/test_analysis_blueprint.py -q`，128 passed、3 skipped；执行 `.venv/bin/pytest -q`，219 passed、4 skipped；执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；通过内置浏览器打开 `http://localhost:5174/settings` 并切换到“LLM 模型”，确认角色绑定、模型表单和空列表渲染，浏览器控制台无 error。
- 残留风险：模型连接测试依赖真实 LiteLLM Proxy 或 OpenAI-compatible 网关可用；本轮不引入 LiteLLM Python SDK，也不实现多租户权限隔离或密钥轮换审计；本地 uvicorn 启动因沙箱阻止连接 `localhost:5432` 未完成真实后端页面联调，但后端 API 已由 TestClient 覆盖。

### 2026-06-10 13:43 · 设计开发方案 docx 补充 LiteLLM 配置化接入

- 涉及文件：`assets/documents/Datalogue_设计开发方案_v2.0_更新版_20260610.docx`、`scripts/update_litellm_docx.py`
- 关键改动：基于 20260605 版 docx 生成 20260610 更新版；封面更新为 v2.0.2；补充 `4.4 LLM 多来源配置` 的当前实现状态、验收结果和剩余边界；同步更新技术选型、双模部署、核心数据库设计、API 设计、P1 路线图、附录差距矩阵和后续任务 `T-014 LLM 故障降级与用量审计`。
- 验证方式：执行更新脚本生成 docx；执行关键内容抽取检查，确认 v2.0.2、`/api/llm/models`、`llm_model_config`、`T-014` 均已写入；执行 `unzip -t assets/documents/Datalogue_设计开发方案_v2.0_更新版_20260610.docx`，无压缩结构错误；执行 python-docx 打开检查，返回 `paragraphs=401 tables=39`。
- 残留风险：按文档处理规范尝试 `render_docx.py` 渲染页面图片，但 bundled LibreOffice 缺少 `liblcms2.2.dylib`，无法完成视觉 PNG QA；本轮完成结构级校验，后续如本机补齐 LibreOffice 依赖可再做页面级视觉验收。

### 2026-06-10 13:50 · LLM 配置表单下拉与自动填充

- 涉及文件：`datalogue-web/src/components/settings.jsx`、`datalogue-web/src/styles.css`
- 关键改动：系统设置的 LLM 模型表单新增接入模板下拉，内置 LiteLLM Proxy、OpenAI、DeepSeek、通义千问、MiniMax、Claude via LiteLLM 和自定义 OpenAI-compatible；供应商和模型名改为下拉选择，模板切换会自动填充供应商、Base URL、描述、默认模型和自动名称；模型选择会同步更新模型名，选择“自定义模型”时展示手动输入框；编辑已有模型时按模型名和 Base URL 反推模板，API Key 继续只支持写入或覆盖，不展示明文。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；通过内置浏览器打开 `http://localhost:5173/settings`，切换到“LLM 模型”，验证模板和模型下拉渲染，选择“通义千问 DashScope”后自动填充 Base URL、供应商、描述和名称，选择“自定义模型”后出现模型名输入框，浏览器控制台无 error。
- 残留风险：模板清单为前端内置常用值，后续如供应商模型命名变化需要同步更新；本轮只验证前端表单交互和构建，不连接真实模型网关做连接测试。

### 2026-06-10 13:55 · LLM 表单增加测试连接按钮

- 涉及文件：`datalogue-web/src/components/settings.jsx`、`datalogue-web/src/styles.css`
- 关键改动：在 LLM 模型编辑表单的保存区域新增“测试连接”按钮，复用已落地的 `/api/llm/models/{id}/test` 后端接口；未保存的新配置点击时给出“请先保存模型配置，再测试 LLM 连通性”提示，避免把未入库的 API Key 明文带入测试请求；列表行原有测试图标标题同步调整为“测试连接”；补充按钮组布局样式，保证“测试连接”和“保存”并排展示且窄宽度可换行。
- 验证方式：执行 `npm run lint`，0 errors、19 warnings；执行 `npm run build`，通过；执行 `git diff --check`，通过；通过内置浏览器打开 `http://localhost:5173/settings`，切换到“LLM 模型”，确认表单操作区显示“测试连接 / 保存”，未保存配置点击“测试连接”后展示先保存提示，浏览器控制台无 error。
- 残留风险：真实连通性测试仍依赖已保存模型配置、后端服务和目标 OpenAI-compatible 网关可用；本轮仅验证前端按钮、提示和构建。

### 2026-06-10 13:18 · Langfuse 可观测能力需求与开发文档

- 涉及文件：`docs/Langfuse可观测能力需求设计文档.md`、`docs/Langfuse可观测能力开发文档.md`
- 关键改动：新增 Langfuse 可观测能力需求设计文档，覆盖 Trace、Prompt Manager、Sessions、Scores、Datasets + Evaluations、Cost Tracking、Annotation Queues、LLM-as-Judge、Releases、Webhooks、Public API 报表和 Playground 的目标、范围、元数据标准、阶段里程碑和验收标准；新增开发文档，结合当前 FastAPI + LangGraph + assistant-ui 链路，明确 `ObservabilityService`、Prompt Registry、Chat Trace/Session、LLM Generation、Feedback API、评测脚本、成本报表、标注队列和 Judge 的接入点、数据模型、接口、测试与任务拆分。
- 验证方式：执行 `git diff --check`，通过；文档内容基于当前代码入口和 Langfuse 官方文档能力核对。
- 残留风险：本轮只生成需求与开发文档，未改动业务代码；具体 SDK 版本、私有化 Langfuse 地址、模型价格表、客户侧报表样式和权限策略需在实施阶段结合部署环境再确认。

### 2026-06-10 13:30 · Langfuse 文档导出 Word

- 涉及文件：`docs/Langfuse可观测能力需求与开发文档.docx`、`scripts/export_langfuse_docs_to_docx.py`、`.codex/项目记忆.md`
- 关键改动：新增 Markdown 转 Word 导出脚本，将 `docs/Langfuse可观测能力需求设计文档.md` 和 `docs/Langfuse可观测能力开发文档.md` 合并导出为一份 Word 文档；Word 文档采用正式工程文档版式，包含封面、文档结构、需求设计文档和开发文档两部分，并保留标题层级、列表、表格和代码块。
- 验证方式：执行导出脚本成功生成 docx；执行 `unzip -t docs/Langfuse可观测能力需求与开发文档.docx`，无压缩结构错误；执行 python-docx 打开检查，返回 `paragraphs=695 tables=11 sections=2`；执行 `git diff --check`，通过。
- 残留风险：按 Documents 技能要求尝试渲染 docx 页面图片，但 bundled LibreOffice 缺少 `/opt/homebrew/opt/little-cms2/lib/liblcms2.2.dylib`，无法完成页面级视觉 QA；本轮已完成结构级校验。

### 2026-06-11 13:15 · 术语功能降级为语义词典

- 涉及文件：`.codex/术语功能舍取-autoplan.md`、`.codex/术语功能舍取-开发计划.md`、`.codex/项目记忆.md`、`datalogue-web/src/components/datasets.jsx`、`datalogue-web/src/components/analysis-blueprints.jsx`
- 关键改动：按 Approach B 落地术语功能舍取，保留后端 `BusinessTerm` 模型、terms API 和运行时术语归一化链路；数据集能力页将“业务术语”从普通一级主路径降级到“高级治理 / 语义词典”，主路径保留数据表、字段标注、指标、维度、分析蓝图和语义验证；语义词典保留原 CRUD、搜索、筛选、候选纳入和冲突检测能力，并更新为空词典不阻塞问数的说明；分析蓝图向导第 2 步新增“建议别名与口径”区域，从蓝图名称、触发词、问法、参数、输出列和口径约束生成候选，支持接受、忽略、重复复用提示，失败不阻塞蓝图保存；语义验证报告将术语命中降级为“语义词典证据”，保留运行时观测价值但降低主操作噪音。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py -k 'term_normalize or clarification or knowledge_term or semantic_asset_resolution' -q`，18 passed、62 deselected；执行 `cd datalogue-api && .venv/bin/pytest tests/test_dataset.py -k 'term' -q`，4 passed、19 deselected；使用本地 headless Chromium 打开 `http://127.0.0.1:5173/datasets`，页面返回 200 且无 console error；点击“语义词典”后确认“语义词典 / 别名与口径”“建议别名”“冲突检测”均可见；切回“分析蓝图”并打开“新建蓝图”向导成功；通过 mock `analyze-description` 响应验证第 2 步“建议别名与口径”候选、接受/忽略按钮可见；通过 mock terms 创建接口点击“接受”后显示“已沉淀到语义词典”。
- 残留风险：蓝图候选别名第一版在前端从 AI 草稿中生成，尚未新增后端候选接口或保存后自动建立蓝图资产关联；高级治理入口已经可达，但仍需真实页面手工验收移动端布局和治理人员发现性；项目仍缺少独立 `DESIGN.md`，已在计划中列为 P3 文档任务。

### 2026-06-11 15:57 · 术语功能 Phase 5 响应式与可访问性收尾

- 涉及文件：`datalogue-web/src/components/datasets.jsx`、`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-web/src/styles.css`、`.codex/项目记忆.md`
- 关键改动：语义词典在移动端改为列表优先，点击词条打开固定详情抽屉，详情抽屉提供关闭按钮并在关闭后把焦点送回原词条行；蓝图向导“建议别名与口径”改为带数量摘要的可折叠面板，桌面默认展开、移动端默认折叠，展开后显示候选和接受/忽略操作；补充移动端触达尺寸约束，候选按钮、词条行和折叠面板头部不小于 44px；冲突和候选状态继续保留文本，不只依赖颜色表达。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；使用本地 headless Chromium 390px 移动端视口打开语义词典，确认 17 个词条可见、点击词条后详情抽屉打开、关闭后抽屉隐藏且焦点回到词条行；使用 mock `analyze-description` 响应在 390px 移动端验证“建议别名与口径”默认折叠、`aria-expanded=false`、展开后候选和接受/忽略按钮可见；桌面 1440px 视口验证候选区域默认展开且 `aria-expanded=true`；各页面测试均无 console error。
- 残留风险：本次完成响应式和可访问性主路径验收，但未引入自动化视觉回归基线；真实 `analyze-description` 仍依赖 LLM/后端耗时，UI 进入第 2 步的完整链路仍建议在后端模型可用时再做一次真接口验收。

### 2026-06-11 16:19 · 数据集能力 Tab 固定像素布局

- 涉及文件：`datalogue-web/src/components/datasets.jsx`、`datalogue-web/src/styles.css`、`.codex/项目记忆.md`
- 关键改动：数据集能力工作区顶部改为固定导航壳，标题、阶段标签和说明不再随当前 Tab 改变；主路径 Tab 与高级治理 Tab 使用稳定 CSS 类替代内联尺寸，高级治理区高度固定；所有主 Tab 内容统一从 `.capability-page-slot` 起始，语义验证面板移回右侧能力区域并去掉额外上边距；数据集页根容器补 `width: 100%` 和 `box-sizing: border-box`，外层主网格改为 `200px minmax(0, 1fr)`，避免不同 Tab 内容反向挤压整体宽度。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过；启动 `npm run dev -- --host 127.0.0.1` 后使用 headless Chromium 打开 `http://127.0.0.1:5173/datasets`，依次切换“数据表 / 语义词典 / 语义验证 / 分析蓝图”，测得能力导航壳高度、x、width、内容起点 y、内容起点 x 变化均为 0，控制台无 error；截图保存在 `/private/tmp/datalogue-fixed-tabs-validation-smoke.png`。
- 残留风险：本次验证覆盖桌面 1440px 视口；移动端已在 Phase 5 覆盖语义词典和蓝图候选，但固定 Tab 的移动端视觉基线仍未建立自动化回归。

### 2026-06-11 17:56 · Langfuse 耗时链路修复与中文节点名

- 涉及文件：`datalogue-api/app/utils/sql_guard.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/prompts/report_generate.py`、`datalogue-api/app/services/observability/tracer.py`、`datalogue-api/tests/test_sql_guard.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_observability.py`、`.codex/项目记忆.md`
- 关键改动：修复 SQL Guard 在表白名单校验时把 CTE 名称误判为未授权物理表的问题，避免 `WITH contract_stats AS (...)` 这类查询被错误拦截；SQL 执行节点在上游编译失败且已有错误时保留原始 Guard 错误和 `should_retry` 决策，不再覆盖成笼统的“SQL 为空”；Langfuse span 名称改为中文展示名，如“DSL 生成 / SQL 执行 / 报告生成”，LLM generation 改为“LLM · 语义层推断 / LLM · SQL 诊断 / LLM · 报告生成”，同时在 metadata 保留 `technical_name` 便于按英文节点排查；报告生成节点压缩输入结果集，最多展开前 30 行、截断超长单元格，并在 prompt 和返回值层面禁止 `<think>` 推理草稿进入最终回答。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/prompts/report_generate.py app/graph/nodes.py app/utils/sql_guard.py app/services/observability/tracer.py tests/test_chat.py tests/test_sql_guard.py tests/test_observability.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py tests/test_observability.py tests/test_sql_guard.py tests/test_sql_audit.py -q`，127 passed、3 skipped；执行 `git diff --check`，通过。
- 残留风险：测试环境中 Langfuse SDK 后台 exporter 仍会尝试访问本机 `localhost:3000`，在沙箱内出现 `Operation not permitted` 警告但不影响测试结果；本轮未重放真实前端问数页面，需重启后端后用“供应商综合评估与分级排名”再验证新 trace 是否减少重试并显示中文节点。

### 2026-06-11 18:22 · Langfuse Token 与性能指标补齐

- 涉及文件：`datalogue-api/app/utils/token.py`、`datalogue-api/app/services/observability/tracer.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/tests/test_observability.py`、`datalogue-api/tests/test_chat.py`、`.codex/项目记忆.md`
- 关键改动：修复 Langfuse generation 的 usage 字段映射，把内部 `prompt_tokens/completion_tokens/total_tokens` 转为 Langfuse UI 识别的 `input/output/total`，避免 `inputUsage/outputUsage` 全为 0；当 OpenAI-compatible 模型未返回 `usage_metadata` 时，按输入消息和输出文本做本地估算，并在 metadata 标记 `usage_source=estimated`，模型返回真实 usage 时标记 `usage_source=provider`；LLM generation 改为调用前开始、调用后结束，记录真实 generation 耗时；结构化 LLM 调用优先走 `stream()` 聚合完整内容，从首个内容 chunk 记录 `completion_start_time/ttft_ms/tps`，不支持 streaming 的模型对象再回退 `invoke()`；报告生成继续使用 `astream()` 记录真实 TTFT；意图识别 LLM 也补充 generation 子节点，避免 trace 中缺少该 LLM 调用。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/utils/token.py app/services/observability/tracer.py app/graph/nodes.py tests/test_observability.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_observability.py::test_token_usage_estimates_when_provider_usage_missing tests/test_observability.py::test_langfuse_observation_names_are_chinese tests/test_chat.py::TestLangGraphNodes::test_report_generator_with_result tests/test_chat.py::TestLangGraphNodes::test_report_generator_compacts_rows_and_strips_think -q`，4 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py tests/test_observability.py tests/test_sql_guard.py tests/test_sql_audit.py -q`，128 passed、3 skipped。
- 残留风险：真实 token usage 仍取决于模型服务商是否返回 `usage_metadata`；未返回时使用本地估算值，只能用于趋势分析，不能作为精确计费依据。若某个模型对象不支持 `stream()` 或未启用 streaming，会自动回退 `invoke()`，该类调用的 TTFT 仍会标记为不可用。

### 2026-06-11 18:50 · LLM Think 模式配置化关闭

- 涉及文件：`datalogue-api/app/models/llm.py`、`datalogue-api/app/schemas/llm.py`、`datalogue-api/app/services/llm_config.py`、`datalogue-api/app/graph/llm.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/api/llm.py`、`datalogue-api/alembic/versions/g2h3i4j5k6l7_add_llm_thinking_enabled.py`、`datalogue-api/tests/test_llm_config.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/components/settings.jsx`、`.codex/项目记忆.md`
- 关键改动：LLM 模型配置新增 `thinking_enabled` 字段并默认关闭，前端“LLM 模型”设置页增加 Think 模式开关和列表状态展示；`get_llm` 根据模型配置为 Qwen/DashScope 下发 `extra_body.enable_thinking=false`，为 Claude/Anthropic 下发 `extra_body.thinking={type: disabled}`，未知供应商不强塞扩展参数；LLM 实例携带 Think 开关状态，通用同步流式调用和报告生成在 Think 关闭时兜底清理 `<think>...</think>`，同时把 `thinking_enabled` 写入 Langfuse generation metadata；模型测试接口也复用同一扩展参数生成逻辑。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/models/llm.py app/schemas/llm.py app/services/llm_config.py app/graph/llm.py app/api/llm.py app/graph/nodes.py alembic/versions/g2h3i4j5k6l7_add_llm_thinking_enabled.py tests/test_llm_config.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_llm_config.py tests/test_chat.py -q`，86 passed、3 skipped；执行 `cd datalogue-api && .venv/bin/pytest tests/test_llm_config.py tests/test_chat.py tests/test_observability.py tests/test_sql_guard.py tests/test_sql_audit.py -q`，135 passed、3 skipped；执行 `cd datalogue-api && .venv/bin/alembic heads`，返回单一 head `g2h3i4j5k6l7`；在沙箱外执行 `cd datalogue-api && .venv/bin/alembic upgrade head`，成功升级 `f1a2b3c4d5e6 -> g2h3i4j5k6l7`；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过；通过 Playwright 打开 `http://127.0.0.1:5173/settings` 并切换“LLM 模型”，确认 `/api/llm/models` 不再 500、控制台无 error、页面可定位到“Think 模式”。
- 残留风险：不同 OpenAI-compatible 网关对关闭思考参数的兼容性不完全一致，本轮只对 Qwen/DashScope 和 Claude/Anthropic 下发已知扩展参数；即使网关忽略参数，后端仍会在 Think 关闭时清理 `<think>` 标签，但无法阻止模型内部生成过程本身消耗时间和 token。

### 2026-06-11 19:18 · 分析蓝图编辑入口

- 涉及文件：`datalogue-web/src/components/analysis-blueprints.jsx`、`.codex/项目记忆.md`
- 关键改动：分析蓝图详情页新增“编辑”按钮；复用现有蓝图向导增加编辑模式，传入现有蓝图后直接进入编辑基础信息步骤，回填名称、描述、触发关键词、触发问法、参数、输出列、步骤、解读提示和原始 SQL；编辑模式保存时调用已有 `updateAnalysisBlueprint` 的 PUT 接口，保存成功后关闭编辑器并更新当前选中蓝图；编辑后点击发布会先保存当前表单改动，再调用发布接口，避免发布旧配置；新建蓝图入口会清空编辑态，防止复用上一次蓝图数据。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；启动 `npm run dev -- --host 127.0.0.1` 后使用 Playwright 打开 `http://127.0.0.1:5173/datasets`，确认页面无 console error，已有蓝图详情页可定位到“编辑”按钮，点击后弹出“编辑分析蓝图”向导且控制台无 error。
- 残留风险：本轮未实际提交保存修改，避免污染本地已有蓝图数据；编辑表单复用创建向导的 JSON 文本编辑方式，参数、输出列和步骤仍需要用户按 JSON 结构维护，后续可再做字段级可视化编辑器。

### 2026-06-11 19:42 · shadcn/ui 基础设施接入

- 涉及文件：`datalogue-web/package.json`、`datalogue-web/package-lock.json`、`datalogue-web/vite.config.js`、`datalogue-web/components.json`、`datalogue-web/jsconfig.json`、`datalogue-web/src/lib/utils.js`、`datalogue-web/src/components/ui/*.jsx`、`datalogue-web/src/styles.css`、`.codex/项目记忆.md`
- 关键改动：为 Vite 前端接入 Tailwind CSS v4 和 `@tailwindcss/vite`，增加 `@` alias 与 shadcn `components.json`；新增 `cn()` 工具和首批 JSX 版 shadcn/ui 组件基座，包括 Button、Badge、Card、Input、Textarea、Select、Tabs、Dialog、Sheet、DropdownMenu、Popover、Tooltip、Checkbox、Switch、Label、Alert、Skeleton、Separator、ScrollArea 和 Table；在 `styles.css` 中把 Datalogue 现有 CSS 变量映射为 shadcn 语义 token，保留当前产品视觉，不迁移 AI 对话相关文件。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、33 warnings；执行 `cd datalogue-web && npm run build`，通过。
- 残留风险：并行迁移 worker 已开始在部分页面加入 shadcn 导入，当前 lint warnings 中包含后续阶段会清理的未使用导入；本阶段只完成组件底座，尚未完成所有业务页面替换。

### 2026-06-11 19:50 · shadcn/ui 视觉 Token 映射

- 涉及文件：`datalogue-web/src/styles.css`、`datalogue-web/src/components/ui/*.jsx`、`.codex/项目记忆.md`
- 关键改动：将 Datalogue 现有 `--bg`、`--surface`、`--text`、`--accent`、`--hairline`、`--radius` 等变量映射为 shadcn 的 `--background`、`--foreground`、`--card`、`--primary`、`--border`、`--input`、`--ring` 等语义 token；组件默认样式使用这些 token，避免引入 shadcn 默认色板造成产品视觉断层。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过。
- 残留风险：本轮完成 token 层映射和构建验证，尚未做逐页面截图级视觉回归；新旧 class 共存期间仍需关注按钮和卡片默认样式叠加。

### 2026-06-11 19:52 · 低风险页面迁移到 shadcn/ui

- 涉及文件：`datalogue-web/src/components/workspace.jsx`、`datalogue-web/src/components/dashboard.jsx`、`datalogue-web/src/components/history.jsx`、`datalogue-web/src/components/pinned.jsx`、`datalogue-web/src/components/knowledge.jsx`、`datalogue-web/src/components/sidebar.jsx`、`.codex/项目记忆.md`
- 关键改动：工作台、看板、历史、收藏、知识库和侧边栏中的普通按钮、状态标签、展示卡片、搜索框和 Tab 控件迁移为本地 shadcn/ui 组件；保留原业务逻辑、事件处理和主要 `className`，降低样式回归风险。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过。
- 残留风险：本阶段以构建和静态迁移为主，尚未逐页完成浏览器视觉验收；后续总体验收需重点检查新 Button/Card/Badge/Tabs 与旧 CSS 叠加后的间距和高度。

### 2026-06-11 19:58 · 表单弹窗抽屉组件迁移到 shadcn/ui

- 涉及文件：`datalogue-web/src/components/editor-modal.jsx`、`datalogue-web/src/components/publish-drawer.jsx`、`datalogue-web/src/components/settings.jsx`、`datalogue-web/src/components/datasources.jsx`、`datalogue-web/src/components/notifications.jsx`、`.codex/项目记忆.md`
- 关键改动：全局编辑弹窗迁移为 `Dialog`，发布接口抽屉和数据源编辑抽屉迁移为 `Sheet`；表单输入、文本域、选择器、开关、复选框、告警、加载占位和普通操作按钮迁移为本地 shadcn/ui 组件；设置页继续保留既有 LLM Think 模式配置逻辑，数据源页保留创建、编辑、测试连接、Schema 和 DDL 同步业务流程。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过。
- 残留风险：本阶段完成组件级迁移和构建验证，尚未逐项手工点击所有表单保存/抽屉关闭流程；Radix Dialog/Sheet 自带焦点管理，后续页面验收需重点确认与旧自定义遮罩样式叠加后的关闭按钮和滚动体验。

### 2026-06-11 21:55 · 数据集治理与分析蓝图迁移到 shadcn/ui

- 涉及文件：`datalogue-web/src/components/datasets.jsx`、`datalogue-web/src/components/analysis-blueprints.jsx`、`.codex/项目记忆.md`
- 关键改动：数据集治理工作台的能力 Tab、统计卡片、筛选输入、选择器、语义词典详情抽屉、数据预览表格和查询约束表单迁移到本地 shadcn/ui 组件；分析蓝图工作台的状态标签、创建/编辑向导 Dialog、详情 Tab、结构化卡片、测试结果表格、加载态、空态和告警迁移到本地 shadcn/ui 组件；保留既有业务逻辑、固定 Tab 骨架、分析蓝图编辑入口和 AI 对话范围边界。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；启动本地 Vite 后用 headless Chromium 打开 `/datasets`、`/settings`、`/chat`，三条路由控制台均无 error，`/chat` 可正常渲染 AI 对话页面；数据集页截图保存在 `/private/tmp/datalogue-shadcn-datasets.png`。
- 残留风险：本轮完成复杂页渲染冒烟和构建验证，未逐项提交真实数据保存、蓝图发布和数据源同步操作；视觉回归以桌面冒烟截图为主，后续如继续压低风险可补移动端截图基线。

### 2026-06-11 22:08 · 非 AI 旧 CSS 清理与 shadcn 迁移收口

- 涉及文件：`datalogue-web/src/components/datasets.jsx`、`datalogue-web/src/styles.css`、`.codex/项目记忆.md`
- 关键改动：统一数据集治理页的 shadcn/ui 导入路径为 `@/components/ui/*`，与其他迁移页面保持一致；清理已经由 Radix Dialog/Sheet 接管、且源码中不再引用的非 AI 遮罩样式 `.blueprint-wizard-backdrop` 和 `.pd-backdrop`，保留仍被发布抽屉和蓝图向导使用的内容容器样式；复核本轮迁移未触碰 `src/assistant/**`、`chat-page.jsx`、`dataset-assistant-panel.jsx`、`agent-panel.jsx`、`message-content.jsx` 等 AI 对话范围文件。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：旧 CSS 仍有大量业务类名服务于现有布局，本轮只删除确定无引用的非 AI 遮罩样式，未做大规模 CSS 资产瘦身，避免影响仍依赖旧 className 的页面。

### 2026-06-11 22:12 · shadcn 迁移后 UI 视觉收口

- 涉及文件：`datalogue-web/src/components/datasets.jsx`、`datalogue-web/src/styles.css`、`.codex/项目记忆.md`
- 关键改动：统一迁移后按钮、图标按钮、数据集能力工作台、能力 Tab、统计卡、数据预览卡、分析蓝图向导和发布抽屉的边框、阴影、背景与交互态，让 shadcn 组件与 Datalogue 原有企业工具视觉更一致；将数据集页顶部操作栏和主体布局从内联样式改为可响应 class；窄屏下全局侧栏压缩为 64px 图标栏，数据集页标题与操作按钮分行，数据集列表和能力工作区上下排列，避免 390px 移动视口下标题竖排和工作区被挤压。
- 验证方式：启动 `cd datalogue-web && npm run dev -- --host 127.0.0.1` 后，用 Playwright 检查 `/datasets` 桌面 1440px、移动 390px 和 `/settings` 桌面页面，控制台均无 error；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：本轮是 CSS 与布局层视觉收口，未逐项提交真实数据保存、数据源同步、蓝图发布等业务写入操作；移动端侧栏采用图标压缩方案，后续如要做到完整移动导航，可再补抽屉式菜单。

### 2026-06-11 22:28 · 修复 shadcn/ui token bridge 变量映射

- 涉及文件：`datalogue-web/src/styles.css`、`.codex/项目记忆.md`
- 关键改动：删除 `:root` 中 `--radius-sm` 和 `--radius-lg` 的自引用映射，仅保留 `--radius-md: var(--radius)`；将 `@theme inline` 的 `--color-accent` 从品牌蓝 `--accent` 改为轻量悬停背景 `--surface-2`，并将 `--color-accent-foreground` 绑定到 `--foreground`；补齐并按 shadcn/ui 预期保留 `background/card/popover/primary/secondary/muted/destructive/border/input/ring/radius` 映射，避免 Tailwind v4 主题 token 失效。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check -- datalogue-web/src/styles.css`，通过。
- 残留风险：本轮只修复 token bridge，不修改 `.btn`、`.sidebar` 等 Datalogue 既有业务 CSS 类，也不修改 `src/components/ui/` 下的 shadcn/ui 组件文件。

### 2026-06-11 22:34 · 修复工作台卡片被 shadcn Button 压扁

- 涉及文件：`datalogue-web/src/components/workspace.jsx`、`.codex/项目记忆.md`
- 关键改动：工作台页的工具 chip、推荐问题卡片、KPI 卡片和最近会话卡片从 shadcn `Button/Card/Badge` 恢复为原生 `button/div/span` 结构；原因是这些组件是高度定制的卡片式交互面，套用 shadcn Button 后会引入固定 `h-9` 和 `whitespace-nowrap`，导致推荐问题和最近会话被压扁。保留原有 `.tool-chip`、`.suggested .item`、`.kpi`、`.recent-card` CSS 接管视觉。
- 验证方式：启动本地 Vite 后用 Playwright 打开 `/` 工作台页，桌面 1440px 下推荐卡片、KPI 卡片和最近会话恢复正常，控制台无 error；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check -- datalogue-web/src/components/workspace.jsx datalogue-web/src/styles.css`，通过。
- 残留风险：工作台这些定制卡片暂不使用 shadcn 组件基座；后续如要统一为 shadcn，需要先封装适配 Datalogue 卡片式按钮的本地变体，不能直接替换为默认 Button。

### 2026-06-11 22:40 · 回滚前端 shadcn/ui 迁移代码

- 涉及文件：`datalogue-web/package.json`、`datalogue-web/package-lock.json`、`datalogue-web/vite.config.js`、`datalogue-web/src/styles.css`、`datalogue-web/src/components/*.jsx`、`datalogue-web/components.json`、`datalogue-web/jsconfig.json`、`datalogue-web/src/components/ui/`、`datalogue-web/src/lib/`、`.codex/项目记忆.md`
- 关键改动：按用户要求恢复代码，撤回本轮 shadcn/ui 迁移对前端依赖、Vite/Tailwind 配置、全局样式 token bridge、业务页面组件替换和新增 UI 组件目录的改动；删除新增的 `components.json`、`jsconfig.json`、`src/components/ui/` 和 `src/lib/`；保留后端及其他非 shadcn 任务改动不处理。
- 验证方式：执行 `cd datalogue-web && npm run lint`、`cd datalogue-web && npm run build` 和 `git diff --check`，确认前端恢复后仍可通过基础验证。
- 残留风险：本次是代码层回滚，飞书中任务 2-7 已同步重新打开；任务 1“归档迁移总计划”仍保持完成，因为计划归档事实没有撤销。

### 2026-06-12 12:00 · Dataset SubAgent Manifest 治理契约首期

- 涉及文件：`datalogue-api/app/models/dataset.py`、`datalogue-api/app/services/dataset_manifest.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/app/schemas/dataset.py`、`datalogue-api/alembic/versions/i4j5k6l7m8n9_add_dataset_subagent_manifest.py`、`datalogue-api/tests/test_dataset.py`、`datalogue-web/src/api/client.js`、`datalogue-web/src/components/datasets.jsx`
- 关键改动：新增 `dataset_subagent_manifest` 版本表和 `DatasetSubAgentManifest` 模型，manifest 内容拆分为 `auto_fields/manual_fields/quality`；新增 Manifest 服务，支持 schema hash 派生、A 类字段自动生成、B 类字段受控保存、description lint、草稿保存、current 发布、历史版本保留、current stale 标记和 route-check 自检；数据集 API 新增 manifest 详情、保存、发布、自检和 current 摘要接口；指标、维度、字段转指标/维度、选表、取消选表、YAML 导入和数据集结构更新后会标记 current manifest `needs_review`；数据集治理页新增 “SubAgent Manifest” 高级治理工作区，支持自动字段展示、人工字段维护、质量提示、发布和路由自检结果保存为语义验证用例。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/models/dataset.py app/models/__init__.py app/schemas/dataset.py app/schemas/__init__.py app/services/dataset_manifest.py app/api/dataset.py alembic/versions/i4j5k6l7m8n9_add_dataset_subagent_manifest.py tests/test_dataset.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_dataset.py -q`，27 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_dataset.py tests/test_dataset_context.py tests/test_chat.py -q`，109 passed、3 skipped；执行 `cd datalogue-api && .venv/bin/alembic heads`，返回单一 head `i4j5k6l7m8n9`；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：首期未启用全局 LeadAgent 自动路由，聊天仍按前端传入 `dataset_id` 执行；真实权限系统尚未落地，`permission_scope` 仍按数据源和已选表范围派生；C 类历史日志挖掘、待采纳队列和事件驱动缓存失效保留为后续阶段。

### 2026-06-12 12:39 · LeadAgent Manifest 自动路由接入

- 涉及文件：`datalogue-api/app/services/dataset_router.py`、`datalogue-api/app/services/dataset_manifest.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/api/client.js`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/components/chat-page.jsx`、`.codex/项目记忆.md`
- 关键改动：新增 `dataset_router.py`，在未传 `dataset_id` 时读取全部 current Manifest 并按 description、业务域、正例、负例、指标和维度计算路由分；得分达到阈值且领先第二名时自动选择数据集，候选接近时返回 `ambiguous`，无足够证据时返回 `no_match`；已传 `dataset_id` 时强制 `locked`，不自动改选，即使其他 Manifest 更匹配也沿用用户显式选择。`/api/chat/stream` 在进入 LangGraph 前发出 `route_decision` SSE 事件，自动命中后把 `dataset_id/manifest_version/bound_schema_version` 注入工作流状态、trace 元数据和 final payload；路由不明确时保存助手消息并直接返回可解释 final，避免回退到无 schema 猜测路径。前端 SSE 客户端和 assistant-ui adapter 支持 `route_decision`，聊天页自动命中后同步顶部选中数据集，并在 trace 中展示 Manifest 路由步骤。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/dataset_manifest.py app/services/dataset_router.py app/graph/state.py app/api/chat.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestChatStreamEvents -q`，8 passed、1 skipped；执行 `cd datalogue-api && .venv/bin/pytest tests/test_dataset.py -q`，27 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_dataset.py tests/test_dataset_context.py tests/test_chat.py -q`，112 passed、3 skipped；执行 `cd datalogue-api && .venv/bin/alembic heads`，返回单一 head `i4j5k6l7m8n9`；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过。
- 残留风险：本阶段仍使用确定性 scorer，未接入 LLM 二次仲裁；`ambiguous/no_match` 只返回提示和候选，不包含多轮澄清状态机；会话级 Manifest 版本锁定和 SubAgent 执行前 schema stale 中断仍是后续任务；尚未启动浏览器对真实页面做自动路由端到端点击验收。

### 2026-06-12 12:56 · 修复术语澄清候选点击与展示名

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/MyMessage.jsx`、`.codex/项目记忆.md`
- 关键改动：后端 `_ensure_pending_term_clarification` 对已有候选和冲突候选统一归一化，兼容 `display_name/displayName/label/title/name/termName` 等字段，并在候选只有 `term_id` 时回查 `BusinessTerm` 补齐 `display_name/name/definition/term_type/aliases`，避免前端降级显示“术语 1”；前端术语澄清卡新增候选字段兼容解析，展示名、副标题和定义从多种字段或嵌套 `term` 对象读取；候选点击后写入结构化 `clarification_response`，通过 `datalogue:composer-submit` 事件交给 ChatPage 内的 `ComposerTextSetter` 设置文本并发送，避免消息组件直接调用 composer 导致 `Composer is not available`。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/api/chat.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestLangGraphNodes::test_chat_stream_conflict_creates_pending_clarification tests/test_chat.py::TestLangGraphNodes::test_chat_stream_conflict_enriches_existing_candidate_labels tests/test_chat.py::TestLangGraphNodes::test_clarification_resolution_selected_term_id -q`，3 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestLangGraphNodes -q`，51 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py -q`，84 passed、3 skipped；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：本轮通过单测和构建验证修复，未在真实浏览器里构造完整术语冲突会话进行端到端点击；若后续出现非 `BusinessTerm` 来源的候选且 payload 没有任何名称字段，前端仍只能降级展示 `术语 {term_id}` 或候选序号。

### 2026-06-12 13:08 · 修复蓝图 SQL 预览未替换参数

- 涉及文件：`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/api/dataset.py`、`datalogue-api/tests/test_analysis_blueprint.py`、`datalogue-api/tests/test_chat.py`、`.codex/项目记忆.md`
- 关键改动：确认蓝图执行原本使用 `conn.execute(text(sql), params)` 绑定参数执行，问题发生在展示层仍返回 `:param` 模板 SQL；新增 `render_blueprint_sql_preview()`，把已提取参数渲染成仅用于展示/审计的 SQL 字面量，缺失参数继续保留占位符；蓝图执行结果和 `sql_result` 同时返回 `sql_template` 与 `sql_preview`，聊天节点和蓝图测试接口优先把 `sql_preview` 返回给前端展示，route payload 保留规范化后的 `sql_template`，实际执行仍使用绑定参数，避免 SQL 注入风险。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/analysis_blueprint.py app/graph/nodes.py app/api/dataset.py tests/test_analysis_blueprint.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_analysis_blueprint.py::test_render_blueprint_sql_preview_replaces_bound_params tests/test_analysis_blueprint.py::test_blueprint_test_remains_optional_validation tests/test_chat.py::TestLangGraphNodes::test_analysis_blueprint_execute_success -q`，3 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_analysis_blueprint.py -q`，20 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py -q`，84 passed、3 skipped；执行 `git diff --check`，通过。
- 残留风险：SQL 预览渲染是轻量替换器，不解析 SQL AST；如果占位符出现在字符串字面量或注释里也可能被替换，但它只用于展示，不参与实际执行。

### 2026-06-12 13:56 · LeadAgent 控制面 Tools 接入

- 涉及文件：`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/api/client.js`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/components/chat-page.jsx`、`.codex/项目记忆.md`
- 关键改动：新增 LeadAgent 控制面工具编排服务，包含 `TimeTool`、`ThreadContextTool`、`ManifestRouterTool`、`SchemaStatusTool`、`ClarificationTool`、`SubAgentDispatchTool` 和 `AuditTraceTool`；聊天入口改为先构造 `lead_agent_context`，再决定是否进入 LangGraph，保留原 `route_decision` SSE，并新增 `lead_agent_tools` SSE 摘要；`AgentState`、trace metadata、消息元数据和 final payload 均透传时间上下文、会话锁定、Manifest 路由、schema stale 状态和调度审计；schema hash 不一致时自动标记 current manifest `needs_review`。前端 SSE、assistant-ui reasoning 和 trace 面板支持展示 LeadAgent 工具摘要。LeadAgent 仍只处理控制面，不读取指标、维度、术语和蓝图细节。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/lead_agent.py app/api/chat.py app/graph/state.py tests/test_lead_agent_tools.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_lead_agent_tools.py tests/test_chat.py::TestChatStreamEvents::test_chat_stream_auto_routes_by_current_manifest tests/test_chat.py::TestChatStreamEvents::test_chat_stream_keeps_explicit_dataset_locked tests/test_chat.py::TestChatStreamEvents::test_chat_stream_no_manifest_blocks_auto_route -q`，7 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py tests/test_lead_agent_tools.py -q`，88 passed、3 skipped；执行 `cd datalogue-api && .venv/bin/pytest tests/test_dataset.py tests/test_lead_agent_tools.py -q`，31 passed；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：TimeTool 首期只解析常见中文相对时间和显式年月/日期，不做复杂自然语言时间推理；会话级 manifest 版本锁定尚未持久化到 `conversation`；SubAgentDispatchTool 当前是上下文交接，不是独立 SubAgent 进程或工具调用框架。

### 2026-06-12 14:11 · LeadAgent ToolPolicy + Skills 自主决策改造

- 涉及文件：`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/prompts/lead_agent.py`、`datalogue-api/app/services/llm_config.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_llm_config.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/components/chat-page.jsx`、`.codex/项目记忆.md`
- 关键改动：将 LeadAgent 从固定顺序工具编排升级为 `ToolPolicy -> Skills -> LLM Planner -> ToolExecutor -> PolicyValidator` 流程；新增内置 Lead Skills 和 allowed/blocked tool contract，明确禁止 LeadAgent 访问指标、维度、术语、蓝图、SQL 和字段级 schema；新增 `lead_agent_planner` 本地 fallback prompt，并通过现有 Prompt Manager 和 `get_llm(role="lead_agent")` 生成结构化 tool plan；Planner 输出非法工具时只记录 `policy_violations` 不执行，输出不完整、LLM 未配置或调用失败时启用安全降级计划；`lead_agent_tools` SSE 增加 tool policy、selected skills、planned/executed tool calls、policy violations 和 fallback 信息，前端 reasoning 与 trace 面板展示摘要。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/lead_agent.py app/services/llm_config.py app/prompts/lead_agent.py app/api/chat.py tests/test_lead_agent_tools.py tests/test_chat.py tests/test_llm_config.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_lead_agent_tools.py tests/test_chat.py::TestChatStreamEvents -q`，15 passed、1 skipped；执行 `cd datalogue-api && .venv/bin/pytest tests/test_llm_config.py -q`，6 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py tests/test_dataset.py -q`，111 passed、3 skipped；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：当前 LLM Planner 在未配置 `lead_agent` API key 时会安全降级，因此本地默认不会真实调用 Planner；Skills 首期为代码内置，暂未做数据库维护和前端配置；PolicyValidator 只覆盖控制面关键不变量，SubAgent 独立运行时和会话级 manifest 持久锁定仍是后续阶段。

### 2026-06-12 14:25 · LeadAgent Planner Langfuse 监测接入

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/services/observability/tracer.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`datalogue-api/tests/test_chat.py`、`.codex/项目记忆.md`
- 关键改动：将 `/api/chat/stream` 的 Langfuse 根 trace 创建提前到 LeadAgent 执行之前，并在 no_match/ambiguous 等 Manifest 路由阻断路径也更新 trace output、写入 `ObservabilityTraceIndex` 并关闭 trace；`build_lead_agent_context()` 支持接收 tracer 和 trace_context，LeadAgent Planner LLM 调用通过 `start_generation/end_generation` 记录 `LLM · LeadAgent Planner` generation，包含 planner input、prompt name/version/source、tool policy、skills、模型输出、usage、parse_ok、planner_fallback 和异常降级原因；Prompt Manager 版本记录现在能挂入同一条问数 trace。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/lead_agent.py app/api/chat.py app/services/observability/tracer.py tests/test_lead_agent_tools.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_lead_agent_tools.py tests/test_chat.py::TestChatStreamEvents -q`，16 passed、1 skipped；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py tests/test_lead_agent_tools.py -q`，92 passed、3 skipped；执行 `cd datalogue-api && .venv/bin/pytest tests/test_dataset.py tests/test_llm_config.py -q`，33 passed；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过。
- 残留风险：本地未启用 Langfuse 时 generation 记录为 no-op，但本地 `ObservabilityTraceIndex` 仍会记录阻断路径；Planner 未配置 `lead_agent` API key 时不会真实调用 LLM，也不会产生 generation，只会在 LeadAgent context 中标记安全降级。

### 2026-06-12 14:38 · 修复 LeadAgent 时间解析未传递到执行链

- 涉及文件：`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/services/analysis_blueprint.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/components/chat-page.jsx`、`.codex/项目记忆.md`
- 关键改动：LeadAgent `time` 工具解析出相对时间后生成 `resolved_question`，例如“去年”替换为“2025年”，并在 `/api/chat/stream` 初始状态中保留 `original_question`、使用 `resolved_question` 进入 LangGraph，避免下游蓝图仍消费原始相对时间问题；SubAgent dispatch payload 增加 `original_question/resolved_question`，Trace metadata、final payload 和前端 trace 面板同步展示；蓝图参数提取新增 `time_context` 兜底，能把 LeadAgent 解析出的起止日期注入 `start_date/end_date` 参数；LeadAgent 审计区分 LLM planner 明确计划的工具和执行器按策略补齐的 `system_inferred_tool_calls`，减少 Trace 中“Planner 没计划却执行了 dispatch”的误判。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/lead_agent.py app/services/analysis_blueprint.py app/graph/nodes.py app/api/chat.py app/graph/state.py tests/test_lead_agent_tools.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_lead_agent_tools.py tests/test_chat.py::TestLangGraphNodes::test_analysis_blueprint_execute_success tests/test_chat.py::TestChatStreamEvents::test_chat_stream_auto_routes_by_current_manifest -q`，11 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py tests/test_lead_agent_tools.py tests/test_analysis_blueprint.py -q`，113 passed、3 skipped；执行 `cd datalogue-api && .venv/bin/pytest tests/test_dataset.py tests/test_llm_config.py -q`，33 passed；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：`resolved_question` 当前只覆盖常见中文相对时间表达和显式年月日，复杂表达仍依赖后续 TimeTool 能力扩展；蓝图参数注入按参数名启发式识别起止日期，若蓝图参数命名完全脱离 `start/end/from/to/begin` 等约定，仍需要人工规范蓝图参数。

### 2026-06-12 14:51 · LeadAgent 渐进式披露与 Trace 完整输出

- 涉及文件：`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/prompts/lead_agent.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/observability/tracer.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/components/chat-page.jsx`、`.codex/项目记忆.md`
- 关键改动：将 LeadAgent Planner 从一次性暴露全部 Skills 和工具 schema 改为两阶段渐进式披露：第一阶段 `lead_agent_skill_selector` 只接收用户问题、会话摘要、ToolPolicy 和 Skill 摘要并选择 Skill；第二阶段 `lead_agent_tool_planner` 只接收已选 Skill 对应且通过 ToolPolicy 允许的工具 schema，再生成 tool plan；SSE 和前端 reasoning 增加 `progressive_disclosure/disclosed_tools/skill_selection_reasoning_summary/tool_planning_reasoning_summary`；Langfuse 中新增 `LeadAgent 控制面` span，输出完整 `lead_agent_context`，包含 original/resolved question、planned/executed/system inferred tool calls、policy violations、route/schema/dispatch/audit 等最终控制面结果，避免只能看到 LLM 原始 planner JSON。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/lead_agent.py app/prompts/lead_agent.py app/api/chat.py app/services/observability/tracer.py tests/test_lead_agent_tools.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_lead_agent_tools.py -q`，10 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestChatStreamEvents -q`，8 passed、1 skipped；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：渐进式披露只约束 Planner 输入侧，ToolExecutor 仍会按系统不变量补齐必要工具并记录 `system_inferred_tool_calls`；真实 Langfuse 页面展示完整 span 仍依赖 `LANGFUSE_ENABLED=true` 和有效 Langfuse key，未启用时本地流程不受影响。

### 2026-06-12 15:08 · LeadAgent 接管自动路由报告生成

- 涉及文件：`datalogue-api/app/services/report_generation.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/observability/tracer.py`、`datalogue-api/tests/test_chat.py`、`.codex/项目记忆.md`
- 关键改动：新增共享报告生成服务，统一复用 `report_generate` prompt、结果压缩、token 统计和 Langfuse generation 记录；SubAgent `report_generator_node` 改为调用共享服务，显式选择数据集或会话锁定数据集时行为保持不变；自动 Manifest 路由命中 `selected` 时在初始状态写入 `skip_subagent_report=True/report_owner=lead_agent`，工作流在 SQL 模板蓝图或 SQL 执行成功后跳过 SubAgent 报告节点，回到 `/api/chat/stream` 后由 LeadAgent 触发 `llm.lead_agent_report_generator` 生成最终回答并流式输出 token；final、message metadata 和 trace metadata 透出 `report_owner/subagent_report_skipped/lead_agent_report`。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/report_generation.py app/graph/workflow.py app/graph/nodes.py app/graph/state.py app/api/chat.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestChatStreamEvents tests/test_lead_agent_tools.py -q`，18 passed、1 skipped；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py tests/test_analysis_blueprint.py -q`，104 passed、3 skipped；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：LeadAgent 报告本阶段只接管单个自动路由 SubAgent 的单次 SQL/蓝图结果，多 Plan 并行执行后的综合报告仍待后续扩展；LeadAgent 报告复用现有 `report_generate` prompt，暂未拆独立 LeadAgent 汇总报告模板。

### 2026-06-12 15:46 · Langfuse Lead/Sub Trace 层级区分

- 涉及文件：`datalogue-api/app/services/observability/context.py`、`datalogue-api/app/services/observability/tracer.py`、`datalogue-api/app/services/runner.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_observability.py`、`.codex/项目记忆.md`
- 关键改动：`ObservabilityRequestContext` 新增 `parent_observation_id` 预留跨进程 SubAgent 追踪；Langfuse 根 trace 增加 `lead` tag，`DatalogueTracer.start_span()` 支持 `trace_tags` 并合并 tenant/env/release/lead 基础标签，避免子 span 更新时覆盖根标签；新增 `DatasetSubAgentRequest` 和 `InProcessDatasetSubAgentRunner`，以进程内协议包裹 LangGraph 执行并创建 `subagent.{dataset_id}` span，追加 `sub` 与 `dataset:{id}` tag；`/api/chat/stream` 将 LeadAgent 路由包入 `lead.routing` span，将 SubAgent 图执行改由 Runner 调用，将 LeadAgent 报告综合包入 `lead.narrate` span，使单条 trace 形成 Lead 路由、SubAgent 执行、Lead 叙述的层级。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/observability/context.py app/services/observability/tracer.py app/services/runner.py app/api/chat.py tests/test_observability.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_observability.py -q`，10 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestChatStreamEvents tests/test_lead_agent_tools.py -q`，18 passed、1 skipped；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py tests/test_analysis_blueprint.py -q`，104 passed、3 skipped；执行 `git diff --check`，通过。
- 残留风险：Runner 当前仍是进程内实现，`parent_observation_id` 只是协议预留；真实 Langfuse UI 中 span 层级和 tag 过滤效果仍依赖 `LANGFUSE_ENABLED=true`、有效 key 和运行时 Langfuse SDK 对 `update_trace(tags=...)` 的合并语义。

### 2026-06-12 15:54 · 修复 Manifest 路由候选展示名称

- 涉及文件：`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/assistant/chat-adapter.js`、`.codex/项目记忆.md`
- 关键改动：前端 assistant-ui 消息元数据开始透传 LeadAgent 的数据集级 `clarification`；澄清候选卡按类型区分业务术语和数据集场景，`dataset_missing/dataset_choice/manifest_route` 场景显示“请选择数据集”，候选主标题优先使用 `dataset_name`，副标题展示业务域、得分和审核状态，点击候选时写入 `selected_dataset_id`，避免把 Manifest 路由候选误渲染成“候选 1 / 业务术语”。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；使用 in-app Browser 打开并刷新 `http://localhost:5173/chat/233`，确认澄清卡显示“请选择数据集 / 生产经营管理系统日志数据集”，不再显示“候选 1 / 业务术语”。
- 残留风险：本次只修复前端展示与点击 payload；后端数据集级澄清状态机尚未持久化，用户点击后仍依赖下一轮文本与 `selected_dataset_id` 的后续消费能力。

### 2026-06-12 16:35 · SubAgent 多轮数据面 merge/digest 最小闭环

- 涉及文件：`datalogue-api/app/graph/state.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/tests/test_multiturn.py`、`.codex/项目记忆.md`
- 关键改动：`AgentState` 新增 `prior_capsule/out_capsule/multiturn_context/turn_type/merge_debug`；LangGraph 入口新增 `merge_prior_context` 节点，在有 `prior_capsule.query_context` 且本轮命中继续追问信号时，确定性提取维度、过滤、时间和 limit delta，合并为 `multiturn_context.merged_query_context`，并把短追问改写成带上一轮问题的完整问题；DSL 生成 prompt 注入多轮查询上下文；SQL 执行和 SubAgent 报告节点生成 `out_capsule`，包含下一轮可复用 `query_context` 与 ResultDigest 骨架。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/graph/state.py app/graph/workflow.py app/graph/nodes.py tests/test_multiturn.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_multiturn.py -q`，4 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestWorkflowRouting tests/test_chat.py::TestLangGraphNodes::test_entry_intent_metric_query tests/test_chat.py::TestLangGraphNodes::test_intent_recognition_query tests/test_chat.py::TestLangGraphNodes::test_schema_recall_appends_blueprint_context -q`，17 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestChatStreamEvents -q`，8 passed、1 skipped；执行 `git diff --check -- datalogue-api/app/graph/state.py datalogue-api/app/graph/workflow.py datalogue-api/app/graph/nodes.py datalogue-api/tests/test_multiturn.py`，通过。
- 残留风险：本轮只实现 SubAgent 图内的数据面最小闭环，`prior_capsule` 尚未由 API/ConversationStore 自动持久化和注入；delta 提取是启发式，不做复杂自然语言时间解析和字段级 schema 绑定；`out_capsule` 的 ResultDigest 仍是骨架摘要，后续可扩展更强的结果语义压缩。

### 2026-06-12 16:35 · LeadAgent 多轮控制面最小闭环

- 涉及文件：`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/prompts/lead_agent.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`.codex/项目记忆.md`
- 关键改动：`build_lead_agent_context()` 新增可选 `multiturn_context`，归一化 `active_dataset_id/inheritance_summary` 等跨轮控制面字段；新增确定性多轮分类 fallback，输出 `continue/switch/interpret/chitchat` 结构；`continue/interpret` 在没有 payload 和会话锁定时复用 `active_dataset_id`，`switch` 不继承旧数据集，`chitchat` 直接在 LeadAgent 停住不调度 SubAgent；`ToolPolicy` 和 `ThreadContext` 增加 `dataset_lock_source/inherited_dataset_locked`；SubAgent dispatch 增加 `capsule/subagent_capsule`，用于回显继承摘要、分类结果和数据集内状态边界。
- 验证方式：执行 `cd datalogue-api && .venv/bin/pytest tests/test_lead_agent_tools.py -q`，14 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py -q`，84 passed、3 skipped。
- 残留风险：多轮分类当前是确定性启发式 fallback，尚未接入专门的 LLM 分类器或持久化 ConversationStore；前端和 ConversationStore 未在本轮修改，`multiturn_context` 需要上层后续传入才能在真实会话里生效。

### 2026-06-12 16:41 · Phase 1 ConversationStore 与会话贯通

- 涉及文件：`datalogue-api/app/schemas/capsule.py`、`datalogue-api/app/schemas/chat.py`、`datalogue-api/app/core/config.py`、`datalogue-api/app/models/conversation.py`、`datalogue-api/app/models/__init__.py`、`datalogue-api/app/services/conversation_store.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/alembic/versions/j5k6l7m8n9o0_add_conversation_state.py`、`datalogue-api/tests/test_multiturn.py`、`.codex/项目记忆.md`
- 关键改动：新增 Capsule Protocol v1 基础 schema；`ChatRequest` 支持业务 `session_id` 和数据集澄清 `selected_dataset_id`；新增 `MULTITURN_ENABLED`、轮次锁 TTL、压缩和蓝图捷径子开关，默认关闭；新增 `ConversationState` 模型、单 head Alembic migration 和 `ConversationStore`，用条件 UPDATE 实现兼容 SQLite/PostgreSQL 的 session 轮次锁；`/api/chat/stream` 外层增加多轮 feature-flag wrapper，默认关闭时完全走原单轮 `_stream_chat_singleturn`，开启后加锁、正常结束写回 completed turn、异常或断连只释放锁不提交半写状态。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/schemas/capsule.py app/schemas/chat.py app/models/conversation.py app/services/conversation_store.py app/api/chat.py alembic/versions/j5k6l7m8n9o0_add_conversation_state.py tests/test_multiturn.py`，通过；执行 `cd datalogue-api && .venv/bin/alembic heads`，返回单一 head `j5k6l7m8n9o0`；执行 `cd datalogue-api && .venv/bin/pytest tests/test_multiturn.py -q`，7 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestChatStreamEvents tests/test_conversation.py -q`，12 passed、2 skipped；执行 `git diff --check`，通过。
- 残留风险：Phase 1 只完成 store 与默认关闭接入，尚未把 ConversationStore 的 active dataset、capsule、pending clarification 真正注入 LeadAgent/SubAgent；客户端未提供 `session_id` 且首轮无 `conversation_id` 时只能用请求级临时 session，待前端或后续 API 层稳定传递业务 session。

### 2026-06-12 16:58 · Phase 2 胶囊管道贯通

- 涉及文件：`datalogue-api/app/services/conversation_store.py`、`datalogue-api/app/services/runner.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_multiturn.py`、`.codex/项目记忆.md`
- 关键改动：`ConversationStore` 新增 LeadAgent 可读的多轮控制面上下文和 capsule 元字段 accessor，支持按数据集读取 `prior_capsule`、校验 capsule version/schema_version、损坏或不匹配时返回降级状态；`DatasetSubAgentRequest` 和 `InProcessDatasetSubAgentRunner` 支持注入 `prior_capsule/prior_capsule_status`，并把状态传入 LangGraph；`/api/chat/stream` 在选定有效数据集后按 `active_dataset_id/effective_dataset_id` 读取对应 capsule，传给 SubAgent，最终从 `out_capsule` 写回同一 dataset 桶；final payload、message metadata、trace metadata 透出 `prior_capsule_status/out_capsule/multiturn_context/turn_type/merge_debug`，便于观测和回归。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/conversation_store.py app/services/runner.py app/graph/state.py app/api/chat.py tests/test_multiturn.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_multiturn.py -q`，9 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_lead_agent_tools.py tests/test_chat.py::TestChatStreamEvents -q`，22 passed、1 skipped；执行 `cd datalogue-api && .venv/bin/alembic heads`，返回单一 head `j5k6l7m8n9o0`；执行 `git diff --check`，通过。
- 残留风险：Phase 2 只完成胶囊在 API/Runner/Store 之间的传递与版本降级，业务语义 merge、解释/闲聊分流、澄清恢复、消息压缩和灰度回归仍在后续 Phase；Langfuse 本地未启用时只能通过本地测试验证 metadata，不验证真实 UI trace 展示。

### 2026-06-12 17:44 · Phase 3 LeadAgent 控制面多轮正式收口

- 涉及文件：`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/prompts/lead_agent.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`.codex/项目记忆.md`
- 关键改动：正式收口 LeadAgent 多轮控制面，支持 `continue/switch/interpret/chitchat` 四类轮次；`continue/interpret` 在存在 ConversationStore `active_dataset_id` 时跳过 Manifest 自动路由并沿用 active dataset，且锁定优先级调整为 `payload.dataset_id > active_dataset_id > conversation.dataset_id`，避免旧 `conversation.dataset_id` 抢占多轮状态；`switch` 不继承旧数据集，继续走 Manifest 路由；`chitchat` 直接在 LeadAgent 生成阻断上下文，不调度 SubAgent；TimeTool 支持基于上一轮 `resolved_time_context` 推导“再看上个月”等相对表达；`interpret` dispatch capsule 标记 `execution_mode=interpret_result`、`should_generate_query=false`、`interpretation_source=prior_capsule.result_digest`，由 SubAgent 基于 prior capsule 解释结果而不是生成新查询。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/lead_agent.py tests/test_lead_agent_tools.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_lead_agent_tools.py tests/test_chat.py::TestChatStreamEvents -q`，24 passed、1 skipped；执行 `cd datalogue-api && .venv/bin/pytest tests/test_multiturn.py -q`，9 passed；执行 `git diff --check`，通过。
- 残留风险：Phase 3 的分类器仍是确定性 fallback，还没有独立 LLM 轮次分类 prompt；`interpret_result` 当前只在 LeadAgent dispatch capsule 中声明，下游 SubAgent 如何消费 ResultDigest 并完全绕过查询生成属于 Phase 4/后续数据面收口；时间继承首期只覆盖明确的“上月/上个月”锚点推导，复杂相对时间仍需扩展。

### 2026-06-12 17:53 · Phase 4 SubAgent 数据面 delta 继承正式收口

- 涉及文件：`datalogue-api/app/schemas/capsule.py`、`datalogue-api/app/graph/state.py`、`datalogue-api/app/graph/workflow.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/tests/test_multiturn.py`、`.codex/项目记忆.md`
- 关键改动：正式收口 SubAgent 数据面多轮继承；`merge_prior_context` 入口节点支持 `refine/drill/compare/new_query/interpret` 类型识别，基于 prior capsule 的 `query_context` 和本轮 delta 做确定性 merge，合并后 metrics 为空时降级为新查询；`interpret_result` 轮次直接基于 `prior_capsule.result_digest` 生成解释回答并结束，不进入 DSL/SQL 生成；DSL 生成 prompt 注入压缩后的 `prior_query_context/delta/merged_query_context`；ResultDigest 改为只保存列结构、行数、数值 min/max/sum、Top 值、`sql_audit_id`、回答预览和错误摘要，不再保存 sample rows 或全量结果；`SubAgentCapsule` schema 与实际 out capsule 对齐，补充 `schema_version/updated_turn/last_result_digest`；蓝图换参捷径在 `MULTITURN_BLUEPRINT_SHORTCUT_ENABLED=true` 时可从入口直接进入 `analysis_blueprint_execute`。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/schemas/capsule.py app/graph/nodes.py app/graph/workflow.py tests/test_multiturn.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_multiturn.py tests/test_lead_agent_tools.py -q`，29 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestWorkflowRouting tests/test_chat.py::TestLangGraphNodes::test_entry_intent_metric_query tests/test_chat.py::TestLangGraphNodes::test_intent_recognition_query tests/test_chat.py::TestLangGraphNodes::test_schema_recall_appends_blueprint_context tests/test_chat.py::TestChatStreamEvents -q`，25 passed、1 skipped；执行 `git diff --check`，通过。
- 残留风险：delta 抽取仍是确定性启发式，不做字段级 schema 绑定；蓝图换参捷径首期只识别过滤、时间和 limit 变化，复杂参数映射仍依赖后续蓝图参数抽取增强；`interpret_result` 使用规则化摘要回答，尚未接入专门的解释型 LLM prompt。

### 2026-06-12 18:05 · 多智能体产品用户手册 DOCX

- 涉及文件：`scripts/export_user_manual_docx.py`、`docs/数语多智能体产品用户手册.docx`、`.codex/项目记忆.md`
- 关键改动：新增可复用 DOCX 导出脚本，按 `compact_reference_guide` 风格生成“数语多智能体产品用户手册”；手册覆盖产品边界、角色工作流、导航、数据源、数据集与语义治理、分析蓝图、对话问数、多轮分析、查询审计、模型设置、排障、术语表和上线前检查清单；内嵌现有问数执行链路图，并为步骤组生成独立 Word 编号，避免跨章节编号延续。
- 验证方式：执行 `/Users/yangkai/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 scripts/export_user_manual_docx.py` 生成 DOCX；执行 `unzip -t docs/数语多智能体产品用户手册.docx`，通过；执行 `python-docx` 结构检查，确认 120 段落、12 张表、1 个 section、1 张图片；使用 Documents 渲染器配合 `DYLD_FALLBACK_LIBRARY_PATH=/Users/yangkai/.cache/codex-runtimes/codex-primary-runtime/dependencies/native/poppler/poppler/lib` 渲染为 12 页 PNG/PDF，并抽查封面、图页、步骤编号页和尾页，无裁切、重叠和异常空白。
- 残留风险：执行链路图按页面高度缩略展示，适合手册总览但不适合作为细节审图；若后续产品 UI 或多智能体能力继续变化，需要重新运行导出脚本并复核正文内容。

### 2026-06-12 18:01 · Phase 5 澄清跨轮恢复正式收口

- 涉及文件：`datalogue-api/app/services/conversation_store.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_multiturn.py`、`.codex/项目记忆.md`
- 关键改动：`ConversationStore` 新增 pending clarification 恢复状态机，支持数据集级澄清按 `selected_dataset_id`、候选序号或数据集名称恢复，并在用户明确取消或换话题时清除挂起态；SubAgent 术语冲突澄清采用重跑注入模式，`ConversationState.pending_clarification` 保存 `conversation_id/dataset_id/clarification_id/candidates/original_question`，下一轮自动注入 `clarification_response` 并复用现有 `PendingClarification` 表和 `clarification_resolution_node` 恢复原问题；`/api/chat/stream` wrapper 在进入单轮链路前派生 `effective_payload`，注入 dataset、conversation 或 clarification_response，不改原始 payload；final payload 统一压缩成下一轮可恢复的 pending state，澄清成功或用户换话题时通过 `clear_pending_clarification` 清理状态。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/conversation_store.py app/api/chat.py tests/test_multiturn.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_multiturn.py -q`，17 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py -k "conflict_creates_pending or conflict_enriches_existing or clarification_resolution" -q`，8 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestChatStreamEvents tests/test_lead_agent_tools.py -q`，24 passed、1 skipped；执行 `git diff --check`，通过。
- 残留风险：Sub 级字段缺失类澄清目前只保存通用 pending 信息，尚未有字段候选级别的恢复注入；术语恢复仍依赖当前 `PendingClarification` 表中的 TTL 和候选格式，后续如改 LangGraph interrupt/PostgresSaver 需迁移这层桥接逻辑。

### 2026-06-12 18:06 · Phase 6 消息压缩正式收口

- 涉及文件：`datalogue-api/app/services/conversation_store.py`、`datalogue-api/tests/test_multiturn.py`、`.codex/项目记忆.md`
- 关键改动：`ConversationStore.append_completed_turn()` 在写入完整轮次后按 `MULTITURN_COMPACTION_ENABLED` 和 `MULTITURN_COMPACTION_TOKEN_THRESHOLD` 判断是否压缩；压缩时只处理旧轮次，最近 2 轮 user/assistant 原文保留在 `messages` 中，旧内容写入 `compacted_summary`；压缩 prompt 固定使用 Langfuse Prompt Management 名称 `datalogue-compaction`，本地 fallback 明确只保留叙事线、口径偏好和未解决问题，禁止保留具体查询条件、SQL、完整结果行或可执行查询状态；LLM 压缩失败时降级为本地摘要，不阻断问数和状态写回。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/conversation_store.py tests/test_multiturn.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_multiturn.py -q`，18 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestChatStreamEvents tests/test_lead_agent_tools.py -q`，24 passed、1 skipped；执行 `git diff --check`，通过。
- 残留风险：当前 token 计数使用本地粗估算，不是模型 tokenizer 精确计数；压缩摘要暂不写独立 trace span，Phase 7 会把 `context-compaction` 观测补齐；本地 fallback 摘要质量有限，只作为 LLM 失败时的安全兜底。

### 2026-06-12 18:15 · Phase 7 Langfuse 多轮观测正式收口

- 涉及文件：`datalogue-api/app/services/observability/tracer.py`、`datalogue-api/app/services/runner.py`、`datalogue-api/app/services/conversation_store.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_observability.py`、`datalogue-api/tests/test_multiturn.py`、`.codex/项目记忆.md`
- 关键改动：`DatalogueTracer.create_trace_context()` 支持显式业务 `session_id`，多轮 wrapper 将 Langfuse session_id 对齐为 `ConversationState.session_id`；`/api/chat/stream` 新增 `context-assembly` 和 `turn-classification` span，分别记录多轮上下文组装和 LeadAgent 轮次分类；`InProcessDatasetSubAgentRunner` 监听 `merge_prior_context` 节点结束事件并记录 `delta-merge` span，输入包含 prior query context，输出包含 merge 前后 context、delta 和 merge_debug；消息压缩触发时记录 `context-compaction` span，输出压缩前后消息数、token 阈值、prompt 名称和摘要来源；trace metadata 增加 `multiturn_metrics`，覆盖轮次分类、delta 类型、delta 矛盾、蓝图捷径候选/命中、capsule loaded/stale/invalid 等代理指标。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/services/observability/tracer.py app/services/runner.py app/services/conversation_store.py app/api/chat.py tests/test_observability.py tests/test_multiturn.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_observability.py tests/test_multiturn.py -q`，30 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py::TestChatStreamEvents tests/test_lead_agent_tools.py -q`，24 passed、1 skipped；执行 `git diff --check`，通过。
- 残留风险：本地测试覆盖 span 调用和 metadata，不验证真实 Langfuse UI 中的树形展示；多轮代理指标目前写入 trace metadata，尚未落独立聚合表或仪表盘，Phase 8 灰度回归时再看是否需要报表化。

### 2026-06-12 18:23 · 真实页面截图版用户操作手册

- 涉及文件：`scripts/export_user_operation_manual_docx.py`、`docs/数语多智能体产品用户手册.docx`、`docs/user-manual-screenshots/01-workspace.png`、`docs/user-manual-screenshots/02-chat-empty.png`、`docs/user-manual-screenshots/03-datasets-blueprints.png`、`docs/user-manual-screenshots/04-datasources.png`、`docs/user-manual-screenshots/05-audit-query.png`、`docs/user-manual-screenshots/06-settings.png`、`docs/user-manual-screenshots/07-history.png`、`docs/user-manual-screenshots/08-apis.png`、`.codex/项目记忆.md`
- 关键改动：按用户反馈将原偏架构说明的用户手册重做为“真实截图 + 操作步骤”的操作手册；使用 Playwright MCP 截取本地 `http://localhost:5173` 真实页面，覆盖工作台、对话问数、数据集与分析蓝图、数据源、查询审计、系统设置、查询历史和 API 接口；新增操作手册导出脚本，按每个功能页组织“功能用途、截图、怎么用、操作完成后看什么、注意事项”，最终覆盖原 `docs/数语多智能体产品用户手册.docx`。
- 验证方式：执行 Playwright MCP 截图，8 张截图均为 1440x960；执行 `/Users/yangkai/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 scripts/export_user_operation_manual_docx.py` 生成 DOCX；执行 `unzip -t docs/数语多智能体产品用户手册.docx`，通过；执行 `python-docx` 结构检查，确认 189 段落、3 张表、1 个 section、8 张图片；使用 Documents 渲染器渲染为 20 页 PNG/PDF，抽查封面、对话问数截图页、数据集截图页和末页，无裁切、重叠和明显分页空白。
- 残留风险：当前截图工具只能导航和截图，不能点击子菜单，所以“系统设置 / LLM 模型”等二级设置以总设置页截图加文字步骤说明；后续若需要每个二级 Tab 的精确截图，可补充可点击截图流程后再次导出。

### 2026-06-12 18:26 · Phase 8 多轮回归与灰度收口

- 涉及文件：`datalogue-api/tests/test_multiturn_regression.py`、`datalogue-api/app/graph/nodes.py`、`.codex/项目记忆.md`
- 关键改动：新增 Phase 8 多轮回归矩阵测试，固定 25 条场景并逐条参数化执行，覆盖 refine、drill、compare、interpret、ResultDigest、蓝图捷径命中/关闭回落、数据集切换、澄清恢复、schema 失效、胶囊损坏、并发锁、断连兜底锁重置、消息压缩和 `MULTITURN_ENABLED=false` 单轮等价路径；补齐 SubAgent 数据面时间 delta 对“上个月”的识别，使其与 LeadAgent 时间继承词表一致。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/graph/nodes.py tests/test_multiturn.py tests/test_multiturn_regression.py tests/test_observability.py tests/test_lead_agent_tools.py`，通过；执行 `cd datalogue-api && .venv/bin/pytest tests/test_multiturn_regression.py -q`，26 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_multiturn.py tests/test_multiturn_regression.py tests/test_lead_agent_tools.py tests/test_observability.py -q`，72 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py -q`，84 passed、3 skipped；执行 `cd datalogue-api && .venv/bin/alembic heads`，返回单一 head `j5k6l7m8n9o0`；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：Phase 8 完成本地自动化回归和灰度前检查，但尚未实际开启线上灰度；Langfuse 真实 UI 树形展示、生产数据库迁移和 2 天内部账号 / 10% 用户灰度指标仍需部署后验证。

### 2026-06-12 18:34 · assistant 聊天链路稳定 session 与多轮真实消费验证

- 涉及文件：`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-api/tests/test_multiturn.py`、`.codex/项目记忆.md`
- 关键改动：`ChatModelAdapter` 在每次 `/api/chat/stream` 请求中显式传递业务 `session_id`，后端 conversation id 可用时稳定为 `conversation-{id}`，仅有 assistant-ui 本地 thread id 时稳定为 `assistant-thread-{threadId}`，无 thread id 时使用 adapter 生命周期内的 fallback session，避免后端落到一次性 `request-*` 状态；新增 API 级两轮追问回归用例，使用真实 `_stream_chat` 多轮 wrapper 验证同一业务 session 会把 `ConversationState.active_dataset_id` 传入 LeadAgent，并把 schema 匹配的 `prior_capsule` 注入 SubAgent request 和 initial_state，最终写回更新后的数据集 capsule。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_multiturn.py -q`，19 passed；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_multiturn_regression.py -q`，26 passed；执行 `cd datalogue-web && npm run lint`，0 errors、19 warnings；执行 `cd datalogue-web && npm run build`，通过。
- 残留风险：本次验证是 API 级真实 `_stream_chat` 包装链路，LLM 和图节点使用可控 runner 替代，避免依赖外部模型和本机 Langfuse；真实浏览器手工追问仍需要在开启 `MULTITURN_ENABLED=true` 的运行环境中复测。

### 2026-06-12 18:41 · 用户操作手册功能与 Tab 级细化

- 涉及文件：`scripts/export_user_operation_manual_docx.py`、`docs/数语多智能体产品用户手册.docx`、`.codex/项目记忆.md`
- 关键改动：按用户反馈把截图版操作手册从“页面级说明”细化为“功能 / Tab / 操作 / 验收点”说明；重点补齐数据集主 Tab（数据表、字段标注、指标、维度、分析蓝图、语义验证）、高级治理 Tab（SubAgent Manifest、语义词典、分析场景、权限、版本历史）、分析蓝图详情 Tab（概览、参数、输出列、业务逻辑、测试、使用记录）和蓝图新建两种流程；同时细化对话问数、数据源概览/Schema/DDL 同步、查询审计、历史会话、API 接口详情 Tab、系统设置 12 个二级菜单和角色检查清单。
- 验证方式：执行 `/Users/yangkai/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/export_user_operation_manual_docx.py`，通过；执行 `/Users/yangkai/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 scripts/export_user_operation_manual_docx.py` 生成 DOCX；执行 `unzip -t docs/数语多智能体产品用户手册.docx`，通过；执行 `python-docx` 结构检查，确认 183 段落、20 张表、8 张图片；使用 Documents 渲染器渲染为 25 页 PNG/PDF，抽查对话问数、数据集主 Tab、高级治理 Tab、蓝图流程、系统设置和末页，修复长句导致的异常断行后确认关键页可读。
- 残留风险：本次继续复用已有 8 张真实页面截图，没有为每个子 Tab 逐一补截图；子 Tab 的具体操作以真实代码结构和当前页面能力文字说明为准，后续如果需要“每个 Tab 一张截图”，还需要补充交互式点击截图流程并再次导出。

### 2026-06-12 19:11 · 用户操作手册补齐 Tab 真实截图

- 涉及文件：`scripts/export_user_operation_manual_docx.py`、`docs/数语多智能体产品用户手册.docx`、`docs/user-manual-screenshots/09-dataset-analysis-blueprints-tab.png` 至 `docs/user-manual-screenshots/40-blueprint-detail-overview-tab.png`、`.codex/项目记忆.md`
- 关键改动：按用户反馈补齐大量只有文字说明但没有截图的功能区域；新增并插入数据集主 Tab、数据集高级治理 Tab、数据源详情 Tab、API 接口详情 Tab、系统设置 12 个二级菜单和分析蓝图详情概览截图；`add_screenshot()` 增加 `keep_with_next`，避免截图标题留在上一页、图片落到下一页导致标题错位；最终手册从页面级截图扩展为功能/Tab 级截图说明。
- 验证方式：使用 Playwright MCP 在本地 `http://localhost:5173` 逐个点击真实 Tab 并截图，截图目录共 41 张 PNG；执行 `/Users/yangkai/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/export_user_operation_manual_docx.py`，通过；执行导出脚本重新生成 DOCX；执行 `unzip -t docs/数语多智能体产品用户手册.docx`，通过；执行 `python-docx` 结构检查，确认 283 段落、20 张表、39 张嵌入图片、1 个 section；使用 Documents 渲染器渲染为 54 页 PNG/PDF，抽查数据集 Tab、API 调用日志 Tab、设置二级菜单截图页，确认标题与截图贴合且无明显裁切。
- 残留风险：分析蓝图详情页当前真实页面点击蓝图卡片后未进入独立多 Tab 详情路由，本轮只补了蓝图详情概览截图；如果后续前端提供独立详情 Tab 路由或弹层，需要再补参数、输出列、业务逻辑、测试、使用记录的逐 Tab 截图。

### 2026-06-12 18:49 · ChatBI 思考过程三层展示收口

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/answer_explanation.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`、`datalogue-web/src/components/agent-panel.jsx`、`datalogue-web/src/components/audit-query.jsx`、`datalogue-web/src/components/message-content.jsx`、`datalogue-web/src/styles.css`、`.codex/项目记忆.md`
- 关键改动：后端新增稳定 `query_profile/explainability`，同时写入 final payload、assistant message metadata 和 trace metadata，覆盖路由路径、蓝图命中、多轮 `prior_query_context/delta/merged_query_context`、SQL 执行摘要和业务阶段聚合；消息内新增常显“查询口径”卡片，展示指标、维度、时间范围、过滤条件、蓝图/场景/临时查询路径和多轮继承说明，并支持发送纠正与重跑；右侧 Agent 面板改为业务阶段命名，完成后收敛成一行执行摘要，SQL 默认折叠；查询审计页保留第三层 trace 入口，但默认不直接渲染 prompt/raw input/raw metadata；前端 `MessageContent` 改为剥离 `<think>` 原始思维链，不再展示折叠思考文本；对话层内部 SQL 重试文案改为中性“链路不稳定/建议复核”。
- 验证方式：执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py -k 'query_profile or final_and_message_metadata' -q`，2 passed；执行 `cd datalogue-api && .venv/bin/pytest tests/test_chat.py -q`，86 passed、3 skipped；执行 `cd datalogue-web && npm run lint`，0 errors、15 warnings（剩余为既有未使用变量和 hook dependency warning）；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：口径卡片纠正当前复用现有 composer-submit 和 message reload 入口，还不是专用“修改参数后局部重跑”接口；查询审计页默认隐藏 raw prompt/metadata 后，开发深调仍需通过后端 trace 数据或后续新增受控调试开关查看完整 Langfuse payload。

### 2026-06-12 19:09 · Langfuse Prompt 批量创建脚本

- 涉及文件：`datalogue-api/app/services/observability/prompt_registry.py`、`datalogue-api/scripts/seed_langfuse_prompts.py`、`datalogue-api/tests/test_observability.py`、`.codex/项目记忆.md`
- 关键改动：新增 Prompt 注册表，汇总当前系统内需要纳入 Langfuse Prompt Manager 的 14 个 Prompt，包括意图识别、DSL 四条路径、报告生成、SQL 审计、LeadAgent Skill/Tool Planner、多轮压缩、字段/表标注和蓝图分析；每个 Prompt 均维护中文名称和中文描述，并写入 Langfuse config 的 `display_name/chinese_name/chinese_description/description`；新增 `scripts/seed_langfuse_prompts.py`，默认 dry-run，传 `--apply` 后按 `LANGFUSE_PROMPT_LABEL` 创建 text prompt，远程同 label 内容或 config 未变化时跳过，`--force` 可强制创建新版本；保留现有运行期拉取逻辑不改。
- 验证方式：执行 `cd datalogue-api && ./.venv/bin/python scripts/seed_langfuse_prompts.py`，dry-run 列出 14 个 Prompt 的中文名称和中文描述；执行 `cd datalogue-api && ./.venv/bin/python scripts/seed_langfuse_prompts.py --apply`，在本机 Langfuse 创建 production label 的 14 个 v1 Prompt；补充中文名称/中文描述后再次执行 `--apply`，创建 14 个 production v2 Prompt；再次执行同一命令，全部返回 `skipped / unchanged`；执行 `cd datalogue-api && ./.venv/bin/python -m py_compile app/services/observability/prompt_registry.py scripts/seed_langfuse_prompts.py tests/test_observability.py`，通过；执行 `cd datalogue-api && ./.venv/bin/python -m pytest tests/test_observability.py -q`，14 passed。
- 残留风险：注册表是当前代码 Prompt 的显式快照，后续新增或修改 Prompt 时需要同步维护注册表并重新执行脚本；真实写入依赖 `.env` 中的 `LANGFUSE_BASE_URL/LANGFUSE_PUBLIC_KEY/LANGFUSE_SECRET_KEY` 可用。

### 2026-06-13 11:30 · LeadAgent LLM 交互日志与意图识别澄清回复修复

- 涉及文件：`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/prompts/intent_router.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`.codex/项目记忆.md`
- 关键改动：
  - LeadAgent Planner 的两阶段 LLM 调用（skill_selector / tool_planner）新增 INFO 级摘要日志（prompt 名、版本、selected_skills、disclosed_tools、locked_dataset_id、parse_ok、selected/planned tool、reasoning_summary、raw_len）和 DEBUG 级完整 system/human 文本日志，便于回查；原失败路径的 `logger.warning` 升级为 `logger.exception` 带堆栈。
  - 新增 `test_planner_logs_prompt_and_response`：使用 caplog 验证两阶段日志都会出现。
  - 意图识别 prompt 增加"多轮澄清识别"段，明确"选择/选/直接列名/第一个"等回复必须判为 `query`；`intent_recognition_node` 在 human_text 中新增【多轮提示】块，带上 `multiturn_context.pending_clarification.kind` 和 `clarification_response` 存在的事实。
  - 入口分类 function 拒答分支前加短路：`dataset_id` 已锁 + `multiturn_context.pending_clarification.kind` 存在时跳过 rejection，按 query 继续走 QueryGraph；其他场景仍走原拒答。
  - 新增 5 个回归测试：短路放行、真实功能操作仍拒答、dataset 未锁不放行、prompt 含澄清规则、human_text 含多轮提示块。
- 验证方式：执行 `cd datalogue-api && python3 -m pytest -x -q`，321 passed；执行 `cd datalogue-api && python3 -m pytest tests/test_lead_agent_tools.py -q`，17 passed；执行 `cd datalogue-api && python3 -m pytest tests/test_chat.py -k "entry_intent_function or clarification_rule or clarification_hint" -q`，5 passed。
- 残留风险：INFO 日志每次 Planner 调用都会输出两行摘要（含 prompt 版本号），如果想压低量级可以改为仅在 parse 失败/降级时打 INFO；LeadAgent 的日志仅覆盖 `plan_tool_calls_with_llm`，未覆盖 `compaction` LLM 调用的 prompt/response，需要时再补。

### 2026-06-14 10:02 · 数语系统设计方案文档

- 涉及文件：`docs/数语系统设计方案.md`、`.codex/项目记忆.md`
- 关键改动：新增当前数语项目系统设计方案，按当前代码基线梳理项目定位、总体架构、端到端链路、问数主链路、LeadAgent 控制面、SubAgent Manifest 治理、数据集治理、多轮上下文、SQL 生成执行诊断、分析蓝图、可观测查询审计、接口设计、Prompt 与模型配置、部署验证和后续实施路线；文档内补充 11 个 Mermaid 图块，覆盖整体架构图、端到端业务链路、LangGraph 主链路、查询请求时序、LeadAgent 内部流程、Manifest 字段分层、Manifest 发布时序、数据模型 ER 图、多轮时序、SQL 诊断重试链路和 trace 分层。
- 验证方式：执行 `wc -l docs/数语系统设计方案.md`，确认文档 846 行；执行脚本检查文档字符数、Mermaid 图块数、代码围栏数和关键章节，确认约 26704 字符、11 个 Mermaid 图块、关键章节无缺失；执行 `rg` 检查 `LeadAgent`、`SubAgent Manifest`、`/api/chat/stream`、`sequenceDiagram`、`flowchart`、`erDiagram` 等核心内容均已覆盖。
- 残留风险：本次生成的是 Markdown 设计方案，未同步导出 DOCX/PDF；文档 Mermaid 渲染依赖阅读器支持，未在独立 Mermaid 渲染器中逐图截图验收；后续如果代码继续演进 LeadAgent 多数据集策略或 SubAgent 服务化，需要同步更新本文档。

### 2026-06-14 16:29 · Phase4 术语澄清早退顺序与 Trace 收尾修复

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`、`.codex/项目记忆.md`
- 关键改动：将 `resolve_term_clarification()` 前移到 `route_query_intent()` 之前，resolved 时用恢复后的原问题参与入口路由，missing/expired/unresolved 时用术语澄清结果覆盖普通 `clarify` 早退，避免用户回复“第一个/术语名/都不是”被普通入口澄清吞掉；新增早退 Trace 收尾 helper，在 `_early_route_return()` 和 `_interpret_early_return()` 发送 final 前同步写入 `trace output`、`response_metadata.langfuse/observability`、`ObservabilityTraceIndex`，并在非 defer 模式下关闭 trace，避免 SSE final 后客户端断开导致收尾丢失。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/api/chat.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_chat.py::TestChatStreamEvents::test_chat_stream_pending_term_resolution_preempts_generic_clarify tests/test_chat.py::TestChatStreamEvents::test_chat_stream_direct_answer_early_return_writes_trace_index -q`，2 passed；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_chat.py tests/test_term_clarification_service.py tests/test_phase4_equivalence.py tests/test_lead_agent_routing.py tests/test_lead_agent_merge.py tests/test_multiturn_context_builder.py -q`，148 passed、3 skipped；执行 `git diff --check`，通过。
- 残留风险：Ruff 未在当前 `.venv` 中安装，无法用 `.venv/bin/python -m ruff` 做 lint；早退分支已补 TraceIndex 和 metadata，但 `query_profile/explainability` 仍为空，后续如果产品需要在闲聊/拒答/澄清早退里展示统一口径卡片，需要单独补早退版 profile。

### 2026-06-14 18:45 · 页面全链路 LangGraph noop 修复

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`.codex/项目记忆.md`
- 关键改动：真实页面从 `/chat` 选择“生产经营管理系统日志数据集”并发送“本月各品类 GMV 结构占比”时，链路在 `LeadAgent · 入口路由` 后触发 LangGraph `Must write to at least one of ...`；根因是 Phase 3 上提入口路由后，保留在图内的 `lead_agent_node` noop 返回 `{}`，不满足 LangGraph 节点必须写入 state 的要求。将该节点改为回写已有 `lead_agent_context`，保持路由和业务字段不变，同时满足图执行约束。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/graph/nodes.py app/api/chat.py app/services/dataset_subagent.py`，通过；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_dataset_subagent.py tests/test_phase4_equivalence.py tests/test_phase5_equivalence.py tests/test_phase6_equivalence.py tests/test_phase7_equivalence.py tests/test_term_clarification_service.py tests/test_chat.py -q`，188 passed、3 skipped；使用 Playwright 打开 `http://localhost:5173/chat`，选择日志数据集并发送“本月各品类 GMV 结构占比”，页面成功进入 `/chat/250`，展示 LeadAgent、Manifest 路由、Schema 召回、DSL 生成、DSL 校验、SQL 编译、SQL 执行、报告生成、查询口径、查询结果、SQL 和 trace id。
- 残留风险：本轮点击右上“推理过程”按钮时浏览器工具拒绝该动作，未展开右侧 AgentPanel；但消息内推理步骤和 SSE final 已证明主链路完成。当前测试问题使用“本月”会解析到 2026-06，示例数据返回空结果，业务上需要确认是否应查询历史月份。

### 2026-06-14 19:03 · 工作日志问题页面链路修复

- 涉及文件：`datalogue-api/app/services/lead_agent_routing.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/tests/test_lead_agent_routing.py`、`datalogue-api/tests/test_chat.py`、`.codex/项目记忆.md`
- 关键改动：使用真实页面问题“查询汤杰前年的工作日志”验证时，先发现入口路由把“工作日志”类问题判成普通澄清，补充 `日志/工作日志/工作日报` 明细查询路由词，确保进入 QueryGraph；继续验证后发现明细 DSL 在 `tables_json` 为空、`metrics` 为空但 `fields` 已命中表名时仍报“无法确定主表”，因此在 DSL 编译阶段复用 fields/dimensions 收集出的 `used_tables` 作为主表兜底，避免明细查询被错误中断。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_lead_agent_routing.py tests/test_chat.py -q`，104 passed、3 skipped；使用 Playwright 打开 `http://localhost:5173/chat`，选择“生产经营管理系统日志数据集”并发送“查询汤杰前年的工作日志”，最终会话 `/chat/256` 完成 LeadAgent 工具、Manifest 路由、LeadAgent 入口路由、Schema 召回、DSL 生成、DSL 校验、SQL 编译、SQL 执行和报告生成；SQL 执行返回 0 行、2 列，报告显示“未查询到汤杰前年的工作日志记录”，页面控制台 0 errors、0 warnings，trace id 为 `72ba79423d230c89bedd0af8006e79ba`。
- 残留风险：当前链路已完成，但真实数据结果为 0 行，需要业务确认汤杰在 2024 年是否存在日报数据，或姓名/账号过滤口径是否应从 `account eq 汤杰` 调整为其他字段；本轮不改数据内容。

### 2026-06-15 14:13 · SubAgent 查询规划层第一版改造

- 涉及文件：`datalogue-api/app/services/subagent_planning/`、`datalogue-api/app/services/dataset_subagent.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/services/dataset_context.py`、`datalogue-api/tests/test_subagent_planning_contracts.py`、`datalogue-api/tests/test_subagent_candidate_assets.py`、`datalogue-api/tests/test_subagent_query_planner.py`、`datalogue-api/tests/test_subagent_execution.py`、`datalogue-api/tests/test_subagent_run.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_dataset_context.py`、`datalogue-web/src/assistant/chat-page.jsx`、`datalogue-web/src/components/agent-panel.jsx`、`.codex/项目记忆.md`
- 关键改动：完成 SubAgent 查询规划第一版，链路从“命中蓝图直接强执行 / 未命中语义资产直接报错”调整为“候选资产召回 -> 查询计划判断 -> 执行策略选择”。候选资产层第一版召回 6 类资产：蓝图、指标、维度、业务术语、表字段、数据集上下文；查询计划层输出 `QueryPlan`，支持 `blueprint_execute`、`blueprint_as_reference`、`query_graph`、`clarify`、`reject` 5 种策略；命中蓝图时先判断参数完整性和问题适用性，不再默认强套 SQL；蓝图不适用但有参考价值时注入 DSL 生成上下文；未命中指标/维度/蓝图时允许基于表字段和数据集上下文继续推理。Chat API 的 SSE、final payload、message metadata、trace metadata 和前端 AgentPanel 均展示候选资产与查询计划；数据集上下文补齐蓝图 `description/when_to_use/parameters/call_template/raw_sql`，保证规划层能在缺参数日报问题上提前澄清。
- 验证方式：使用 FastAPI `TestClient` 跑真实 `/api/chat/stream` 分支，固定 LeadAgent 路由和 SubAgent 真实规划，验证 4 类问题：`查询张三昨天的个人日报` 走 `blueprint_execute`，`查询10条用户日志` 不走蓝图强执行并走明细策略，`最近10条失败日志有哪些` 走 QueryGraph/蓝图参考策略，`查一下日报` 在规划层输出 `clarify`；执行 `cd datalogue-api && ./.venv/bin/pytest tests/test_dataset_context.py tests/test_subagent_planning_contracts.py tests/test_subagent_candidate_assets.py tests/test_subagent_query_planner.py tests/test_subagent_execution.py tests/test_subagent_run.py tests/test_chat.py tests/test_dataset_subagent.py tests/test_phase5_equivalence.py tests/test_phase6_equivalence.py tests/test_phase7_equivalence.py -q`，206 passed、3 skipped；执行 `cd datalogue-web && npm run lint`，0 errors、15 warnings；执行 `cd datalogue-web && npm run build`，通过。
- 残留风险：第一版以稳定策略和可解释链路为主，LLM 规划质量、资产评分召回、蓝图参数从路由实体到执行参数的统一映射、跨轮计划继承、真实 Langfuse 规划质量报表仍属于第二版“规划质量增强”；第二版开始前应先确认是否进入该阶段，避免一次性扩大到完整 LeadAgent 重写。

### 2026-06-15 14:49 · SubAgent 查询规划层第二版规划质量增强

- 涉及文件：`datalogue-api/app/services/subagent_planning/asset_recall.py`、`datalogue-api/app/services/subagent_planning/contracts.py`、`datalogue-api/app/services/subagent_planning/planner.py`、`datalogue-api/app/services/dataset_subagent.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_subagent_candidate_assets.py`、`datalogue-api/tests/test_subagent_planning_contracts.py`、`datalogue-api/tests/test_subagent_query_planner.py`、`datalogue-api/tests/test_subagent_run.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/components/agent-panel.jsx`、`.codex/项目记忆.md`
- 关键改动：完成第二版“规划质量增强”核心闭环。候选资产评分升级为 `candidate_asset_score_v2`，支持 `exact/contains/alias/synonym/trigger_example/field_display/table_context` 多信号叠加、0.99 封顶、coverage/top asset type 审计；`QueryPlan` 新增 `decision_factors/planner_warnings/governance_suggestions` 三类审计字段，并在 LLM planner、fallback planner、SSE/final payload、`query_plan_debug`、AgentPanel 和消息 reasoning 中展示；fallback planner 支持多蓝图候选对比，最高匹配蓝图进入 selected/reference，其余蓝图进入 rejected，并修复 0 分蓝图被“报告/分析”等泛词误执行的风险；`blueprint_execute` 执行前合并 `routing.entities` 与 `route_payload.params/input_params/parameters`，保证规划层判断参数已满足后，执行层也能拿到 `user_name/start_date` 等已确认参数。
- 验证方式：执行 `cd datalogue-api && ./.venv/bin/pytest tests/test_subagent_candidate_assets.py tests/test_subagent_planning_contracts.py tests/test_subagent_query_planner.py tests/test_subagent_execution.py tests/test_subagent_run.py tests/test_dataset_subagent.py tests/test_chat.py tests/test_dataset_context.py tests/test_phase5_equivalence.py tests/test_phase6_equivalence.py tests/test_phase7_equivalence.py -q`，215 passed、3 skipped；执行 `cd datalogue-web && npm run lint`，0 errors、15 warnings；执行 `cd datalogue-web && npm run build`，通过；使用 FastAPI `TestClient` 跑真实 `/api/chat/stream` 分支，验证 4 类问题仍分别走 `blueprint_execute`、非强制蓝图、`query_graph/blueprint_as_reference`、`clarify`，且 final payload 均包含 `decision_factors` 和 `candidate_asset_score_v2`。
- 残留风险：第二版仍是启发式评分和保守参数映射，只透传简单标量参数；历史成功查询候选、审计页完整独立视图、基于 trace 的治理报表和跨轮 QueryPlan 继承尚未展开，后续可作为第二版后续小迭代或第三版多 SubAgent / 多数据集能力的一部分。

### 2026-06-15 16:08 · 模型 Think 标签关闭后的流式泄露修复

- 涉及文件：`datalogue-api/app/utils/think.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/services/report_generation.py`、`datalogue-api/tests/test_think_utils.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/components/message-content.jsx`、`datalogue-web/src/components/audit-query.jsx`、`.codex/项目记忆.md`
- 关键改动：定位 Think 标签仍出现在 LLM 回复中的根因是“最终 answer 已按模型配置清理，但报告生成和 LangGraph `report_generator` 的流式 token 会在最终清理前直接 SSE 推给前端”；新增统一 Think 清理工具，支持大小写、带属性开标签和跨 chunk 流式过滤；报告生成流式输出在 `thinking_enabled=false` 时只发送清理后的可见 token，最终 answer 继续清理；聊天 SSE 对 `report_generator` 原生 `on_chat_model_stream` token 增加同样过滤；前端消息渲染和查询审计页的兜底清理从大小写敏感的 `<think>` 正则升级为兼容 `<Think>` 和 `<think ...>`。
- 验证方式：执行 `cd datalogue-api && ./.venv/bin/pytest tests/test_think_utils.py tests/test_chat.py::TestLangGraphNodes::test_report_generator_compacts_rows_and_strips_think tests/test_chat.py::TestLangGraphNodes::test_report_generator_stream_strips_think_tokens_when_disabled tests/test_chat.py::TestLangGraphNodes::test_invoke_llm_with_metrics_strips_think_when_disabled tests/test_chat.py::TestChatStreamEvents::test_chat_stream_report_generator_strips_think_tokens tests/test_llm_config.py -q`，13 passed；执行 `cd datalogue-api && ./.venv/bin/python -m py_compile app/utils/think.py app/api/chat.py app/graph/nodes.py app/services/report_generation.py`，通过；执行 `cd datalogue-web && npm run lint`，0 errors、15 warnings（均为既有 warning）；执行 `cd datalogue-web && npm run build`，通过。
- 残留风险：模型侧 `enable_thinking=false` 仍是按供应商能力 best-effort，下游过滤负责用户可见层不泄露；本轮未真实调用外部模型验证供应商是否完全停止生成 Think 内容，后续如果接入新供应商，需要在 `build_llm_model_kwargs()` 中补充对应关闭参数。

### 2026-06-15 16:21 · SubAgent 查询规划 Langfuse Trace 增强

- 涉及文件：`datalogue-api/app/services/dataset_subagent.py`、`datalogue-api/app/services/subagent_planning/planner.py`、`datalogue-api/tests/test_subagent_run.py`、`datalogue-api/tests/test_subagent_query_planner.py`、`.codex/项目记忆.md`
- 关键改动：为 SubAgent 查询规划补齐 Langfuse 可观测节点：`DatasetSubAgent.run()` 中新增 `subagent.candidate_assets` span，记录数据集、问题、manifest/schema 版本、召回资产数量、summary 和 recall_debug；新增 `subagent.query_plan` span，记录候选资产摘要、执行策略、query_type、confidence、planner_source、fallback_reason、validation_error、资产选择数量、decision_factors、planner_warnings 和 governance_suggestions；`plan_query()` 内新增 `llm.subagent_query_planner` generation，真实聊天请求的活跃 observability context 下记录 planner LLM 输入、模型名、成功策略或 fallback metadata；LLM 调用失败、JSON 解析失败、硬规则校验失败都会把 `fallback_reason` 与 `validation_error` 分开写入 metadata，并同步放入 `QueryPlan.debug.validation_error`。
- 验证方式：按 TDD 先新增失败测试，确认缺少 span/generation 与 validation_error 时失败；实现后执行 `cd datalogue-api && ./.venv/bin/pytest tests/test_subagent_run.py tests/test_subagent_query_planner.py tests/test_subagent_candidate_assets.py tests/test_subagent_planning_contracts.py -q`，47 passed；执行 `cd datalogue-api && ./.venv/bin/pytest tests/test_chat.py::TestChatStreamEvents::test_stream_chat_emits_query_plan_step tests/test_chat.py::TestChatStreamEvents::test_blueprint_route_enters_subagent_run tests/test_chat.py::TestChatStreamEvents::test_chat_stream_final_and_message_metadata_include_query_profile -q`，3 passed；执行 `cd datalogue-api && ./.venv/bin/python -m py_compile app/services/dataset_subagent.py app/services/subagent_planning/planner.py tests/test_subagent_run.py tests/test_subagent_query_planner.py`，通过。
- 残留风险：本轮验证了 tracer 调用契约和 Chat 透传路径，未打开真实 Langfuse 页面逐项截图；planner generation 只在当前请求存在 active observability context 时开启，避免直接单测或脚本调用时误触发外部 exporter。

### 2026-06-15 16:43 · 前端与 Trace 节点名统一为原始节点名

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/dataset_subagent.py`、`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/services/lead_agent_routing.py`、`datalogue-api/app/services/runner.py`、`datalogue-api/app/services/conversation_store.py`、`datalogue-api/app/services/observability/tracer.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_subagent_run.py`、`datalogue-web/src/components/agent-panel.jsx`、`datalogue-web/src/components/chat-page.jsx`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/MyMessage.jsx`、`datalogue-web/src/assistant/thread-list-adapter.js`、`.codex/项目记忆.md`
- 关键改动：按用户要求把前端步骤和 Langfuse trace observation 展示名从中文业务名统一改为原始节点名；`_NODE_DISPLAY_NAMES` 改为 `intent_recognition/schema_recall/dsl_generate/sql_execute/sql_audit/report_generator` 等原始 LangGraph 节点名；LeadAgent 自定义节点改为 `lead_agent`、`lead.routing`、`lead_agent_routing`、`lead_agent_control_plane`、`lead.merge_prior_context`、`lead.narrate`、`context-assembly`、`turn-classification`、`context-compaction`、`delta-merge`、`interpret_result`；SubAgent 节点保持 `subagent.candidate_assets`、`subagent.query_plan`；前端 AgentPanel、assistant-ui reasoning、历史线程 adapter 和 chat adapter 的本地兜底映射同步改为原始节点名；Langfuse generation 展示名优先使用传入的 `llm.*` 原始调用名，例如 `llm.subagent_query_planner`。
- 验证方式：执行 `cd datalogue-api && ./.venv/bin/pytest tests/test_chat.py::TestChatAPI::test_sql_audit_node_is_exposed_to_stream_payloads tests/test_chat.py::TestChatStreamEvents::test_stream_chat_emits_query_plan_step tests/test_chat.py::TestChatStreamEvents::test_blueprint_route_enters_subagent_run tests/test_subagent_run.py::test_subagent_run_emits_candidate_assets_and_query_plan tests/test_subagent_run.py::test_subagent_run_records_candidate_assets_and_query_plan_spans tests/test_subagent_query_planner.py::test_plan_query_records_llm_generation_with_fallback_metadata -q`，6 passed；执行 `cd datalogue-api && ./.venv/bin/python -m py_compile app/api/chat.py app/services/dataset_subagent.py app/services/observability/tracer.py app/services/lead_agent.py app/services/lead_agent_routing.py app/services/runner.py app/services/conversation_store.py`，通过；执行 `cd datalogue-web && npm run lint`，0 errors、15 warnings（既有 warning）；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：语义资产、字段、Prompt Manager 里的 `display_name` 仍保留中文，因为它们不是 trace 节点名；本轮未打开真实 Langfuse 页面截图确认，但后端 span/generation 展示名和前端本地映射均已按原始节点名收口。

### 2026-06-15 17:32 · LLM 全量统一 LiteLLM SDK

- 涉及文件：`datalogue-api/app/graph/llm.py`、`datalogue-api/app/api/llm.py`、`datalogue-api/app/services/subagent_planning/planner.py`、`datalogue-api/pyproject.toml`、`datalogue-api/requirements.txt`、`datalogue-api/uv.lock`、`datalogue-api/tests/test_llm_config.py`、`.codex/项目记忆.md`
- 关键改动：按“系统里的 LLM 全部使用 LiteLLM”的要求，将 `get_llm()` 的唯一客户端实现切换为进程内 LiteLLM SDK 适配器 `LiteLLMChatClient`，不再按 provider 分流到 `ChatOpenAI`；适配器对外保留现有 `invoke()` / `stream()` 接口，内部把 LangChain messages 转成 LiteLLM/OpenAI chat messages，并返回 `AIMessage` / `AIMessageChunk`，避免改动 LeadAgent、DSL 生成、SQL 审计等调用方；数据库里的 `provider` 现在只用于 LiteLLM 模型名前缀归一和 Think 扩展参数生成，例如 `provider=openai, model=MiniMax-M3, base_url=https://api.minimaxi.com/v1` 会走 `model=openai/MiniMax-M3`。模型连接测试接口同步改为直接构造 `LiteLLMChatClient`；SubAgent planner 的 LLM 异常模块分类补充 `litellm`，保证 SDK 报错仍进入模型调用 fallback 诊断。依赖新增 `litellm>=1.70,<1.83`，`uv lock` 解析到 `litellm==1.80.0`，避免升级当前 `pydantic==2.7.4` 与 `langchain-openai==0.1.7` 依赖栈。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/graph/llm.py app/api/llm.py app/services/subagent_planning/planner.py tests/test_llm_config.py`，通过；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_llm_config.py -q`，7 passed；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_chat.py::TestLangGraphNodes::test_invoke_llm_with_metrics_strips_think_when_disabled -q`，1 passed；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_subagent_query_planner.py -q`，18 passed；执行 `git diff --check`，通过；执行 `cd datalogue-api && uv lock`、`uv sync`，成功安装 `litellm==1.80.0`；使用现有 default MiniMax 配置做一次 LiteLLM SDK live call，`model=openai/MiniMax-M3`、`api_base=https://api.minimaxi.com/v1` 返回 `content='\nOK'`，usage 为 `prompt_tokens=183 / completion_tokens=27 / total_tokens=210`。
- 残留风险：MiniMax 通过 OpenAI-compatible 模式调用时仍返回独立 `reasoning_content`，适配器只取 `message.content`，不会写入业务回答，但是否真正关闭模型侧 reasoning 仍取决于 LiteLLM/MiniMax 支持的参数，后续需要单独验证 no-think / reasoning 控制参数；如果要让 LiteLLM 内置 provider 直连 MiniMax，可把模型名配置为 `minimax/MiniMax-M3` 并按 LiteLLM provider 要求补齐对应密钥环境或参数。

### 2026-06-15 18:06 · LLM 慢节点与日志明细正确性联合优化

- 涉及文件：`datalogue-api/app/graph/llm.py`、`datalogue-api/app/api/llm.py`、`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/services/lead_agent_routing.py`、`datalogue-api/app/services/dataset_subagent.py`、`datalogue-api/app/services/subagent_planning/contracts.py`、`datalogue-api/app/services/subagent_planning/planner.py`、`datalogue-api/app/services/subagent_planning/execution.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/tests/test_llm_config.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`datalogue-api/tests/test_subagent_query_planner.py`、`datalogue-api/tests/test_subagent_execution.py`、`datalogue-api/tests/test_chat.py`、`.codex/项目记忆.md`
- 关键改动：落地三轴优化：一是 LLM 总线按角色加调用策略，`intent/lead_agent/dsl` 默认 `max_tokens` 封顶并启用 JSON-only，`sql_audit` 只封顶；二是 LeadAgent 在锁定数据集的新自包含查询上走确定性工具计划，跳过 skill_selector/tool_planner 两次 LLM；三是 SubAgent `QueryPlan.planner_source` 扩为 `deterministic/template/llm/fallback`，明细查询已召回 field/table 时前置规则规划，日志明细拒绝日报蓝图污染，并把 `selected_main_table/join_hints/schema_token_budget` 写入 debug；dataset 10 的日志明细命中 `dataset10_log_detail` 模板，DSL 节点在创建 LLM 前直接消费 `query_plan.debug.sql_template` 返回 `direct_sql`；蓝图参考上下文只保留名称、适用场景和参数元信息，不再注入 raw SQL；DSL 资产目录按 QueryPlan 已选表字段裁剪，避免重复灌全量字段；修复 intent LLM 调用失败时 tracer fallback 传入不支持 `error=` 参数的问题。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/graph/llm.py app/api/llm.py app/services/lead_agent.py app/services/subagent_planning/contracts.py app/services/subagent_planning/planner.py app/services/subagent_planning/execution.py app/services/dataset_subagent.py app/graph/nodes.py tests/test_llm_config.py tests/test_lead_agent_tools.py tests/test_subagent_query_planner.py tests/test_subagent_execution.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_llm_config.py tests/test_lead_agent_tools.py tests/test_subagent_query_planner.py tests/test_subagent_execution.py tests/test_subagent_run.py tests/test_chat.py -q`，159 passed、3 skipped；单独执行 `tests/test_chat.py::TestChatStreamEvents::test_chat_stream_final_and_message_metadata_include_query_profile`，1 passed。
- 残留风险：dataset 10 日志明细模板当前使用 MySQL 风格 `DATE_SUB(CURDATE(), INTERVAL 30 DAY)`，适配生产经营管理系统日志数据集的现有口径；如果该数据源实际切到 PostgreSQL/Oracle，需要按 datasource dialect 注册模板变体。模板覆盖的是“无人员筛选的最近 N 条日志/工作日志”首批高频场景，带人员姓名、状态等复杂过滤仍会进入 QueryGraph/DSL 裁剪路径。

### 2026-06-15 18:18 · 明细查询空 DSL 错误提示收窄

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/tests/test_chat.py`、`.codex/项目记忆.md`
- 关键改动：在 DSL 校验节点新增明细查询空 DSL 的专用诊断。当 `query_plan.query_type=detail_query` 且 DSL 没有 `metrics/dimensions/fields`、也没有模板 `direct_sql` 时，错误从通用“查询条件不足”收窄为“字段召回不足或模板未命中”，并提示检查 `candidate_assets`、`routing.dataset_id`、`query_plan.debug.template_name/sql_template`，避免误导为语义层整体配置或数据源连接故障。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/graph/nodes.py tests/test_chat.py`，通过；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_chat.py -q`，100 passed、3 skipped。
- 残留风险：本轮只优化错误表达和诊断方向，仍保留现有 `should_retry=True` 重试行为；如果要进一步减少空转，需要后续把 detail query 空 DSL 分支改为不重试或直接走 QueryPlan selected fields fallback。

### 2026-06-15 18:30 · DSL 语义层提示词渐进式披露

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/tests/test_query_plan_prompting.py`、`.codex/项目记忆.md`
- 关键改动：将 DSL 生成的语义层确定性路径从“完整 `schema_context` + 额外资产目录”的大 prompt，改为 L0-L3 渐进式披露上下文：L0 数据集与任务、L1 查询约束和数据集级硬约束、L2 按 QueryPlan 裁剪后的相关语义资产、L3 召回预算摘要。字段目录继续按 `query_plan.selected_assets` 裁剪，指标和维度资产补充 `expr/table/time_field/column` 等必要语义，避免砍 prompt 时丢失生成依据；语义层 prompt 不再重复追加完整“所选表字段与样例”。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m py_compile app/graph/nodes.py tests/test_query_plan_prompting.py`，通过；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_query_plan_prompting.py tests/test_chat.py::TestChatAPI::test_dsl_asset_catalog_filters_fields_by_query_plan -q`，9 passed；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_chat.py -q`，100 passed、3 skipped。
- 残留风险：当前渐进式披露先落在 DSL semantic prompt 入口，不改变上游 `build_dataset_query_context()` 仍会构建完整 `schema_context` 的行为；后续如果要进一步降低 schema 召回成本，应把 L0-L3 下沉到 dataset context 构建阶段，并记录每层 token 预算。

### 2026-06-15 19:30 · Thread Memory 与 QueryTaskCapsule 基础状态

- 涉及文件：`datalogue-api/app/services/task_capsule.py`、`datalogue-api/app/services/conversation_store.py`、`datalogue-api/tests/test_task_capsule.py`、`.codex/项目记忆.md`
- 关键改动：新增 QueryTaskCapsule/last_success_task 基础结构，支持把上一轮成功查询保存为可复用任务状态，明细查询不再要求必须有 metrics，只要 `fields/main_table/query_plan/dsl` 任一存在即可作为查询目标；线程级状态第一版固定写入 `ConversationState.subagent_capsules["_thread"]`，不引入 migration，并支持浅合并、显式 `user_id` 和 JSON 化写入；`capsule_metas()` 跳过 `_thread` 保留键，避免 LeadAgent 把线程状态误判为真实 SubAgent capsule；追问继承仅允许同 dataset 且上一轮有查询目标的 `followup_refine`。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_task_capsule.py -q`，22 passed、2 warnings；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_task_capsule.py tests/test_message_gateway.py -q`，26 passed、2 warnings；最终规格复审和代码质量复审均为 APPROVED。
- 残留风险：与 `tests/test_dataset_subagent.py` 联合运行时仍有两个既有 trace `display_name` 断言失败，期望中文名但当前系统已统一为原始节点名；该问题与本次 Thread Memory/TaskCapsule 改动无关，建议后续单独清理旧断言。

### 2026-06-15 19:56 · 明细追问上下文承接与 TaskCapsule 写回

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/conversation_store.py`、`datalogue-api/app/services/multiturn_context.py`、`datalogue-api/app/services/task_capsule.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_task_capsule.py`、`datalogue-api/tests/test_multiturn_context_builder.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`.codex/项目记忆.md`
- 关键改动：修复 `MultiturnContextBuilder` 对明细查询追问的错误降级，新增 `has_query_target()`，使 `detail_query` 在有 `fields/main_table/query_plan/dsl` 任一目标时都能承接上下文；QueryTaskCapsule 兜底只接受同 dataset、`followup_refine`、`base_task_ref=last_success_task` 的任务，且用 `base_question` 避免二次拼接；`_stream_chat_singleturn` 在 merge 前从 Thread Memory 构建 `query_task_capsule` 并传入 merge/initial_state；`_persist_completed_turn()` 在成功查询后自动写 `_thread.last_success_task`，下一轮无需手工 seed 即可承接。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_task_capsule.py tests/test_multiturn_context_builder.py tests/test_lead_agent_tools.py tests/test_chat.py::TestChatStreamEvents::test_message_gateway_detects_thread_last_success_task tests/test_chat.py::TestChatStreamEvents::test_stream_chat_writes_last_success_task_and_reuses_it_next_turn tests/test_chat.py::TestChatStreamEvents::test_singleturn_stream_injects_query_task_capsule_from_thread_memory -q`，63 passed、10 warnings；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_chat.py tests/test_task_capsule.py tests/test_multiturn_context_builder.py tests/test_lead_agent_tools.py -q`，164 passed、3 skipped、89 warnings；最终规格复审和代码质量复审均为 APPROVED。
- 残留风险：当前写回只在 final payload 无 `error` 且 `last_success_task` 有查询目标时生效；错误查询、澄清早退和解释轮不会覆盖上一轮成功任务。现有 `datetime.utcnow()` 弃用 warning 为既有问题，本轮未处理。

### 2026-06-16 09:59 · SubAgent 与 DSL 消费 QueryTaskCapsule

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/runner.py`、`datalogue-api/app/services/dataset_subagent.py`、`datalogue-api/app/graph/nodes.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_subagent_run.py`、`datalogue-api/tests/test_query_plan_prompting.py`、`.codex/项目记忆.md`
- 关键改动：`DatasetSubAgentRequest` 新增 `query_task_capsule` 与 `turn_event` 字段；`_stream_chat_singleturn` 构造 SubAgent request 时同步传入 QueryTaskCapsule/turn_event，与已有 initial_state 保持一致；`DatasetSubAgent.run()` 在无 chat initial_state 或 initial_state 缺字段时，会从 request 级 QueryTaskCapsule 补齐 `query_task_capsule/turn_event/original_question/question`，且 `question` 优先使用 `standalone_question`、`original_question` 保留当前用户问题，已有 initial_state 胶囊不会被 request 覆盖；QueryGraph initial_state 显式携带任务胶囊和 turn event；DSL prompt 在查询规划上下文附近追加安全的“任务胶囊”摘要，只包含 `turn_type/base_task_ref/base_main_table/standalone_question/base_question`，并递归过滤 SQL、DSL、结果行、records/data/raw 等高风险字段，对允许字段的 value 也阻断 SQL 形态、结构化结果片段和敏感 token，避免重复追加。
- 验证方式：先执行目标测试确认新增红测失败；实现后执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_subagent_run.py tests/test_query_plan_prompting.py -q`，20 passed、2 warnings；补充 value 级敏感内容过滤后执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_query_plan_prompting.py tests/test_subagent_run.py tests/test_chat.py::TestChatStreamEvents::test_stream_chat_writes_last_success_task_and_reuses_it_next_turn tests/test_chat.py::TestChatStreamEvents::test_singleturn_stream_injects_query_task_capsule_from_thread_memory -q`，23 passed、10 warnings；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_subagent_run.py tests/test_query_plan_prompting.py tests/test_chat.py -q`，125 passed、3 skipped、89 warnings。
- 残留风险：测试期间 Langfuse exporter 因沙箱网络限制无法连接 `localhost:3000`，不影响单测断言。现有 `datetime.utcnow()` 弃用 warning 为既有问题，本轮未处理。

### 2026-06-16 10:34 · Message Gateway 与 QueryTaskCapsule SSE 可观测

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_chat.py`、`datalogue-web/src/components/agent-panel.jsx`、`datalogue-web/src/assistant/chat-adapter.js`、`datalogue-web/src/assistant/thread-list-adapter.js`、`datalogue-web/src/assistant/MyMessage.jsx`、`.codex/项目记忆.md`
- 关键改动：`_stream_chat_singleturn` 在构建 `query_task_capsule` 后、进入 LangGraph/SubAgent 执行前新增 `message_gateway` SSE step，顶层和 `payload` 均携带 `turn_event` 与安全版 `query_task_capsule`，并同步写入 assistant message `step_trace`，便于前端实时面板、历史回放和调试链路直接读取；final payload、trace metadata 与 message `response_metadata` 同步加入安全版 `query_task_capsule`，过滤 `sql/raw_sql/sql_template/result_rows/records/data` 等高风险字段，避免把上一轮 query plan debug SQL 暴露到 SSE/UI；interpret/direct 等早退路径也同步写入 gateway step 和安全任务胶囊；AgentPanel 兼容 `message_gateway`/`message-gateway` 节点名，结束态 compact 面板仍单独展示 Message Gateway 结构化详情；chat adapter 与 thread-list adapter 保留 `stepTrace`，MyMessage 的“右侧面板查看”会先清空旧 trace 再回放历史 steps 和 final。`datalogue-api/app/services/observability/tracer.py` 未修改，因为现有 tracer 已记录 message-gateway span，本任务只需要补 SSE/UI/final 可见性。
- 验证方式：先新增 `test_stream_message_gateway_step_and_final_include_turn_event_and_task_capsule` 并确认 RED，失败原因是没有 `message_gateway` step；实现后执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_chat.py::TestChatStreamEvents::test_stream_message_gateway_step_and_final_include_turn_event_and_task_capsule tests/test_chat.py::TestChatStreamEvents::test_interpret_early_return_keeps_gateway_step_and_task_capsule tests/test_chat.py::TestChatStreamEvents::test_singleturn_stream_injects_query_task_capsule_from_thread_memory tests/test_chat.py::TestChatStreamEvents::test_chat_stream_direct_answer_early_return_writes_trace_index -q`，4 passed；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_chat.py tests/test_message_gateway.py tests/test_task_capsule.py -q`，133 passed、3 skipped；最终执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_message_gateway.py tests/test_task_capsule.py tests/test_multiturn_context_builder.py tests/test_lead_agent_tools.py tests/test_subagent_run.py tests/test_query_plan_prompting.py tests/test_chat.py -q`，191 passed、3 skipped；执行 `cd datalogue-web && npm run lint`，0 errors、15 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。
- 残留风险：dataset_select/clarify 早退路径继续保持只带 `turn_event`，不强行构造查询型 `query_task_capsule`，避免改变非查询事件语义；现有前端 lint warnings 和 Vite chunk size warning 为既有问题，本轮未处理。

### 2026-06-16 11:32 · 日志明细模板 fallback 与 LiteLLM 流式报告修复

- 涉及文件：`datalogue-api/app/graph/nodes.py`、`datalogue-api/app/graph/llm.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_llm_config.py`、`datalogue-web/src/components/chat-page.jsx`、`.codex/项目记忆.md`
- 关键改动：修复“查询10条用户日志”在 `planner_source=fallback` 时虽然识别到 `dataset10_log_detail` 且生成 `debug.sql_template`，但 DSL 节点只信任 `planner_source=template` 导致跳过模板、转入 LLM 后报“字段召回不足或模板未命中”的问题；新增可信 fallback SQL 模板白名单，只允许 `dataset10_log_detail` 这类确定性模板在 fallback planner 下直接进入 `direct_sql`，避免信任任意 fallback/LLM SQL；补齐 `LiteLLMChatClient.astream()`，让报告生成节点可继续使用异步流式输出，修复 SQL 已执行成功后 final 阶段报 `'LiteLLMChatClient' object has no attribute 'astream'`；前端在 `final.route_decision` 中同步 dataset 选择，覆盖数据集选择早退只发送 final、不发送单独 `route_decision` step 的路径。
- 验证方式：按 TDD 先新增失败测试，确认 fallback 模板计划仍会创建 LLM 并失败；实现后执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_chat.py::TestChatAPI::test_dsl_generate_fallback_template_plan_skips_llm tests/test_chat.py::TestChatAPI::test_dsl_generate_template_plan_skips_llm -q`，2 passed；补齐 `astream` 红测后执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_llm_config.py::test_litellm_chat_client_astream_yields_chunks tests/test_chat.py::TestChatAPI::test_dsl_generate_fallback_template_plan_skips_llm -q`，2 passed；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_llm_config.py tests/test_chat.py tests/test_subagent_query_planner.py tests/test_subagent_run.py tests/test_query_plan_prompting.py -q`，156 passed、3 skipped；执行 `cd datalogue-web && npm run lint`，0 errors、15 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过；真实 API SSE 请求 `dataset_id=10` + `查询10条用户日志` 成功返回 final，Trace ID 为 `fe03aa9c364454336dd35115e57ff8f2`，`dsl_generate` 约 7ms、`generation_mode=template`，`sql_execute` 返回 10 行。
- 残留风险：真实页面自然语言选择数据集时曾出现 `/chat/277` 与 `/api/conversation/277` 状态不一致、底部仍显示“全部数据集”的现象，本轮已补 final route_decision 同步，但页面会话绑定异常未继续扩大排查；报告生成仍需调用 LLM，本次真实 API 链路里报告节点约 36s，属于后续性能优化项，不影响模板 SQL 和 SQL 执行正确性。

### 2026-06-16 12:53 · assistant-ui 本地线程与后端会话 ID 映射修复

- 涉及文件：`datalogue-web/src/assistant/thread-list-adapter.js`、`datalogue-web/src/assistant/chat-adapter.js`、`.codex/项目记忆.md`
- 关键改动：`thread-list-adapter` 导出 `resolveRemoteId()` 和 `resolveRecentInitializedRemoteId()`，让 `chat-adapter.run()` 在发送 `/api/chat/stream` 前按 `idMap -> 最近 initialize remoteId -> URL /chat/:id -> 数字 unstable_threadId` 的顺序解析后端 `conversation_id`；新增 `datalogue:conv-resolved` 事件监听与映射回写，`lead_agent_tools.thread_context.conversation_id` 和 `final.conversation_id` 都会把实际后端会话 ID 写回 `idMap`；更新映射时清理旧 `reverseIdMap`，保留已有 `externalId`。该修复覆盖 assistant-ui 首轮 `unstable_threadId` 与 initialize local id 不一致、URL 尚未完成同步时仍会另起会话的问题。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、15 warnings；执行 `cd datalogue-web && npm run build`，通过；执行 `git diff --check`，通过。使用内置浏览器 E2E 打开 `http://localhost:5173/chat`，先发送“查询最近10条用户日志”，再点击候选“生产经营管理系统日志数据集”，最后发送“按部门汇总一下这批日志的数量”；首次 E2E 暴露 `/chat/291` 与孤儿会话 `292`、补 URL 兜底后仍暴露 `/chat/293` 与孤儿会话 `294`；最终补最近 initialize remoteId 后复测通过，只有 `conversation_id=295` 承载完整 6 条消息，第二轮 `route_decision=locked`、`dataset_id=10`、`row_count=13`、`langfuse_session=conversation-295`，页面显示“沿用上一轮已锁定的数据集和场景上下文”。
- 残留风险：真实链路中 `查询最近10条用户日志` 当前仍因 Manifest 阈值进入候选数据集选择，这是路由治理问题，不属于会话 ID 映射修复；现有前端 lint warnings 和 Vite chunk size warning 为既有问题。

### 2026-06-16 13:07 · 数据集选择后恢复上一轮原问题执行

- 涉及文件：`datalogue-api/app/api/chat.py`、`datalogue-api/app/services/conversation_store.py`、`datalogue-api/tests/test_chat.py`、`datalogue-api/tests/test_multiturn.py`、`.codex/项目记忆.md`
- 关键改动：`ConversationStore.resolve_pending_clarification()` 在数据集候选选择成功时返回 pending 中保存的 `original_question`；`_stream_chat_singleturn()` 新增 `pending_resolution` 入参，在用户本轮可见输入“选择：数据集”已落库后，把后续执行 payload 恢复为上一轮原问题并注入已选 `dataset_id`，同时清理本轮执行上下文中的 `pending_clarification`，避免 Message Gateway 继续把本轮判成 `clarification_answer`；普通无 pending 的 `dataset_select` 仍保持确认早退，不进入查询图。`_persist_completed_turn()` 写 `_thread.last_success_task` 时改用 final payload 的实际执行问题，避免把“选择：数据集”保存成可承接查询任务。
- 验证方式：执行 `datalogue-api/.venv/bin/python -m pytest datalogue-api/tests/test_multiturn.py::test_conversation_store_resolves_dataset_pending_clarification datalogue-api/tests/test_chat.py::TestChatStreamEvents::test_dataset_select_does_not_enter_workflow datalogue-api/tests/test_chat.py::TestChatStreamEvents::test_dataset_pending_selection_restores_original_question -q`，3 passed；执行 `datalogue-api/.venv/bin/python -m pytest datalogue-api/tests/test_chat.py::TestChatStreamEvents -q`，22 passed、1 skipped；执行 `datalogue-api/.venv/bin/python -m py_compile datalogue-api/app/api/chat.py datalogue-api/app/services/conversation_store.py`，通过。扩大运行 `datalogue-api/tests/test_multiturn.py datalogue-api/tests/test_chat.py::TestChatStreamEvents` 时，`TestChatStreamEvents` 通过，但 `test_stream_chat_consumes_store_active_dataset_and_prior_capsule` 暴露既有测试仍 monkeypatch 已不存在的 `app.api.chat.InProcessDatasetSubAgentRunner`，与本次修复无关。
- 残留风险：本轮修复的是“已有 pending 数据集澄清 + 用户选择候选”后的恢复执行语义；首轮为什么未自动命中“生产经营管理系统日志数据集”仍属于 Manifest 路由阈值/证据问题，未在本轮调整。

### 2026-06-16 18:14 · LeadAgent Planner 输入投影 M1 灰度接入

- 涉及文件：`datalogue-api/app/core/config.py`、`datalogue-api/app/services/lead_agent_planner_projection.py`、`datalogue-api/app/services/lead_agent.py`、`datalogue-api/tests/test_lead_agent_planner_projection.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`docs/superpowers/plans/2026-06-16-lead-agent-planner-projection-m1.md`
- 关键改动：按 M1 计划完成 LeadAgent Planner 输入投影灰度；`Settings` 新增 `LEAD_AGENT_PLANNER_USE_PROJECTION`，默认 `False`；`plan_tool_calls_with_llm` 在 LLM 不可用判定之后读取开关，开启时把 Skill Selector / Tool Planner 的人消息 payload 替换为 `build_skill_selector_input` / `build_tool_planner_input` 投影输入，新增 `_build_projection_recent_context` 把 `conversation_summary` 翻译为 `dataset_id/routing_path/prior_turns`；关闭时维持原有 `question/conversation/tool_policy/skills` 与 `tool_schemas` 结构不变；`tracer.start_generation` 与 `end_generation` 的 metadata 增加 `projection_enabled`、`projection_schema_version` 与 `projection_metrics`（含 `raw_chars/projected_chars/projection_saved_chars`），便于在 Langfuse 按字段筛选并对比灰度前后 token 体积；新增 3 个回归测试覆盖“开关关闭保持原 payload、开关开启不泄露原始大上下文字段、Langfuse metadata 记录投影启用状态和 schema version”。
- 验证方式：执行 `datalogue-api/.venv/bin/python -m py_compile datalogue-api/app/services/lead_agent.py datalogue-api/tests/test_lead_agent_tools.py`，通过；执行 `datalogue-api/.venv/bin/python -m pytest datalogue-api/tests/test_lead_agent_planner_projection.py datalogue-api/tests/test_lead_agent_tools.py datalogue-api/tests/test_lead_agent_routing.py datalogue-api/tests/test_lead_agent_merge.py -q`，52 passed；执行 `datalogue-api/.venv/bin/python -m pytest datalogue-api/tests/test_chat.py -q`，108 passed、3 skipped（与历史一致）。
- 残留风险：M1 默认关闭，仅作为灰度开关；投影后字符量指标当前按 `json.dumps` 估算，并非模型 tokenizer 的精确 token，灰度对比时需结合 Langfuse 的 `usage.total_tokens` 一起观察；`_build_projection_recent_context` 只取 `multiturn_context.last_resolved_question` / `last_question` / `inheritance_summary` 单条 prior_turn，后续若需要更复杂的多轮摘要可在 M1.1 扩展；Planner projection 仍按 `selected_skill_names[0]` 投影单 skill，下游 LLM prompt 期望的复合 Skill 编排效果需在灰度期观察。

### 2026-06-16 18:52 · LeadAgent Planner 投影审查问题修复

- 涉及文件：`datalogue-api/app/services/lead_agent_planner_projection.py`、`datalogue-api/app/services/lead_agent.py`、`datalogue-api/tests/test_lead_agent_planner_projection.py`、`datalogue-api/tests/test_lead_agent_tools.py`、`.codex/项目记忆.md`
- 关键改动：修复 Planner 输入投影在开启灰度后丢失真实 Skill `purpose` 的问题，`project_skills_for_selector()` 现在兼容 `description/purpose/summary`；新增 `recent_context.turn_policy` 精简策略摘要，保留 `intent`、`should_inherit_dataset`、`dataset_lock_source`、`explicit_dataset_locked`、`inherited_dataset_locked`，避免完整 `conversation/tool_policy` 重新进入 LLM payload 的同时，保住 prompt 需要的多轮和锁定来源判断信号；补齐真实 `available_lead_skills()` 形状和投影 payload 的回归断言。
- 验证方式：执行 `cd datalogue-api && ./.venv/bin/python -m pytest tests/test_lead_agent_planner_projection.py tests/test_lead_agent_tools.py`，34 passed、2 warnings；执行 `cd datalogue-api && ./.venv/bin/python -m py_compile app/services/lead_agent.py app/services/lead_agent_planner_projection.py tests/test_lead_agent_planner_projection.py tests/test_lead_agent_tools.py`，通过；执行 `git diff --check`，通过。
- 残留风险：`turn_policy` 仍是 M1 的最小策略摘要，不包含完整 allowed/blocked tool 列表；工具合法性当前仍主要依赖上游披露的候选工具集合和执行侧校验，后续若灰度发现模型规划出未披露工具，可单独补 `_normalize_planner_plan()` 的 disclosed tool 白名单校验。

### 2026-06-17 10:11 · 建立 Agent 上下文轻量入口

- 涉及文件：`docs/上下文入口.md`、`CLAUDE.md`、`.codex/项目记忆.md`
- 关键改动：新增项目级上下文入口文档，把 Agent 上下文分为协作规则、任务路由、项目历史、系统叙事、计划文档和 CodeGraph 代码链路六层；按 SubAgent 查询规划、多轮上下文、LeadAgent 路由、Manifest 治理、Langfuse 可观测、前端页面、系统设计等任务类型列出最小读取文档、CodeGraph 优先符号和验证口径；新增 Claude Code 专用入口，明确先读 `AGENTS.md` 和 `docs/上下文入口.md`，不要全文读取 `.codex/项目记忆.md` 或大范围源码。
- 验证方式：文档类改动；执行文件存在性检查和 `git diff --check`。
- 残留风险：该规则依赖 Agent 自觉遵守；如果后续新增大主线，需要同步维护 `docs/上下文入口.md` 的任务路由，避免上下文入口过期。

### 2026-06-17 10:14 · 清理 Agent 启动上下文冗余

- 涉及文件：`AGENTS.md`、`docs/上下文入口.md`、`CLAUDE.md`、`.codex/项目记忆.md`
- 关键改动：在 `AGENTS.md` 新增上下文预算规则，明确 `.codex/项目记忆.md` 不是启动上下文、`docs/superpowers/` 长计划只在对应任务继续实施时读取；将 `docs/上下文入口.md` 从展开式说明压缩为任务路由表和最小验证选择；将 `CLAUDE.md` 收缩为 Claude Code 的最小启动说明，删除与上下文入口重复的长说明。
- 验证方式：执行 `wc -l AGENTS.md CLAUDE.md docs/上下文入口.md .codex/项目记忆.md` 确认入口文件体积收缩；执行 `git diff --check`。
- 残留风险：历史完成记录仍保留在 `.codex/项目记忆.md`，未做删除或归档；上下文消耗的主要改善依赖后续 Agent 遵守“只检索、不全文读取”的规则。

### 2026-06-17 10:44 · LeadAgent 渐进式语义资产注入 Phase 2（过滤层与配置层）

- 涉及文件：`datalogue-api/app/core/config.py`、`datalogue-api/app/services/lead_agent.py`、`datalogue-api/app/services/lead_agent_planner_projection.py`、`datalogue-api/app/services/lead_agent_planning/__init__.py`、`datalogue-api/app/services/lead_agent_planning/asset_filter_config.py`、`datalogue-api/app/services/lead_agent_planning/asset_filter.py`、`datalogue-api/tests/test_lead_agent_planning_assets.py`
- 关键改动：在 `Settings` 新增 `LEAD_AGENT_USE_PROGRESSIVE_ASSETS` 总开关及按资产类型的 Top-K、置信度阈值、阶段 Token 预算、全局最小置信度、metadata 白名单、signals 保留数量等配置字段；新建 `lead_agent_planning` 包，实现 `AssetFilterConfig` 与 `build_filter_config`（支持 Settings -> 数据集级覆盖（兼容性占位） -> 显式覆盖的优先级合并）；实现 `filter_lead_planner_assets`，完成去重、六类资产白名单、全局/类型置信度截断、Top-K 截断、metadata 脱敏、match_signals 截断并按置信度全局降序输出；在 M1 投影层新增 `project_assets_for_lead_planner`，将过滤后资产投影为含 `assets/summary/stage/token_budget/projected_at` 的 Prompt 片段，并做 token 预算二次保险截断；在 `lead_agent.py` 的 `plan_tool_calls_with_llm` 中仅预留 Phase 3 集成 TODO 注释，不改动现有集成逻辑。
- 验证方式：执行 `cd datalogue-api && ./.venv/bin/python -m pytest tests/test_lead_agent_planning_assets.py -v`，14 passed；执行 `./.venv/bin/python -m pytest tests/test_lead_agent_planner_projection.py tests/test_subagent_candidate_assets.py tests/test_subagent_planning_contracts.py tests/test_lead_agent_tools.py -v`，74 passed；执行 `ruff check`（全局 Python 3.13 ruff）与 `black --check`（全局 black）均通过；执行 `git diff --check`，通过。
- 残留风险：数据集级覆盖配置当前读取 `SemanticDataset.planner_config`，该字段尚未在模型中固化，因此为兼容性占位，读取失败时安全降级为空覆盖；Phase 3 需将 `recall_candidate_assets -> filter_lead_planner_assets -> project_assets_for_lead_planner` 接入 `plan_tool_calls_with_llm` 的 skill_input / planner_input，并同步升级 Prompt 模板以让 LLM 利用 `candidate_assets`；当前投影的 token 估算采用 JSON 字符 / 4 的极简 heuristic，不是真实 tokenizer，上线后应结合 Langfuse token 统计做校准。

### 2026-06-17 11:16 · LeadAgent 渐进式语义资产注入 Phase 3 集成

- 涉及文件：`datalogue-api/app/services/lead_agent.py`、`datalogue-api/tests/test_lead_agent_progressive_assets_integration.py`、`datalogue-api/tests/test_subagent_candidate_assets.py`（仅 black 格式化）
- 关键改动：在 `plan_tool_calls_with_llm` 的确定性快路径之后、skill_input 之前接入 `recall_candidate_assets -> build_filter_config -> filter_lead_planner_assets -> project_assets_for_lead_planner(stage="skill_selection")`，skill 选择完成后再用同一批过滤资产以 `stage="tool_planning"` 重新投影，分别注入 `skill_input["candidate_assets"]` 与 `planner_input["candidate_assets"]`；tracer metadata 新增 `candidate_asset_recall_called`、`candidate_asset_count`、`candidate_asset_summary`；召回/过滤/投影全流程 try/except 包裹，失败时降级为空资产，不影响主链路；使用 `getattr(...)` 读取渐进式资产配置，兼容旧测试中只暴露 `LEAD_AGENT_PLANNER_USE_PROJECTION` 的 settings stub。
- 验证方式：执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_lead_agent_planning_assets.py tests/test_lead_agent_tools.py tests/test_subagent_candidate_assets.py tests/test_subagent_planning_contracts.py tests/test_lead_agent_planner_projection.py tests/test_lead_agent_progressive_assets_integration.py -q`，77 passed；执行 `ruff check` 与 `black --check` 针对改动文件，均通过；新增集成测试覆盖开关开启+锁定数据集、开关关闭、召回失败三种场景。
- 残留风险：当前 `manifest_version` / `bound_schema_version` 传 `None`，召回精度略低于 SubAgent 阶段；prompt 模板尚未升级，LLM 可能暂时忽略 `candidate_assets` 字段；SubAgent.run 内部仍会再次召回，后续需评估通过 capsule 透传避免重复 DB 召回；`build_filter_config` 直接读取 `Settings` 字段，若测试中传入 partial stub 仍可能触发 AttributeError，但生产环境使用完整 `Settings` 实例无此问题。

### 2026-06-17 11:19 · 历史对话恢复 SQL 与执行结果展示

- 涉及文件：`datalogue-web/src/assistant/thread-list-adapter.js`、`.codex/项目记忆.md`
- 关键改动：修复历史对话加载时只恢复 `stepTrace/routePayload/queryProfile`、没有恢复实时消息 `metadata.custom.sql` 和 `metadata.custom.sqlResult` 的问题；新增 `buildHistoryMessageCustom()`，将后端落库的 `response_metadata.sql/sql_result/sql_diagnosis/sql_audit_result/query_plan/candidate_assets/clarification/langfuse/observability/feedback` 等字段映射回 `AIMessage` 已使用的 custom 契约；对较旧历史数据增加 `step_trace` 兜底，从最后一个 `dsl_compiler` step 恢复 SQL，并在 `sql_execute.rows` 为数组时恢复结果表。
- 验证方式：执行 `cd datalogue-web && npm run lint`，0 errors、15 warnings（均为既有 warning）；执行 `cd datalogue-web && npm run build`，通过，仅保留既有 Vite chunk size warning；执行 `git diff --check`，通过。尝试用 Node 直接导入 `thread-list-adapter.js` 做轻量断言时，被 Vite 源码中的无扩展名 `../api/client` ESM import 解析限制阻塞，未作为业务失败处理。
- 残留风险：历史消息若只保存了旧版 `step_trace` 且 `sql_execute` step 只有行数没有完整 rows，前端无法反推出执行结果明细；完整表格仍依赖后端已落库的 `response_metadata.sql_result`。

### 2026-06-17 10:44 · 蓝图步骤结构化视图 T1

- 涉及文件：`datalogue-web/src/components/analysis-blueprints.jsx`、`datalogue-web/src/components/blueprint-step-view.jsx`、`datalogue-web/tests/unit/components/analysis-blueprints-step-card.test.jsx`、`datalogue-web/src/styles.css`
- 关键改动：采用方案 B，把 `BlueprintStepView` 和 `StepCard` 提取到独立文件 `blueprint-step-view.jsx`；删除 `analysis-blueprints.jsx` 中内联的同名组件定义，消除标识符冲突，保留 `import { BlueprintStepView } from './blueprint-step-view'`；统一 JSON 格式化函数名为 `formatJson`；更新单元测试，改为从生产组件导入并新增字符串步骤的 `StepCard` 独立用例；在 `styles.css` 中补充 `.blueprint-step-view`、`.step-card-header`、`.step-card-body`、`.step-section`、`.step-section-label`、`.step-rule-list`、`.step-output-tags`、`.step-output-tag`、`.step-confidence-bar`、`.step-confidence-fill`、`.step-confidence-text`、`.step-card-footer`、`.step-json-toggle`、`.step-json-block` 等样式，保证视觉布局正确。
- 验证方式：执行 `cd datalogue-web && npm run lint`，无新增错误；执行 `npm run build`，构建通过；执行 `npm test`，11 个测试全部通过。
- 残留风险：当前 `StepCard` 对字符串步骤仍采用最简单的垂直布局，若后续需要与对象步骤一致的头/身体/脚结构可再调整；T1 项目记忆原本记录在 worktree 的 gitignored 文件中，本次已同步到主仓库。

### 2026-06-17 12:39 · last_success_task 最小承接快照与跨轮状态瘦身

- 涉及文件：`datalogue-api/app/services/multiturn/__init__.py`、`datalogue-api/app/services/multiturn/last_success_task.py`、`datalogue-api/app/services/task_capsule.py`、`datalogue-api/app/api/chat.py`、`datalogue-api/tests/test_task_capsule.py`、`datalogue-api/tests/test_message_gateway.py`、`datalogue-api/tests/test_multiturn_context_builder.py`、`datalogue-api/tests/test_chat.py`、`TODOS.md`、`.codex/项目记忆.md`
- 关键改动：新增 `LastSuccessTask` v1 严格白名单 schema，跨轮只持久化字段引用、去重 join、schema/manifest 版本、SQL hash 和轻量 result_digest，不再保存完整 `query_plan/dsl/sql/sample_rows/selected_assets/rejected_assets/debug`；`build_query_task_capsule()` 只在 v1 capsule 校验通过且 schema/manifest 未漂移时注入最小 `base_query_plan`，并输出 `inheritance_status`；`chat.py` 在 Message Gateway 前读取 current manifest 版本并评估 `_thread.last_success_task`，schema drift、legacy、corrupt 状态不会启用 followup_refine；`_persist_completed_turn()` 在 size guard 或构造失败时跳过 `last_success_task` 写入但仍清理 `active_task`，不让本轮成功查询失败；将 artifact store、报告产物化记录到 `TODOS.md` 作为独立后续 PR。
- 验证方式：执行 `datalogue-api/.venv/bin/python -m py_compile datalogue-api/app/services/multiturn/last_success_task.py datalogue-api/app/services/task_capsule.py datalogue-api/app/api/chat.py`，通过；执行 `cd datalogue-api && .venv/bin/python -m pytest tests/test_task_capsule.py tests/test_message_gateway.py tests/test_multiturn_context_builder.py tests/test_chat.py tests/test_subagent_run.py tests/test_query_plan_prompting.py -q`，177 passed、3 skipped、93 warnings。
- 残留风险：若当前数据集没有 current manifest，入口校验无法比较 schema/manifest 版本，会按 v1 capsule 自身合法性允许承接；artifact store、完整报告和结果产物化、`source: inherited` 的筛选来源链路已明确不属于本 PR，后续独立处理。
