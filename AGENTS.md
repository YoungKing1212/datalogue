# AGENTS.md

## 项目沟通

- 用户：杨凯（Ken Yang）；默认使用中文回复。
- 当前项目：数语（智能问数）。
- Java 任务按需使用 `jdk8` 或 `jdk17`，执行前确认项目所需版本。

## 默认执行方式

- 默认采用快速路径：只读取与任务直接相关的文件，做聚焦修改和最小充分验证，不进行无关扫描、重构或长篇汇报。
- 需求明确时直接实施；只有关键歧义会显著改变结果或产生高风险副作用时才询问用户。
- 仅在用户明确要求，或任务涉及安全边界、数据迁移、跨模块架构、生产故障、依赖升级时，扩大分析和验证范围。
- 工具或推荐命令不可用时，快速切换到可靠的替代方式，不反复尝试同一路径。

## 代码修改

- 修改前阅读直接相关上下文，沿用现有结构和风格；保持改动范围聚焦，不回滚用户或其他工具的已有改动。
- 注释用于解释不直观的业务意图、边界和风险；不强制逐行注释，也不为显而易见的调用、赋值或分支补注释。
- 新文件写清用途和关键职责即可，优先沿用所在模块的文件头或文档字符串风格，不强制统一大段模板。
- 不要求为实现过程临时创建 `TODO`；若确实留下待办，必须说明原因，并在完成后清理。

## 验证策略

- 默认执行与改动最相关的测试、类型检查或 lint；能用单文件、单模块验证时，不运行全量检查。
- 前端只有在改动影响公共配置、依赖、构建链、跨模块接口，或用户要求交付级验证时，才同时执行完整 `npm run lint` 和 `npm run build`。
- dev server、页面验收、真实链路回放和端到端测试只在任务本身需要时运行。
- 无法执行某项验证时，如实说明未验证内容和风险，不为追求形式上的完整而阻塞交付。

## 项目记忆

- [.codex/project-memory.md](.codex/project-memory.md) 只记录会影响后续会话的重要架构决策、需求变更、迁移事项、运行方式变化或未完成交接。
- 小范围修复、样式调整、测试补充、普通文档与规则调整、纯分析和代码审查默认不记录。
- 需要记录时保持简短，包含时间、事项、涉及范围、验证和残留风险；只按关键词读取相关段落，不默认全文读取。

## Claude Code 代码审查 Hook

- 默认不触发 Claude Code 审查。仅当用户明确要求，或改动涉及安全、权限、数据迁移、关键生产链路及较大范围跨模块重构时，才在完成实现和验证后附加 `<!-- CLAUDE_REVIEW_REQUIRED -->`。
- 纯问答、分析、文档或规则调整、小范围低风险修改不触发审查。
- Hook 返回问题时只处理成立且与本次任务相关的问题；调用异常时说明原因，不循环触发。

## 上下文与工具

- 仓库存在 `.codegraph/` 时优先尝试 CodeGraph；若当前版本缺少所需命令或结果不足，立即改用 `rg`、`sed` 等只读工具，不初始化或修复索引。
- 需要任务路由时再读 [docs/上下文入口.md](docs/上下文入口.md)；`docs/superpowers/` 只在继续对应计划时读取。

## AgentScope 2.0.3

- 只有新增或升级 Agent、RAG、工具、工作流、上下文、中间件、权限等框架集成，或对 API 用法不确定时，才查阅 `~/code_place/study/agentscope-docs/` 中直接相关章节。
- 普通业务修复、已有模式内的小改动无需重复查文档；需要实现框架能力时优先使用 AgentScope 原生 API。
- 修改现有 assistant-ui 前端时优先沿用项目内已有 runtime、Thread 和 transport 模式；只有用法不确定或升级依赖时才查外部文档。
