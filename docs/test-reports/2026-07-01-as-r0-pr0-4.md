# AS-R0 PR0.4 测试报告：安全测试矩阵

## 结论

- 状态：通过
- 范围：PR0.4 安全测试矩阵
- 日期：2026-07-01

## 本次实现

- 新增 `tests/test_as_r0_security_matrix.py`，集中覆盖：
  - Agentic Shell context projection。
  - BI atomic tool response。
  - SSE 用户可见 payload 和 trace_only 随流 payload。
  - AgentScope mirror session metadata / event payload。
  - Workbench View Model。

- 统一禁露字段：
  - `raw_rows`
  - `repair_patch`
  - `patch_body`
  - `blueprint_body`
  - 对应 camelCase / 归一形式

- SSE 公开兼容层移除内部 `node/display_name`，避免 `query_plan` 等内部节点名进入浏览器可见 payload。

## TDD 记录

- RED：
  - SSE payload 仍暴露 `raw_rows`。
  - AgentScope mirror 不拒绝 `blueprint_body` / `repair_patch` 主体。
  - Workbench View Model 对 DB 中已有污染 payload 未 fail-closed。
  - trace_only SSE 顶层保留 `node: query_plan`。

- GREEN：
  - 补齐 SSE blocked key 集合。
  - 补齐 mirror forbidden fragments。
  - 补齐 Workbench View Model forbidden output keys。
  - `_with_event_envelope()` 顶层公开 payload 同步移除 `node/display_name`。

## 验收命令

```bash
cd datalogue-api && python3 -m pytest tests/test_as_r0_security_matrix.py tests/test_event_envelope.py -q
```

结果：

```text
11 passed, 2 warnings
```

扩大回归：

```bash
cd datalogue-api && python3 -m pytest tests/test_as_r0_security_matrix.py tests/test_event_envelope.py tests/test_agentic_shell_contract.py tests/test_agentscope_runtime_driver_contract.py tests/test_agentscope_chat_bridge.py tests/test_agentscope_mirror_models.py tests/test_workbench_view_api.py tests/test_c3_workbench_acceptance.py -q
```

结果：

```text
61 passed, 12 warnings
```

静态验证：

```bash
python3 -m py_compile datalogue-api/app/api/chat.py datalogue-api/app/services/agentscope_mirror.py datalogue-api/app/services/workbench_view_model.py datalogue-api/tests/test_as_r0_security_matrix.py
git diff --check
```

## 残留风险

- PR0.4 不替换 `/chat/stream` 主链。
- PR0.4 不接真实 AgentScope runner。
- P1 进入 runtime adapter 后，需要继续沿用本矩阵扫描新 Shell runtime 的 final payload、artifact refs 和 trace contract。
