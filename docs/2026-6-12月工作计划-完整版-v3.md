# 数语（Datalogue）2026 年 6–12 月工作计划（完整版 v3）

> 制定日期：2026-07-13（v3：本体建模提升至 P0，P0 延长至 10/17）
> 拆分粒度：双周 Sprint（约 10 个工作日）
> 团队规模：3 人（后端 / 前端 / AI Agent 域）
> 覆盖范围：2026-06-02 至 2026-12-26，共 15 个 Sprint
> 状态：✅ 已完成 ｜ 🔄 进行中 ｜ ⏳ 待启动

---

## 一、项目当前状态（截至 2026-07-13）

### 1.1 已交付的核心能力

| 模块 | 状态 | 说明 |
|------|------|------|
| AgentScope 主链 | ✅ 稳定 | Agent Team API (`/api/agent-team/tasks/stream`) 为统一入口；Leader + BI Worker 双 Agent；mirror 四表 |
| BI Worker 查询链路 | ✅ 稳定 | QueryPlan 契约 → DSL 编译 → SQL 执行 → artifact 闭环；L0–L5 渐进式上下文；RepairPlan + RepairPatch |
| Workbench 工作台 | ✅ 稳定 | 右侧 Panel + 隐藏路由；artifact 详情、timeline、受控 retry |
| #24 数据展示分页 | ✅ 上线 | 上限 10000 行；前端 DataTable 每页 100 行 |
| 多轮对话/会话恢复 | ✅ 可用 | 懒创建、上下文槽位、历史回放 `/chat/{id}` |
| 安全基线 | ✅ 落地 | 用户可见层禁止 SQL/schema/raw rows/query_plan |
| 认证体系 | ✅ 上线 | 登录、用户管理、密码策略、base64 传输加密 |
| LLM 配置层 | ✅ 完成 | LiteLLM → AgentScope credential 原生管理 |
| 前端目录治理 | ✅ 完成 | `features/chat`、`shared`、`assistant-ui` 边界 |
| 后端测试 496 个 | ✅ | 前端测试 ~190 个 |

### 1.2 已知待办与债务

- **BI Tools 渲染**：Tool UI 和多智能体可视化未上线
- **子智能体频繁创建/删除**：每次问数都走 AgentCreate → TeamDelete
- **上下文膨胀**：多轮对话无压缩机制
- **表关系靠 LLM 猜测 join key**：无知识图谱驱动
- **Report Worker 未激活**：只完成了骨架和 prompt，未真正接入主链
- **预编译 Query-Plan 未做**：重复问题每次都走完整 LLM→Compile
- **字段脱敏未做**：安全只有输出层阻断，无配置化脱敏
- **本体建模未做**：术语/指标/维度分散，无法系统性地消歧业务概念

---

## 二、优先级体系

```
P0（主链稳固 + 核心功能完善，7–10 月中旬，7 个 Sprint，S4–S10）
  四条主线并行推进：

  A. 主链 UI 与链路稳定
     - BI Tools 渲染 / Multi-Agent 可视化
     - 子智能体会话级保留
     - 多轮快速筛选优化
     - AgentScope 原生化观测 / OTel
     - 上下文压缩

  B. 查询链路深度加固
     - 表关系维护 / 知识图谱
     - 预编译 Query-Plan
     - BI Worker 失败类收敛
     - Report Worker 节点稳固

  C. 业务语义基座
     - 本体建模（术语/指标/维度 → 业务本体 → 驱动消歧）

  D. 安全与数据治理基座
     - 字段级数据脱敏
     - 权限审计
     - API 限流
     - 数据库连接池
     - 认证与管理后台补强

P1（能力扩展，10 月下旬–11 月，2 个 Sprint，S11–S12）
  数据导出 / Python+Bash Worker / 接口发布 / 监控告警 / 性能基线

P2（平台化，12 月，3 个 Sprint，S13–S15）
  多租户 / 数据源定制平台 / SSO / 开放 API / 交付材料 / 收尾
```

---

## 三、用户需求 → Sprint 映射总表

| # | 用户需求 | 映射 Sprint | 优先级 |
|---|---------|------------|--------|
| 1 | BI Tools 渲染 | S4–S6 | P0 |
| 2 | 多轮对话快速筛选优化 | S6 | P0 |
| 3 | 子智能体会话级保留 | S4–S5 | P0 |
| 4 | 多租户 | S13–S15 | P2 |
| 5 | 字段级数据脱敏 | S8 | P0 |
| 6 | AgentScope 原生化、观测与运维 | S3–S5 + S12 | P0 |
| 7 | 认证、权限与管理后台 | S8 + S14 | P0 |
| 8 | 数据源定制平台 | S13–S14 | P2 |
| 9 | Assistant-UI 聊天体验与多智能体可视化 | S4–S7 | P0 |
| 10 | 表关系维护（知识图谱） | S6–S8 | P0 |
| 11 | OTel 链路观测 | S6–S7 + S12 | P0 |
| 12 | Report/Bash/Python Worker | S7 + S11 | P0→P1 |
| 13 | 接口发布 | S11–S12 | P1 |
| 14 | 权限审计 | S8 | P0 |
| 15 | 三方登录 SSO | S13–S14 | P2 |
| 16 | 开放智能体能力暴露 | S14 | P2 |
| 17 | 本体建模 | S9 | P0 |
| 18 | 预编译 Query-Plan | S7–S8 | P0 |
| 19 | 上下文压缩 | S5–S6 | P0 |
| 20 | 数据库连接池 | S10 | P0 |
| 21 | API 限流 | S10 | P0 |
| 22 | 数据导出 | S11 | P1 |
| 23 | 监控体系补全 | S12–S13 | P1 |
| 24 | 数据展示分页问题 | S3 ✅ | — |

---

## 四、双周 Sprint 计划（15 个 Sprint，6/02–12/26）

---

### Sprint 1：6/02 – 6/13 ✅ 「基础工程与问数链路打通」

**目标**：建立工程规范，打通问数基础链路。

| 交付项 | 说明 |
|--------|------|
| 工程规范 | 项目记忆、Python 文件头模板、AGENTS.md、编码规范（作者: yangkai / KenYang） |
| 问数入口 | 蓝图分析、NL2DSL、语义资产解析、术语归一化、数据集上下文服务 |
| SQL 安全 | SQL 只读校验、SQL 自动修复 Retry Trace、回答解释包与可信度气泡 |
| 基础观测 | LiteLLM 配置化、Langfuse 本地观测（Trace、Prompt Registry）、查询审计 Trace 页面 |
| SubAgent 骨架 | Dataset SubAgent Manifest、LeadAgent Planner / 控制面工具、渐进式披露 |
| 数据模型 | 语义数据集、指标、维度、术语、蓝图、数据源模型建立 |

**Git 关键提交**：`55c7e3f6` 至 `3e0db99d`，约 50 个提交。

**验收标准**：后端 pytest 通过；`docker compose up` 可启动；前端 dev server 可访问。

---

### Sprint 2：6/16 – 6/27 ✅ 「多轮对话与 Workbench 上线」

**目标**：SubAgent 规划收口、多轮状态管理、Workbench 工作台交付。

| 交付项 | 说明 |
|--------|------|
| SubAgent v2 | 查询规划层 v2、语义资产渐进式披露、DSL 提示词、`DatasetRouter` 候选匹配 |
| Thread Memory | `QueryTaskCapsule`、`ConversationState`、多轮槽位承接、多轮 refinement slots |
| C1 RepairPlan | 字段失败诊断 (`classify_sql_failure`)、RepairPlan 契约、事件、Artifact refs |
| C2 RepairPatch | 离线 Patch Engine、字段候选 (`blueprint_join`)、Tool validation、主链接入 |
| C3-P0 Workbench | mirror 四表 (`agentscope_session/message/event/ref`)、Chat Bridge、View Model API、Controlled Retry、Chat 右侧 Panel |
| B-first C-ready 核心链路 | BI_SOUL 契约同步、Capability Router 路由收窄、QueryGraph Compiler、SubAgent ToolAdapter、统一 Event Envelope、ArtifactCard |

**Git 关键提交**：`2f267d2e` 至 `5b46a945`，约 40 个提交。

**验收标准**：真实问题"查询杨凯 2024 年工作日志"成功问数并显示结果时间线；Workbench 展示 timeline + artifact；历史会话可恢复。

---

### Sprint 3：6/30 – 7/11 ✅ 「治理收口与 AgentScope 主链迁移」

**目标**：Langfuse 移除、目录治理、AgentScope Agent Team 主链建立、BI Worker 渐进式上下文链路。

| 交付项 | 说明 |
|--------|------|
| Langfuse 移除 | 删除依赖、Docker 服务、API 挂载、前端 Trace 入口；改为本地日志 |
| 目录治理 Phase A/B/C/D/E/F | facade-first 策略；`graph/` → `core/`、`agents/` → `domains/bi/agent/`、`agentscope_service/` → `runtime/engine/`、`services/` → `domains/`、残余名去前缀 |
| AgentScope Agent Team 主链 | `/api/agent-team/tasks/stream` SSE 统一入口；Leader Agent + BI Worker 双 Agent；AgentScope Service 子应用挂载 |
| BI Worker L0-L5 渐进式 | L0 候选数据集 / L1 资产匹配 / L2 Schema 切片 / L3 值域 / L4 校验 / L5 受控执行；`BIWorkerQueryPlan` v1 契约；失败类收敛（FIELD_NOT_FOUND 死循环修复） |
| LLM 配置层迁移 | LiteLLM → AgentScope credential 原生管理；模型选择器接 AgentScope ModelCard；credential 字段持久化 |
| Report Worker 骨架 | Leader Prompt 强制触发规则；worker 注册表收敛为 `bi` + `report` 两类 |
| 前端体验修复 | 左侧功能栏真实数量、历史回放、chat 测试迁移 |
| 产物分页（#24） | 上限提升至 10000 行；前端 DataTable 每页 100 行 |

**Git 关键提交**：`5b46a945` 至 `49c7a968`，约 150 个提交（含 AS-R0 P0-P2 全套迁移、Phase B-F 目录治理、AgentScope 2.0.3 主链、BI Worker 渐进式上下文）。

**验收标准**：前端 190+ passed；后端关键子集 189 passed；`/chat` 可完成候选数据集确认 → 查询 → artifact 闭环。

---

### Sprint 4：7/14 – 7/25 🔄 P0 「UI 体验闭环 + 子智能体设计」

**一句话目标**：Chat 主路径完整承接 BI Tools 渲染与 Multi-Agent 可视化，子智能体保留机制设计落地。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **BI Tools 渲染（Tool UI）** | Chat 消息中展示工具运行中/成功/失败/artifact refs/安全摘要/耗时；Streamdown 流式状态接续 | 前端 | 5 |
| **Multi-Agent 可视化初版** | nested messages 展示 Leader/Worker 协作过程；reasoning 摘要只含安全业务信息，raw thinking 仅 debug gate 下可见 | 前端 | 4 |
| **子智能体会话级保留机制设计** | 同一 session 内 BI Worker 不随单轮任务结束而删除；设计 session-scoped worker pool；分析 AgentScope 2.0.3 的 session 生命周期与 agent 绑定关系 | AI + 后端 | 4 |
| **上下文压缩技术方案** | 基于 DeerFlow 三层中间件参考，制定 Datalogue 多轮上下文压缩方案（摘要阈值、技能包留存、工具输出外部化）；评估 AgentScope 中间件钩子可行性 | AI 域 | 3 |
| **Workbench 去常驻化收尾** | 普通 `/chat` 不挂 WorkbenchPanel；artifact 详情/失败态/retry 入口迁到 Chat 内 | 前端 | 3 |

**验收标准**：Chat 中可见工具执行过程卡片 + artifact 结果卡 + 推理摘要；子智能体保留方案完成评审；Workbench 不再常驻。

---

### Sprint 5：7/28 – 8/08 ⏳ P0 「主链加固 + 观测基线」

**一句话目标**：子智能体 session pool 落地，AgentScope 全链路可观测基线建立。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **子智能体 session-scoped pooling 落地** | 同一会话 session 内复用 BI Worker agent_id；空闲 TTL 到期释放；避免频繁 AgentCreate → TeamDelete；降级方案：Redis 缓存 worker agent_id | 后端 + AI | 5 |
| **AgentScope 观测基线** | task_id/trace_id 贯穿 Leader session → BI Worker → 工具调用 → artifact → 最终 answer；覆盖四个关键节点：session 创建、AgentCreate、工具执行、artifact 写入 | 后端 | 4 |
| **BI Worker 失败类收敛** | 针对 FIELD_NOT_FOUND/FILTER_MISSING 建立回归用例；校验 field_refs 自动兜底误报率 | AI 域 | 3 |
| **Chat 页失败态 + retry 闭环** | 失败诊断卡片（safe diagnosis + recommended action）；retry 按钮触发重试，保留原 artifact refs | 前端 | 3 |

**验收标准**：同一会话连续 3 轮问数不产生新 AgentCreate；task_id 可在日志中追踪全链路；代表性业务问题误报率下降。

---

### Sprint 6：8/11 – 8/22 ⏳ P0 「上下文压缩 + OTel + 知识图谱启动」

**一句话目标**：多轮对话上下文不爆、OTel 链路落地、表关系知识图谱初版启动。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **上下文压缩功能上线** | 多轮超过阈值自动触发历史压缩；保留技能包与关键 artifact_refs；摘要前 hook 写入长期存储 | 后端 + AI | 5 |
| **多轮快速筛选优化** | 已有 SQL 骨架时后续轮次跳过完整 QueryPlan 生成，只做 filter/limit 参数替换；深化 MULTITURN_BLUEPRINT_SHORTCUT | AI 域 | 3 |
| **OTel 链路观测补全** | Span 覆盖：task 创建 → session → 工具执行 → SQL 编译 → DB 执行 → artifact 写入；exporter 默认关闭，排障时按窗口打开 | 后端 | 3 |
| **Tool UI 完善 + Mermaid/ECharts** | Chat 消息支持 Mermaid 流程图和 ECharts 纯 JSON 图表渲染；图表失败时降级为数据表格 | 前端 | 4 |
| **表关系知识图谱启动** | 从 `analysis_blueprint.call_template` 和 `join_requirements` 自动提取表间关系；数据集管理页展示 ER 图（Mermaid）；手动补录 join key 入口 | 后端 + AI | 3 |

**验收标准**：12 轮以上上下文 ≤ 128K 80%；task_id 串起 5 层 span；已有 SQL 快速筛选不重建 QueryPlan；数据集管理页可见 ER 图初版。

---

### Sprint 7：8/25 – 9/05 ⏳ P0 「Report Worker 稳固 + 预编译 Query-Plan」

**一句话目标**：Report Worker 激活进入灰度，预编译 Query-Plan POC 落地，知识图谱深化。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **Report Worker 激活并稳固** | Worker 注册表增加 "report" 类型；Leader 根据用户意图和结果复杂度自动判断是否派生；报告合并入聊天 Markdown，不做独立面板；失败不影响 artifact 展示 | AI 域 | 4 |
| **预编译 Query-Plan POC** | 高频重复问题缓存已编译 QueryPlan + SQL；命中缓存跳过 LLM→Plan→Compile 链路；POC 覆盖 5 个场景 | AI 域 | 3 |
| **表关系知识图谱深化** | 跨 schema 关系提取；知识图谱驱动自动 join（替代 LLM 猜测 join 条件）；在 SQL 编译器中消费 knowledge_graph refs | 后端 | 4 |
| **Chat 多轮上下文体验优化** | 多轮快速筛选的前端交互：筛选条件可视化编辑（不改 SQL 只改 filter value）、历史 artifact 快速引用 | 前端 | 3 |
| **OTel 观测面板（内部）** | task_id 查询 → 全链路 span 可视化；耗时分段展示；失败节点高亮 | 后端 | 2 |

**验收标准**：含"报告/分析/总结"关键词自动生成 Markdown 报告；5 个预编译场景缓存命中跳过 LLM；跨表 JOIN 可通过知识图谱自动确定 join key。

---

### Sprint 8：9/08 – 9/19 ⏳ P0 「安全基座：脱敏 + 权限 + 知识图谱上线」

**一句话目标**：字段脱敏全链路覆盖、权限审计可用、管理后台安全管控落地、知识图谱上线。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **字段级数据脱敏** | 手机号/身份证/银行卡/邮箱按配置化规则脱敏展示；`query_constraints` JSON 扩展脱敏规则；Chat/Workbench/导出/artifact API 全部生效 | 后端 | 5 |
| **权限审计** | 记录谁、何时、查询了什么数据集、SQL 指纹、返回行数、脱敏命中；审计日志保留 ≥ 90 天，支持按用户/数据集/时间查询 | 后端 | 3 |
| **认证与管理后台补强** | 管理员鉴权覆盖：用户创建/数据源管理/数据集发布/模型凭证；用户角色（管理员/数据管理员/业务用户）；管理后台页面：用户管理、模型凭证、数据源、数据集发布、系统健康 | 后端 + 前端 | 5 |
| **预编译 Query-Plan 扩展** | POC 扩展到 20 个场景；Query-Plan 指纹计算（问题语义 + 数据集 + 蓝图）；缓存 TTL 按数据新鲜度可配 | AI 域 | 2 |
| **表关系知识图谱上线** | 知识图谱在数据集管理页完全可用；手动补录 join key 功能完整；与蓝图版本联动更新；SQL 编译器消费 knowledge_graph refs 投入生产 | 后端 + 前端 | 3 |

**验收标准**：敏感字段在 Chat/导出/Workbench/artifact API 均脱敏；非管理员不能调用管理接口；知识图谱可交互编辑，驱动生产 JOIN 路径。

---

### Sprint 9：9/22 – 10/03 ⏳ P0 「业务语义基座：本体建模」

**一句话目标**：基于已上线的知识图谱和语义资产，建立业务本体，驱动问题理解的实体识别→概念映射→口径消歧。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **本体模型设计与契约** | 定义 Datalogue 本体三层结构：实体层（business_term → 业务对象分类）、概念层（metric/dimension → 口径语义）、关系层（知识图谱 refs → 业务关联）；本体 JSON 化存储契约；与现有 `business_term`/`semantic_metric`/`semantic_dimension` 的映射关系 | AI 域 | 3 |
| **本体驱动的 NLU 增强** | 在 BI Worker L0 候选数据集匹配阶段引入本体映射：用户问题实体识别 → 本体概念映射 → 口径消歧（如"销售额"通过本体匹配到"GMV"或"实付金额"，而非 LLM 猜测）；补 10 条以上常见业务概念消歧规则 | AI 域 | 4 |
| **本体管理页面** | 数据集管理页新增"业务本体"Tab：本体实体 CRUD、概念树可视化（Mermaid）、与术语/指标/维度的双向绑定、消歧规则编辑 | 前端 | 4 |
| **本体与知识图谱双向同步** | 本体中定义的业务关系自动写入知识图谱 join_key 候选；知识图谱中新发现的表关系可一键提升为本体概念关联 | 后端 + AI | 3 |
| **本体导入导出** | 支持 JSON/YAML 格式导入导出本体模型；模板导出供新数据集复用 | 后端 | 2 |

**验收标准**：
- 本体模型可消歧至少 5 类常见业务概念混淆（如"金额"→订单金额/实付金额/退款金额，"活跃用户"→DAU/MAU/周活）
- BI Worker 候选数据集匹配不再仅靠关键词+LLM 猜测，本体映射参与消歧
- 本体管理页面可 CRUD 实体/概念/关系，Mermaid 可视化
- 本体与知识图谱关系同步生效（双向）

---

### Sprint 10：10/06 – 10/17 ⏳ P0 「性能基座 + P0 全面验收」

**一句话目标**：数据库连接池加固、API 限流上线、P0 全部交付验收。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **数据库连接池优化** | SQLAlchemy pool_size/max_overflow 按负载调优；连接空闲回收；慢查询日志（> 5s 告警）；连接泄漏检测 | 后端 | 3 |
| **API 限流上线** | FastAPI middleware：按用户/IP 限流（每分钟查询次数、每天次数）；超限 429 + 可读提示；配置化阈值 | 后端 | 2 |
| **BI Worker 回归 + 20 条 E2E** | 建立 20 条代表性中文业务问题的可重复 E2E 套件；每类失败有明确 failure_type + 安全摘要 + 推荐下一步；至少 5 条本体消歧场景 | AI 域 | 4 |
| **P0 阶段性验收** | Chat 完整链路：登录→问数→Tool UI→artifact→本体消歧→报告→失败恢复→retry；全部安全边界验证（无 SQL/schema/raw rows 泄露） | 全体 | 3 |
| **前端 Chat 体验打磨** | Tool UI 动画、artifact 卡片样式、报告 Markdown 排版、Mermaid/ECharts 响应式、本体概念高亮 | 前端 | 3 |

**P0 里程碑验收标准**：
- Chat UI 完整闭环：Tool UI + Multi-Agent 可视化 + 报告 + 失败 recovery
- 同一会话内子 Agent 复用，不频繁创建/删除
- 12 轮对话上下文不爆；已有 SQL 快速筛选不重建 QueryPlan
- 跨表 JOIN 由知识图谱自动确定 join key
- 预编译 Query-Plan 20 个场景缓存可用
- Report Worker 激活，报告失败不影响 artifact
- 业务本体驱动至少 5 类概念消歧，本体管理页面可用
- 字段脱敏全链路覆盖；权限审计可查；API 限流生效
- 数据库连接池无泄漏、慢查询可发现
- 20 条业务问题 E2E 可重复（含本体消歧场景）；task_id 可追踪全链路
- 无 SQL/schema/raw rows 泄露
- 后端 `pytest tests/` 核心子集全绿；前端 `npm run lint` + `npm run build` 通过

---

### Sprint 11：10/20 – 10/31 ⏳ P1 「能力扩展一：导出 + Worker + 接口」

**一句话目标**：数据导出上线、Python/Bash Worker 激活、接口发布。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **数据导出（Excel/CSV）** | artifact 支持 Excel 导出（后端 pyarrow/openpyxl）；前端下载按钮；10 万行内 30 秒完成；脱敏字段导出同样脱敏 | 后端 + 前端 | 5 |
| **Python/Bash Worker 激活** | Python Worker（pandas 数据分析 + matplotlib 图表）；Bash Worker 仅内部运维使用；AgentScope 沙箱安全边界评估 | AI 域 | 3 |
| **接口发布完善** | OpenAPI/Swagger 完整注解；示例请求/响应；API 版本管理策略；使用示例（Python SDK / curl） | 后端 | 3 |
| **前端性能优化** | 大 artifact 虚拟滚动；chat 消息列表懒加载；骨架屏；Vite chunk 拆分（lazy loading） | 前端 | 3 |

**验收标准**：artifact 详情页可下载 Excel/CSV，内容与页面一致（含脱敏）；Python Worker 可执行数据分析任务；API 文档覆盖率 ≥ 90%。

---

### Sprint 12：10/20 – 10/31 → 11/03 – 11/14 ⏳ P1 「监控告警 + 性能基线」

**一句话目标**：Prometheus/Grafana 告警体系就绪、全链路性能基线确立。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **Prometheus/Grafana 告警上线** | Dashboard（QPS/延迟/错误率/Token/成功率/Worker 并发）；告警规则：查询失败率 > 5%、P95 > 30s、LLM 调用连续失败 → 飞书通知 | 后端 | 4 |
| **业务指标补全** | 查询成功率、LLM Token 消耗趋势、P50/P95/P99 响应时间、Worker 并发数、artifact 生成量、本体命中率 | 后端 | 3 |
| **全链路性能基线** | 首 token / 候选数据集 / 本体映射 / QueryPlan / SQL 编译 / DB 执行 / artifact / 最终回答 分段耗时报告；压测 50 并发 | 后端 | 3 |
| **前端 E2E 自动化** | Playwright：登录→问数→本体消歧→artifact→详情→retry→报告；核心场景每天自动跑 | 前端 | 2 |
| **管理后台完善** | 数据集发布审批流；模型凭证管理 UX 优化；系统健康 Dashboard 嵌入管理后台 | 前端 | 3 |

**验收标准**：Grafana 可查看过去 7 天核心指标；告警通知可收到；性能基线报告包含分段耗时（含本体映射耗时）。

---

### Sprint 13：11/17 – 11/28 ⏳ P1→P2 「平台化过渡：方案 + 设计」

**一句话目标**：OTel 全链路收尾，数据源定制平台、多租户和 SSO 技术方案完成。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **OTel 全链路观测完成** | 8 层 span（task → session → AgentCreate → tool → compile → execute → artifact → answer）完整覆盖；trace_id 贯穿后端日志 + AgentScope span + 前端请求 | 后端 | 3 |
| **数据源定制平台技术方案** | REST API 数据源接入方案（无需 JDBC/ODBC）；schema 自动发现；自定义 SQL 查询执行器隔离；与现有 Datasource adapter 架构共存 | 后端 | 3 |
| **多租户技术方案** | 租户边界定义（数据源/数据集/用户/会话/artifact/本体 隔离）；数据库层面方案评估（tenant_id column vs schema per tenant）；迁移成本评估；与 AgentScope Service 兼容分析 | 后端 | 3 |
| **SSO 技术选型与 POC** | 飞书 OAuth2/OIDC 集成评估；企业微信/钉钉 fallback；Authlib 框架选型；POC 对接飞书 | 后端 | 2 |
| **管理后台 UX 全线优化** | 统一管理后台视觉风格；用户/数据集/数据源/凭证/本体/健康检查 六大模块完整 | 前端 | 3 |

**验收标准**：数据源定制平台方案通过评审；多租户方案通过评审；飞书 SSO POC 可登录。

---

### Sprint 14：12/01 – 12/12 ⏳ P2 「平台化落地：数据源平台 + 开放 API + SSO」

**一句话目标**：数据源定制平台初版可用、开放智能体能力暴露、多租户开发启动、SSO 落地。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **数据源定制平台初版** | REST API 数据源接入：配置 endpoint/auth/schema 映射；schema 自动发现与采样；受控查询执行器（读 SQL 走 SQL Guard）；与现有 Datasource adapter 统一管理界面 | 后端 + 前端 | 5 |
| **开放智能体能力暴露** | RESTful API 暴露 BI Worker 查询能力（非 SSE，同步/异步）；API Token 鉴权；速率限制；输入/输出 schema 文档；使用示例 | 后端 + AI | 5 |
| **多租户功能开发** | 基于 S13 方案实现：数据源/数据集/本体 租户隔离；用户-租户绑定；tenant context 注入 AgentScope session；管理员跨租户查询（审计用途） | 后端 | 4 |
| **SSO 落地** | 完成飞书 SSO 对接上线；用户身份映射到 Datalogue 用户体系；SSO 用户首次登录自动创建账号并绑定默认租户 | 后端 | 2 |

**验收标准**：可通过 REST API 接入一个外部数据源并执行查询；外部系统可通过 API Token 调用问数接口；多租户模式下租户 A 看不到租户 B 的数据集和本体；飞书 SSO 可登录。

---

### Sprint 15：12/15 – 12/26 ⏳ 「交付收尾 + 2027 规划」

**一句话目标**：多租户完成、交付材料补齐、安全审计通过、红有达验收、2027 Q1 路线图。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **多租户完善** | 租户管理页面；租户级配置（LLM 模型/速率限制/数据源/本体）；租户使用统计 Dashboard；自服务租户创建（管理员审批） | 后端 + 前端 | 3 |
| **交付材料补齐** | 产品介绍文档（面向非研发用户，含本体建模说明）、架构图更新、用户引导（数据源→数据集→本体→问数→报告）、运维手册（部署/巡检/故障排查）、安全白皮书 | 全体 | 4 |
| **安全审计** | pip-audit / npm audit 漏洞扫描；密钥轮转检查；最小权限审计（DB / Redis / AgentScope）；SQL 注入/敏感数据泄露渗透测试 | 全体 | 3 |
| **红有达部署验收** | 在红有达测试环境完成全功能部署验收；修复部署兼容性问题 | 全体 | 3 |
| **缓冲修复** | 遗留技术债务清理；pre-existing 测试失败修复；lint/build warning 清零；性能回归验证 | 全体 | 2 |
| **2027 Q1 路线图** | 基于下半年使用反馈和技术债务，制定 2027 Q1 优先级路线图：多数据集联合查询、指标市场、行级权限、企业 SSO 全量、K8s 部署 | 全体 | 2 |
| **项目记忆压缩** | `.codex/project-memory.md` 按规则压缩归档旧记录 | 全体 | 1 |

**验收标准**：`pytest tests/` 全绿；`npm run lint` 0 errors / 0 warnings；`npm run build` 无 warning；安全漏洞清零（高危）；红有达验收通过；2027 Q1 计划通过评审。

---

## 五、里程碑检查点

| 检查点 | 日期 | 判定标准 |
|--------|------|----------|
| **M0** 基础链路 | 6/27 | 问数主链打通；多轮可用；Workbench 上线 |
| **M1** 主链迁移 | 7/25 | AgentScope Agent Team 主链替代旧 LangGraph；Chat UI 闭环 |
| **M2** 上下文可控 | 8/22 | 子 Agent 复用；12 轮不爆上下文；快速筛选可用；OTel 5 层 span；知识图谱启动 |
| **M3** 核心能力 | 9/19 | Report Worker 激活；预编译 QP 20 场景；知识图谱上线；脱敏半链路 |
| **M4** 语义基座 | 10/03 | 本体模型上线，驱动至少 5 类概念消歧；本体管理页面可用；本体与知识图谱双向同步 |
| **M5** P0 全面验收 | 10/17 | 连接池无泄漏；限流生效；20 条 E2E（含本体场景）可重复；P0 全部交付 |
| **M6** P1 完成 | 11/14 | 导出/Worker/接口/监控/性能基线 全部交付 |
| **M7** P2 + 收尾 | 12/26 | 数据源平台/多租户/SSO/开放 API/审计通过；红有达验收通过 |

---

## 六、3 人职责总览

| 角色 | P0 Sprint（S4–S10） | P1（S11–S12） | P2（S13–S15） |
|------|---------------------|---------------|---------------|
| **后端** | 子智能体池化/压缩/观测/知识图谱/脱敏/审计/限流/连接池/本体后端 | 导出/接口发布/监控告警 | 数据源平台/多租户/SSO |
| **前端** | Tool UI/Multi-Agent UI/图表渲染/失败态retry/Chat UX/本体页面 | 导出 UI/性能优化/管理后台完善 | 管理后台/交付材料 |
| **AI 域** | 子智能体设计/压缩/预编译 QP/Report Worker/失败收敛/E2E/本体模型设计+NLU | Python+Bash Worker | 开放 API/文档 |

资源约束：3 人非全职（兼其他项目），每 Sprint 有效约 5–7 人天/人。同 Sprint 内 3 人可并行推进不同工作线。

---

## 七、时间线总览

```
 6月           7月           8月           9月             10月            11月          12月
├─ S1-S3 ──────┤── S4-S10 (P0) ──────────────────────────────────────┤ S11-12 (P1) ┤ S13-15 (P2) ┤
│ 基础+多轮+    │  主链稳固 + 查询加固 + 业务语义 + 安全基座            │  能力扩展    │  平台化+交付 │
│ AgentScope    │                                                      │             │            │
│ 主链迁移 ✅   │                                                      │             │            │
│               │                                                      │             │            │
│ S1: 基础工程  │ S4:  UI闭环+子Agent设计                               │ S11: 导出+   │ S13: 过渡   │
│ S2: 多轮+     │ S5:  池化+观测基线                                    │      Worker  │ S14: 平台化 │
│     Workbench │ S6:  压缩+OTel+知识图谱启动                            │ S12: 监控    │ S15: 收尾   │
│ S3: 治理+     │ S7:  Report+预编译QP+知识图谱深化                      │              │            │
│     主链迁移  │ S8:  脱敏+权限+知识图谱上线                            │              │            │
│               │ S9:  ★ 本体建模（实体/概念/关系，驱动消歧）★            │              │            │
│               │ S10: 连接池+限流+P0全面验收                            │              │            │
```

---

*本计划基于 Datalogue 项目 2026-06-02 至 2026-07-13 的实际提交历史（420 条）和 `.codex/project-memory.md` 完成记录，结合用户手动 24 项需求和上传版工作规划整合。v3 将本体建模从 P2 提升至 P0，新增 S9 专做本体建模 Sprint，P0 延长至 10/17。每个 Sprint 结束时应更新状态标记，季度末检查里程碑达标情况。*
