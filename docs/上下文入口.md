# 数语（Datalogue）项目上下文入口

> 本文件是 Codex / Claude 等 AI Agent 的项目入口文档。

## 项目概述

数语是一个面向企业数据的 **AI 原生智能问数平台**。业务用户用自然语言提问，系统通过 Agent 驱动的数据查询链路，给出可追溯、可审计的回答。

## 核心架构（当前 AS-R0）

```
用户 → POST /api/agent-team/tasks/stream (SSE)
  → AgentTeamTaskRuntime
    → AgentScopeServiceTaskRunner（HTTP 调用 AgentScope Service）
      → AgentScope Service (子应用 /agentscope)
        → Agent Team (Leader Agent + BI Worker Agent)
          → Datalogue BI Tools（dataset_query / SQL编译/执行）
```

**关键点：**
- 主链已从 LangGraph 完全迁移到 **AgentScope 官方 Agent Team 架构**
- AgentScope Service 作为 FastAPI 子应用挂载在 `/agentscope`
- 入口统一为 **Agent Team API** (`/api/agent-team/tasks/stream`)
- 旧 LangGraph 相关代码和文档已归档

## 新文档索引

| 文档 | 位置 |
|------|------|
| 文档总索引 | `docs/README.md` |
| 目录治理与模块边界 | `docs/architecture/目录治理与模块边界.md` |
| 系统架构 | `docs/architecture/系统架构.md` |
| 执行链路 | `docs/architecture/执行链路.md` |
| Leader 统一收口与 ECharts 可视化 | `docs/architecture/Leader统一收口与ECharts可视化设计.md` |
| 数据模型 | `docs/architecture/数据模型.md` |
| AgentScope 集成 | `docs/architecture/AgentScope集成.md` |
| API 概览 | `docs/api/API概览.md` |
| 计划与待办 | `docs/plans/README.md` |
| 运维文档 | `docs/operations/` |
| 研究材料 | `docs/research/README.md` |
| E2E 截图资产清单 | `docs/assets/screenshots/e2e/README.md` |

## 文档目录边界

- `docs/architecture/`：当前架构、目录治理、执行链路、AgentScope 集成等长期有效设计材料。
- `docs/api/`：当前 API 说明。
- `docs/operations/`：部署、观测、健康检查和故障处理 Runbook。
- `docs/plans/`：仍需跟踪的路线图、专项实施方案和待办；架构事实仍以 `docs/architecture/` 为准。
- `docs/research/`：竞品和技术调研，只作为决策输入，不直接代表当前实现。
- `docs/assets/`：文档图像资产；长期截图放 `docs/assets/screenshots/`，临时验证截图放 `outputs/` 或 `.omx/artifacts/`。
- `docs/test-reports/`：历史测试报告和阶段验收记录，只有需要追溯验证证据时读取。
- `docs/deliverables/`：对外交付文档，不作为常规开发上下文。
- `docs/archive/`：只读历史归档区，不参与常规上下文入口；仅在追溯旧 LangGraph、Langfuse 下线或历史交付背景时按需读取。

仓库顶层不再设置通用 `assets/` 文档目录。业务文档统一进入 `docs/`，当前文档引用的图片统一进入 `docs/assets/`，旧素材统一进入 `docs/archive/`。

## 项目记忆

当前开发进度和实际项目变更完成记录见 `.codex/project-memory.md`；普通对话、临时分析和项目无关事项不进入项目记忆。

## 快速命令

```bash
# 本地开发
cd datalogue-api && uvicorn app.main:app --reload

# 前端
cd datalogue-web && pnpm dev

# Docker 部署
docker compose up -d

# 测试
cd datalogue-api && pytest
```
