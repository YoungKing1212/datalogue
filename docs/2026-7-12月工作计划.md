# 数语（Datalogue）2026 年 7–12 月工作计划

> 制定日期：2026-07-13
> 拆分粒度：双周 Sprint（约 10 个工作日）
> 团队规模：最多 3 人（后端 / 前端 / AI Agent 域）
> 状态：✅ 已完成 ｜ 🔄 进行中 ｜ ⏳ 待启动

---

## 一、项目全景（现状分析）

### 1.1 已完成的核心能力

| 模块 | 状态 | 交付物 |
|------|------|--------|
| AgentScope 主链（AS-R0 P0/P1） | ✅ 稳定 | `/api/agent-team/tasks/stream` SSE 统一入口；Leader + BI Worker 双 Agent 协作；RepairPlan + RepairPatch 自动修复 |
| BI Worker 查询链路 | ✅ 稳定 | QueryPlan 契约 → DSL 编译 → SQL 执行 → artifact 产物闭环；Join Keys 全链路串通（7/13 交付） |
| Workbench 工作台（C3-P0/P1/P2） | ✅ 稳定 | 右侧 Panel + `/workbench/:threadId`；artifact 详情、timeline、受控 retry、状态模型 |
| 产物分页 | ✅ 上线 | 上限提升至 10000 行；前端 DataTable 每页 100 行 |
| 前端目录治理（G055-G093） | ✅ 完成 | `features/chat`、`shared`、`assistant-ui` 边界划定 |
| 安全基线 | ✅ 落地 | 用户层禁止 SQL/schema/raw rows；安全摘要 + 脱敏 artifact |
| 多轮对话/会话恢复 | ✅ 可用 | 懒创建、上下文槽位、历史回放 `/chat/{id}` |
| 认证体系 | ✅ 上线 | 登录、用户管理、密码策略、base64 传输加密 |

### 1.2 遗留技术债务

| 债务项 | 影响评估 | 涉及范围 |
|--------|---------|---------|
| Phase B 目录迁移未启动 | 高 | `graph/` → `core/`、`agents/` → `domains/`、`agentscope_service/` → `runtime/engine/` 等 7 步 |
| artifact store 未落地 | 高 | SQL 结果集持久化缺失，跨轮只能引用同轮 artifact |
| E2E 自动化测试未覆盖 | 高 | 方案已制定（`docs/superpowers/plans/`），未自动化 |
| 前端测试债务 | 中 | `settings.test.jsx` Router 缺失、`DatalogueMessage` echarts mock 等 pre-existing 失败 |
| lint warnings + Vite chunk-size | 低 | 13 个 pre-existing warnings；Vite chunk size 警告 |
| Report/Python/Audit Worker 未启用 | 中 | 仅 BI Worker 活跃，其余是 disabled placeholder |
| OTel 观测仅本地可用 | 中 | 无 Prometheus/Grafana 生产集成 |
| 权限体系仅基线 | 高 | 无行级权限、字段脱敏、审计日志 |
| 查询缓存未实现 | 中 | 每次查询都重新调用 LLM + 执行 SQL |
| Docker 部署未加固 | 中 | 生产镜像、K8s 模板、备份恢复未做 |
| `bi_worker_timeline_cache` 临时模块 | 低 | 标记 "TODO(后期删除)"，待 OTel 替代 |

### 1.3 代码库基线

| 指标 | 数值 |
|------|------|
| 后端测试数 | 496 个 |
| 前端测试数 | ~190+（含 pre-existing failures） |
| 主分支状态 | 干净（main） |
| 未合并工作树 | `codex/doris-oracle-datasource-team`（落后 main 较多） |
| Python | 3.11 + FastAPI 0.111 + AgentScope 2.0.3 |
| 前端 | React + assistant-ui + Vite |

---

## 二、整体路线图：三大阶段

```
7月                     8月                  9-10月                 11-12月
├── 架构收尾 ──────┼── 能力扩展 ──────────┼── 生产就绪 ────────────┤
│ Phase B 迁移      │  多数据源 JOIN       │  行级权限/字段脱敏     │
│ artifact store    │  图表可视化          │  缓存体系             │
│ E2E 自动化        │  智能推荐/同义词     │  监控告警             │
│ Worker 扩展       │  上下文压缩          │  K8s 部署             │
│                   │  Report Worker 上线  │  生产文档             │
```

---

## 三、双周 Sprint 计划

### Sprint 1：7/14 – 7/25 🔄 **Phase B 启动 + artifact store 基线**

**目标**：Phase B 前两步落地，artifact 跨轮引用可用。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **B1 — `graph/llm.py` → `core/llm.py`** | fan-in 仅 3 个 import 点，风险最低，打样用 | 后端 | 2 |
| **B2 — `agents/bi_agent/` → `domains/bi/agent/`** | 7 个 import 点，需更新 registry 引用 | 后端 | 3 |
| **artifact store 核心** | SQL 结果集持久化到 `query_artifact`；跨轮状态压缩为 `result_ref` / `report_id`；Alembic 迁移 | 后端 | 4 |
| **前端测试债务修复** | `settings.test.jsx` Router 上下文修复；`DatalogueMessage` echarts mock | 前端 | 2 |
| **API 级 E2E 测试** | 按测试方案落地 curl SSE 检测：事件序列、artifact ref、安全边界 | AI Agent 域 | 3 |

**验收标准**：
- `app/graph/` 移除，`core/llm.py` 成为 canonical 入口
- `app/agents/` 移除，`domains/bi/agent/` 成为 canonical 入口
- 新查询产物可在下一轮对话中通过 ref 回放
- `pytest tests/` 关键子集全绿；`npm run test` pre-existing 修复

---

### Sprint 2：7/28 – 8/08 🔄 **Phase B 深水区 + E2E 验收**

**目标**：核心代码目录迁移完成，E2E 浏览器自动化通过。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **B3 — `agentscope_service/bi_worker_*` → `domains/bi/worker/`** | 6 个文件，包含 runtime、contracts、validator、tools | 后端 | 4 |
| **B4a — 框架桥接 → `runtime/engine/`** | 改动面积最大（8 个文件），`runner.py` 等核心文件 | 后端 | 3 |
| **浏览器 E2E 自动化** | Playwright/Claude 自动化：主查询、artifact 详情、多轮追问、会话恢复、安全边界 | AI Agent 域 | 4 |
| **lint warning 治理** | 13 个 pre-existing warnings 逐条修复或标注为 intentional | 前端 | 1 |
| **Workbench 图表预览初版** | artifact 含图表数据时，Workbench 显示图表预览而非纯表格 | 前端 | 3 |

**验收标准**：
- `app/agentscope_service/` 中 bi_worker 相关文件消失
- E2E 覆盖 5 类证据（页面、Network、后端日志、DB、artifact API）
- Workbench 图表预览可展示
- 风险：B4a 需拆 4a/4b/4c 子步骤，每步独立 commit 可回滚

---

### Sprint 3：8/11 – 8/22 ⏳ **Phase B 收尾 + 上下文压缩**

**目标**：Phase B 全部完成，LLM 上下文大小可控。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **B4b/c — 业务层 → `domains/agent_team/`** | 删除 `agentscope_runtime/`；事件投影、progress bridge 搬迁 | 后端 | 4 |
| **B5 — `services/` 残余清理** | import 改新路径；旧 facade 加 DeprecationWarning | 后端 | 2 |
| **B6 — `utils/` SQL 工具 → `domains/query_execution/`** | SQL guard、方言适配、执行器搬迁（8 个文件） | 后端 | 3 |
| **产物上下文压缩** | `last_success_task` 白名单落地；`_thread` 上下文大小 guard；大产物不进 LLM 承接态 | AI Agent 域 | 3 |
| **前端 chat 测试迁移** | `src/assistant/*.test.js` → `src/features/chat/__tests__/` | 前端 | 2 |
| **Vite chunk-size 治理** | 代码分割（lazy loading），减小首屏 JS 体积 | 前端 | 2 |

**验收标准**：
- `app/agentscope_service/` 和 `app/agentscope_runtime/` 目录消失
- `app/utils/` 只保留无领域归属工具（`think.py`、`token.py`、`json_utils.py`）
- 多轮对话时 LLM 上下文 ≤ 安全阈值（128K token 的 80%）
- `npm run build` 无 chunk size warning

---

### Sprint 4：8/25 – 9/05 ⏳ **多数据源联合查询 + Report Worker 上线**

**目标**：支持跨表 JOIN，Report Worker 激活。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **多表 JOIN 完善** | `relationship_ref` 稳定解析到真实 join key；跨表 group/filter 不缺 JOIN | 后端 | 4 |
| **Report Worker 激活** | Worker 注册表 `bi` + `report` 两类；Leader Prompt 强制触发规则；Event 投影适配 | AI Agent 域 | 4 |
| **跨数据源查询（初版）** | 语义层统一：MySQL/PostgreSQL/Doris 在语义层视为统一逻辑数据集 | 后端 | 3 |
| **前端 `src/app` 拆分** | `app-root.jsx`、`app-shell.jsx`、`routes.jsx` 薄 re-export 建立 | 前端 | 2 |

**验收标准**：
- "统计 2026 年 4-5 月各地区各品类 GMV" 跨表 JOIN 成功
- Report Worker 可输出结构化报表
- 后端 `pytest tests/` 全绿

---

### Sprint 5：9/08 – 9/19 ⏳ **图表可视化 + 智能推荐**

**目标**：查询结果支持图表展示，用户输入更智能。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **图表可视化** | Chat 消息中支持折线图/柱状图/饼图；完善 echarts `ChartBlock`；Workbench 图表面板同步 | 前端 | 5 |
| **报表导出** | artifact 支持 Excel/CSV 导出；前端提供下载按钮 | 后端+前端 | 3 |
| **智能推荐问题** | 基于用户历史查询和数据源特征，展示 5 条推荐问题 | AI Agent 域 | 3 |
| **同义词/别名扩展** | 术语表支持同义词环；"销售额"可匹配 "GMV"、"营收" | AI Agent 域 | 2 |
| **意图识别增强** | 区分"查询明细"、"统计汇总"、"趋势分析"、"对比分析"四类意图 | AI Agent 域 | 3 |

**验收标准**：
- 图表 artifact 在 Chat 和 Workbench 均可展示
- 导出 Excel 内容与页面展示一致
- 新用户进入 Chat 时侧栏展示推荐问题
- 意图分类准确率 ≥ 80%

---

### Sprint 6：9/22 – 10/03 ⏳ **权限体系与数据安全**

**目标**：企业级权限基线、敏感数据保护。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **行级权限** | 基于用户身份/部门的数据行过滤；SQL 执行前自动注入权限条件 | 后端 | 5 |
| **字段级脱敏** | 敏感字段（手机号、银行卡）按规则脱敏展示；配置化脱敏规则 | 后端 | 3 |
| **数据集权限** | 数据集/数据源可见性控制；用户只能看到授权的数据集 | 后端 | 2 |
| **注册接口管理员限制** | `POST /api/auth/register` 限制为管理员调用 | 后端 | 1 |
| **数据访问审计** | 记录谁、何时、查询了什么数据、返回行数；审计日志查询 | AI Agent 域 | 3 |

**验收标准**：
- 非 admin 用户查询"员工信息"时只看到自己部门记录
- 敏感字段在 Chat/Workbench/导出中均脱敏
- 审计日志保留 ≥ 90 天并可查询

---

### Sprint 7：10/06 – 10/17 ⏳ **性能优化与缓存体系**

**目标**：查询响应速度和系统吞吐量提升。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **查询结果缓存** | 相同/相似 SQL 查询结果缓存（Redis）；TTL 按数据新鲜度配置 | 后端 | 4 |
| **语义缓存** | 相同问题的语义 embedding 缓存；直接命中历史答案 | AI Agent 域 | 4 |
| **SQL 执行优化** | 大表 COUNT(*) 优化、分页查询优化、索引建议 | 后端 | 2 |
| **BI Worker 并发控制** | 并发数限制；队列积压时 graceful degradation | 后端 | 2 |
| **前端性能** | 大产物虚拟滚动、懒加载、骨架屏 | 前端 | 3 |
| **时序推理性能基线** | timeline 耗时格式化；LLM 调用耗时独立追踪 | 后端 | 1 |

**验收标准**：
- 缓存命中率 ≥ 30%；缓存命中时响应 < 1s
- 并发 10 个查询时系统不崩溃、排队有序
- 10000 行产物前端滚动流畅

---

### Sprint 8：10/20 – 10/31 ⏳ **监控告警与运维可观测**

**目标**：生产级监控告警体系就绪。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **系统指标接入 Prometheus/Grafana** | QPS、延迟、错误率、LLM token 消耗、并发 Worker 数 | 后端 | 4 |
| **业务指标** | 查询成功率、意图识别准确率、用户满意度（thumbs up/down） | AI Agent 域 | 3 |
| **告警规则** | 查询失败率 > 5%、平均延迟 > 30s、LLM 调用失败时触发告警 | AI Agent 域 | 2 |
| **结构化日志 + trace_id 贯穿** | JSON 格式日志输出；统一 trace_id 贯穿全链路 | 后端 | 2 |
| **/health 扩展** | 数据库、Redis、AgentScope Service 子健康状态 | 后端 | 1 |
| **删除临时调试模块** | 移除 `bi_worker_timeline_cache` 及 `worker_logging._cache_bi_worker_timeline_if_enabled`；OTel 正式接手 | 后端 | 1 |

**验收标准**：
- Grafana dashboard 可查看过去 7 天核心指标
- 告警通道可正常接收
- 单个请求可在日志中通过 trace_id 追踪全链路

---

### Sprint 9：11/03 – 11/14 ⏳ **部署自动化与灾难恢复**

**目标**：生产部署流程完善，系统可恢复。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **Docker 生产镜像加固** | 多阶段构建（已有）、镜像瘦身 < 500MB、非 root 运行、HEALTHCHECK | 后端 | 2 |
| **K8s 部署模板** | Deployment、Service、ConfigMap、Secret、HPA、Ingress 模板 | 后端 | 4 |
| **数据库迁移自动化** | Alembic 迁移生产前备份与回滚策略 | 后端 | 2 |
| **灾难恢复** | 数据库定期备份、RPO/RTO 目标、恢复演练文档 | 后端 | 2 |
| **Nginx HTTPS 配置** | SSL 证书挂载、CORS 安全策略、速率限制 | 后端 | 1 |
| **前端生产构建优化** | 环境变量注入、build 产物压缩、CDN 部署准备 | 前端 | 2 |

**验收标准**：
- `docker build` 产物镜像 < 500MB
- K8s 模板在测试集群可部署并通过健康检查
- 数据库迁移可在 5 分钟内完成并验证

---

### Sprint 10：11/17 – 11/28 ⏳ **文档完善与生产验收**

**目标**：补齐生产级文档，通过内部验收。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **运维手册** | 部署指南、日常巡检、故障排查、扩容操作 | 后端 | 3 |
| **API 文档完善** | OpenAPI/Swagger 完整注解；示例请求/响应 | 后端 | 3 |
| **用户手册** | 面向业务用户的问数指南、常见问题、最佳实践 | 前端 | 2 |
| **安全白皮书** | 数据安全架构、权限模型、审计策略、合规说明 | AI Agent 域 | 3 |
| **性能基线报告** | 压测结果、瓶颈分析、优化建议、容量规划 | 后端 | 2 |
| **2027 Q1 规划** | 基于用户反馈和技术债务，制定下季度计划 | 全体 | 1 |

**验收标准**：
- 新团队成员可按运维手册独立部署一套环境
- API 文档覆盖率 ≥ 90%
- 压测报告包含至少 50 并发用户场景

---

### Sprint 11：12/01 – 12/12 ⏳ **缓冲收尾**

**目标**：处理遗留债务，预留突发需求空间。

| 交付项 | 说明 | 负责人 | 预估人天 |
|--------|------|--------|---------|
| **技术债务清理** | 未关闭的 TODO、pre-existing 测试失败、lint warning | 按域分配 | 3 |
| **安全审计** | 第三方依赖漏洞扫描（pip-audit / npm audit）；密钥轮转；最小权限检查 | 全体 | 3 |
| **性能回归** | 全链路压测确认无性能退化 | 后端 | 2 |
| **用户反馈闭环** | 收集早期用户反馈，修复高优先级问题 | 全体 | 3 |
| **Datalogue 部署验收** | 在红有达测试环境完成部署验收；修复部署中发现的兼容性问题 | 全体 | 4 |
| **项目记忆压缩更新** | 检查 `.codex/project-memory.md`，按压缩规则归档旧记录 | 全体 | 1 |

**验收标准**：
- `pytest tests/` 全绿；`npm run lint` 0 errors、0 warnings；`npm run build` 无 warning
- 安全扫描无高危漏洞
- 下季度计划文档通过评审

---

## 四、3 人职责矩阵

| 角色 | 核心职责 | Sprint 覆盖 |
|------|---------|------------|
| **后端** | Phase B 迁移、artifact store、权限体系、缓存、监控、K8s、SQL 引擎优化 | S1-S11 |
| **前端** | 测试债务修复、图表可视化、报表导出、性能优化、用户手册 | S1-S11 |
| **AI Agent 域** | E2E 测试、Worker 扩展、智能推荐、上下文压缩、安全白皮书、运维文档 | S1-S11 |

资源约束说明：
- 3 人非全职 Datalogue（兼其他项目），每 Sprint 有效投入约 5-7 人天/人
- 复杂 Sprint（如 S2、S6）需要 3 人并行推进不同工作线
- S11（缓冲 Sprint）预留弹性，视前期 Sprint 完成度动态调整

---

## 五、关键依赖与风险矩阵

| 风险项 | 影响 | 缓解措施 | 负责人 |
|--------|------|----------|--------|
| Phase B 迁移与主工作区未提交改动冲突 | 高 | 每步独立 commit + 可回滚；主工作区改动先单独收口 | 后端 |
| artifact store DB schema 变更 | 中 | Alembic 迁移 + 回滚脚本；staging 预验证 | 后端 |
| 多数据源联合查询方言差异 | 中 | 先同数据集 JOIN；跨数据源用语义层统一 | 后端 |
| 权限体系改动面大 | 高 | 先数据集粗粒度；行级权限分阶段实现 | 后端 |
| LLM 模型成本波动 | 中 | 语义缓存降低重复调用；本地模型 fallback | AI Agent 域 |
| 前端测试长期感知失败 | 低 | S1 集中修复后持续护航 | 前端 |
| 红有达内部环境兼容性 | 中 | S10-S11 预留部署验收窗口 | 全体 |
| Richard（其他人）可用性波动 | 中 | 关键路径不依赖特定人员；每项写文档，交接成本最低化 | 全体 |

---

## 六、里程碑检查点

| 检查点 | 日期 | 判定标准 |
|--------|------|----------|
| M1 Phase B 核心收尾 | 8/22 | `app/agents/`、`app/agentscope_service/` 目录消失；E2E 自动化通过 |
| M2 高级查询能力 | 9/19 | 多表 JOIN 可用；图表可视化上线；智能推荐上线 |
| M3 企业安全基线 | 10/17 | 行级权限/字段脱敏可用；缓存体系上线；监控告警可工作 |
| M4 生产就绪 | 11/28 | K8s 部署可用；灾难恢复演练通过；文档齐全 |
| M5 季度收尾 | 12/12 | 无 P0 债务；性能基线达标；2027 Q1 计划就绪 |

---

*本计划基于 Datalogue 项目截至 2026-07-13 的实际状态制定。Sprint 结束时应更新 `.codex/project-memory.md` 并同步本计划的状态。*
