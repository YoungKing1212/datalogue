# Agent Planning 历史说明

本目录保留早期 LeadAgent、SubAgent、语义资产披露、筛选阈值和 LLM 评估基线设计。

这些文档属于历史设计依据，不是当前主链规划。当前规划以 `docs/README.md`、`docs/上下文入口.md`、`docs/architecture/系统架构.md` 和 `docs/architecture/执行链路.md` 为准。

阅读规则：

- 文中出现的旧 LeadAgent / SubAgent / planner 口径，只能作为当时方案背景。
- 文中出现的 Langfuse、旧 trace 字段或在线评估字段，只作为历史观测方案。
- 新增规划不得继续把本目录中的旧入口、旧 trace 或旧执行主语写成当前事实。
