# 数语（Datalogue）2026 年 6–12 月工作计划（完整版）

> 制定日期：2026-07-13
> 拆分粒度：双周 Sprint（约 10 个工作日）
> 团队规模：3 人（后端 / 前端 / AI Agent 域）
> 覆盖范围：2026-06-02 至 2026-12-26，共 15 个 Sprint
> 状态：✅ 已完成 ｜ 🔄 进行中 ｜ ⏳ 待启动

---

## 一、项目当前状态（截至 2026-07-13）

### 1.1 已交付的核心能力

| 模块 | 状态 | 说明 |
|------|------|------|
| AgentScope 主链 | ✅ 稳定 | Agent Team API (`/api/agent-team/tasks/stream`) 为统一入口；Leader + BI Worker 双 Agent 协作；mirror 四表、Chat Session Bridge、Workbench View Model |
| BI Worker 查询链路 | ✅ 稳定 | QueryPlan 契约 → DSL 编译 → SQL 执行 → artifact 产物闭环；L0-L5 渐进式上下文；RepairPlan + RepairPatch 自动字段漂移修复 |
| Workbench 工作台 | ✅ 稳定 | 右侧 Panel + 隐藏 `/workbench/:threadId` 路由；artifact 详情、timeline、受控 retry |
| #24 数据展示分页 | ✅ 上线 | 上限 10000 行；前端 DataTable 每页 100 行 |
| 多轮对话/会话恢复 | ✅ 可用 | 懒创建、上下文槽位、历史回放 `/chat/{id}` |
| 安全基线 | ✅ 落地 | 用户可见层禁止 SQL/schema/raw rows/query_plan |
| 认证体系 | ✅ 上线 | 登录、用户管理、密码策略、base64 传输加密 |
| LLM 配置层 | ✅ 完成 | 从 LiteLLM 迁移到 AgentScope credential 原生管理 |
| 前端目录治理 | ✅ 完成 | `features/chat`、`shared`、`assistant-ui` 边界 |
| 后端测试 | ✅ | 496 个，前端测试 ~190 个 |

---

## 二、优先级体系

```
P0（主链稳固 + 核心功能完善，6–10 月初，9 个 Sprint）
  三条主线并行推进：
  A. 主链 UI 与链路稳定
     - BI Tools 渲染 / Multi-Agent 可视化（#1、#9）
     - 子智能体会话级保留（#3）
     - 多轮快速筛选优化（#2）
     - AgentScope 原生化观测 / OTel（#6、#11）
     - 上下文压缩（#19）
  B. 查询链路深度加固
     - 表关系维护 / 知识图谱（#10）
     - 预编译 Query-Plan（#18）
     - BI Worker 失败类收敛
     - Report Worker 节点稳固（#12 之 report）
  C. 安全与数据治理基座
     - 字段级数据脱敏（#5）
     - 权限审计（#14）
     - API 限流（#21）
     - 数据库连接池（#20）
     - 认证与管理后台补强（#7）

P1（能力扩展，10–12 月，3 个 Sprint）
  数据导出 / Python+Bash Worker / 接口发布 / 监控告警 / 管理后台完善

P2（平台化，12 月，3 个 Sprint）
  多租户 / 数据源定制平台 / SSO / 开放 API / 本体建模 / 交付材料
```

---

## 三、双周 Sprint 计划（15 个 Sprint，6/02–12/26）

---

### Sprint 1：6/02 – 6/13 ✅ 「基础工程与问数链路打通」

**目标**：建立工程规范，打通问数基础链路。

| 交付项 | 说明 |
|--------|------|
| 工程规范 | 项目记忆、Python 文件头模板、AGENTS.md、编码规范（作者: yangkai / KenYang） |
| 问数入口 | 蓝图分析、NL2DSL、语义资产解析、术语归一化、数据集上下文服务 |
| SQL 安全 | SQL 只读校验、SQL 自动修复 Retry Trace、回答解释包与可信度气泡 |
| 基础观测 | LiteLLM 配置化、Langfuse 本地观测（Trace、Prompt Registry）、查询审计 Trace 页面 |
| SubAgent 骨架 | Dataset SubAgent Manifest、LeadAgent Planner / 控制面工具、渐进式披露 |
| 数据模型 | 语义数据集、指标、维度、术语、蓝图、数据源模型建立 |

**Git 关键提交**：`55c7e3f6` 至 `3e0db99d`，约 50 个提交。

**验收标准**：后端 pytest 通过；`docker compose up` 可启动；前端 dev server 可访问。

---

### Sprint 2：6/16 – 6/27 ✅ 「多轮对话与 Workbench 上线」

**目标**：SubAgent 规划收口、多轮状态管理、Workbench 工作台交付。

| 交付项 | 说明 |
|--------|------|
| SubAgent v2 | 查询规划层 v2、语义资产渐进式披露、DSL 提示词、`DatasetRouter` 候选匹配 |
| Thread Memory | `QueryTaskCapsule`、`ConversationState`、多轮槽位承接、多轮 refinement slots |
| C1 RepairPlan | 字段失败诊断 (`classify_sql_failure`)、RepairPlan 契约、事件、Artifact refs |
| C2 RepairPatch | 离线 Patch Engine、字段候选 (`blueprint_join`)、Tool validation、主链接入 |
| C3-P0 Workbench | mirror 四表 (`agentscope_session/message/event/ref`)、Chat Bridge、View Model API、Controlled Retry、Chat 右侧 Panel |
| B-first C-ready 核心链路 | BI_SOUL 契约同步、Capability Router 路由收窄、QueryGraph Compiler、SubAgent ToolAdapter、统一 Event Envelope、ArtifactCard |

**Git 关键提交**：`2f267d2e` 至 `5b46a945`，约 40 个提交。

**验收标准**：真实问题"查询杨凯 2024 年工作日志"成功问数并显示结果时间线；Workbench 展示 timeline + artifact；历史会话可恢复。

---

### Sprint 3：6/30 – 7/11 ✅ 「治理收口与 AgentScope 主链迁移」

**目标**：Langfuse 移除、目录治理、AgentScope Agent Team 主链建立、BI Worker 渐进式上下文链路。

| 交付项 | 说明 |
|--------|------|
| Langfuse 移除 | 删除依赖、Docker 服务、API 挂载、前端 Trace 入口；改为本地日志 |
| 目录治理 Phase A/B | facade-first 策略；`app/services/` → `domains/` / `core/`；`agents/` → `domains/bi/agent/`；`agentscope_service/` 边界划定 |
| AgentScope Agent Team 主链 | `/api/agent-team/tasks/stream` SSE 统一入口；Leader Agent + BI Worker 双 Agent；AgentScope Service 子应用挂载 |
| BI Worker L0-L5 渐进式 | L0 候选数据集 / L1 资产匹配 / L2 Schema 切片 / L3 值域 / L4 校验 / L5 受控执行；`BIWorkerQueryPlan` v1 契约 |
| LLM 配置层迁移 | LiteLLM → AgentScope credential 原生管理；模型选择器接 AgentScope ModelCard |
| Report Worker 骨架 | Leader Prompt 强制触发规则；worker 注册表收敛为 `bi` + `report` 两类 |
| 前端体验修复 | 左侧功能栏真实数量、历史回放、chat 测试迁移 |
| 产物分页 | 上限提升至 10000 行；前端 DataTable 每页 100 行 |

**Git 关键提交**：`5b46a945` 至 `49c7a968`，约 150 个提交（含 AS-R0 P0/P1/P2 和 AgentScope 2.0.3 主迁移）。

**验收标准**：前端 `npm run test` 190+ passed；后端关键子集 189 passed；`/chat` 可完成候选数据集确认 → 查询 → artifact 闭环。

---

### Sprint 4：7/14 – 7/25 🔄 P0 「UI 体验闭环 + 子智能体设计」

**一句话目标**：Chat 主路径完整承接 BI Tools 渲染与 Multi-Agent 可视化，子智能体保留机制设计落地。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **BI Tools 渲染（Tool UI）** | Chat 消息中展示工具运行中/成功/失败/artifact refs/安全摘要/耗时；Streamdown 流式状态接续 | 前端 | 5 |
| **Multi-Agent 可视化初版** | nested messages 展示 Leader/Worker 协作过程；reasoning 摘要只含安全业务信息，raw thinking 仅 debug gate 下可见 | 前端 | 4 |
| **子智能体会话级保留机制设计** | 同一 session 内 BI Worker 不随单轮任务结束而删除；设计 session-scoped worker pool；分析 AgentScope 2.0.3 的 session 生命周期与 agent 绑定关系 | AI + 后端 | 4 |
| **上下文压缩技术方案** | 基于 DeerFlow 三层中间件参考，制定 Datalogue 多轮上下文压缩方案（摘要阈值、技能包留存、工具输出外部化）；评估 AgentScope 中间件钩子可行性 | AI 域 | 3 |
| **Workbench 去常驻化收尾** | 普通 `/chat` 不挂 WorkbenchPanel；artifact 详情/失败态/retry 入口迁到 Chat 内 | 前端 | 3 |

**验收标准**：Chat 中可见工具执行过程卡片 + artifact 结果卡 + 推理摘要；子智能体保留方案完成评审；Workbench 不再常驻。

---

### Sprint 5：7/28 – 8/08 ⏳ P0 「主链加固 + 观测基线」

**一句话目标**：子智能体 session pool 落地，AgentScope 全链路可观测基线建立，BI Worker 关键失败类收敛。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **子智能体 session-scoped pooling 落地** | 同一会话 session 内复用 BI Worker agent_id；空闲 TTL 到期释放；避免频繁 AgentCreate → TeamDelete；降级方案：Redis 缓存 worker agent_id | 后端 + AI | 5 |
| **AgentScope 观测基线** | task_id/trace_id 贯穿 Leader session → BI Worker → 工具调用 → artifact → 最终 answer；覆盖四个关键节点 | 后端 | 4 |
| **BI Worker 失败类收敛** | 针对 FIELD_NOT_FOUND/FILTER_MISSING 建立回归用例；校验 field_refs 自动兜底误报率 | AI 域 | 3 |
| **Chat 页失败态 + retry 闭环** | 失败诊断卡片（safe diagnosis + recommended action）；retry 按钮触发重试，保留原 artifact refs | 前端 | 3 |

**验收标准**：同一会话连续 3 轮问数不产生新 AgentCreate；task_id 可在日志中追踪全链路；代表性问题误报率下降。

---

### Sprint 6：8/11 – 8/22 ⏳ P0 「上下文压缩 + OTel + 知识图谱启动」

**一句话目标**：多轮对话上下文不爆、OTel 链路落地、表关系知识图谱初版启动。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **上下文压缩功能上线** | 多轮超过阈值自动触发历史压缩；保留技能包与关键 artifact_refs；摘要前 hook 写入长期存储 | 后端 + AI | 5 |
| **多轮快速筛选优化** | 已有 SQL 骨架时后续轮次跳过完整 QueryPlan 生成，只做 filter/limit 参数替换 | AI 域 | 3 |
| **OTel 链路观测补全** | Span 覆盖：task 创建 → session → 工具执行 → SQL 编译 → DB 执行 → artifact 写入 | 后端 | 3 |
| **Tool UI 完善 + Mermaid/ECharts** | Chat 消息支持 Mermaid 流程图和 ECharts 纯 JSON 图表渲染；图表失败时降级为数据表格 | 前端 | 4 |
| **表关系知识图谱启动** | 从 `analysis_blueprint.call_template` 和 `join_requirements` 自动提取表间关系；数据集管理页展示 ER 图 | 后端 + AI | 3 |

**验收标准**：12 轮以上上下文 ≤ 128K 80%；task_id 串起 5 层 span；已有 SQL 快速筛选不重建 QueryPlan；数据集管理页可见 ER 图初版。

---

### Sprint 7：8/25 – 9/05 ⏳ P0 「Report Worker 稳固 + 预编译 Query-Plan」

**一句话目标**：Report Worker 激活进入灰度，预编译 Query-Plan POC 落地，知识图谱深化。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **Report Worker 激活并稳固** | Worker 注册表增加 "report" 类型；Leader 根据用户意图和结果复杂度自动判断是否派生；报告合并入聊天 Markdown；失败不影响 artifact 展示 | AI 域 | 4 |
| **预编译 Query-Plan POC** | 高频重复问题缓存已编译 QueryPlan + SQL；命中缓存跳过 LLM→Plan→Compile 链路；POC 覆盖 5 个场景 | AI 域 | 3 |
| **表关系知识图谱深化** | 跨 schema 关系提取；知识图谱驱动自动 join（替代 LLM 猜测）；在 SQL 编译器中消费 knowledge_graph refs | 后端 | 4 |
| **Chat 多轮上下文体验优化** | 多轮快速筛选的前端交互：筛选条件可视化编辑、历史 artifact 快速引用 | 前端 | 3 |
| **OTel 观测面板（内部）** | task_id 查询 → 全链路 span 可视化；耗时分段展示；失败节点高亮 | 后端 | 2 |

**验收标准**：含"报告/分析/总结"关键词自动生成 Markdown 报告；预编译场景缓存命中跳过 LLM；跨表 JOIN 可通过知识图谱自动确定 join key。

---

### Sprint 8：9/08 – 9/19 ⏳ P0 「安全基座一：脱敏 + 权限」

**一句话目标**：字段脱敏全链路覆盖、权限审计可用、管理后台安全管控落地。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **字段级数据脱敏** | 手机号/身份证/银行卡/邮箱按配置化规则脱敏；Chat/Workbench/导出/artifact API 全部生效 | 后端 | 5 |
| **权限审计** | 记录谁、何时、查询了什么数据集、SQL 指纹、返回行数、脱敏命中；审计日志保留 ≥ 90 天 | 后端 | 3 |
| **认证与管理后台补强** | 管理员鉴权覆盖用户创建/数据源管理/数据集发布/模型凭证；用户角色（管理员/数据管理员/业务用户）；管理后台页面完整 | 后端 + 前端 | 5 |
| **预编译 Query-Plan 扩展** | POC 扩展到 20 个场景；Query-Plan 指纹计算；缓存 TTL 可配 | AI 域 | 2 |
| **表关系知识图谱上线** | 知识图谱在数据集管理页完全可用；手动补录 join key；与蓝图版本联动 | 后端 + 前端 | 3 |

**验收标准**：敏感字段在 Chat/导出/Workbench 均脱敏；非管理员不能调用管理接口；知识图谱在数据集管理页可交互编辑。

---

### Sprint 9：9/22 – 10/03 ⏳ P0 「性能基座 + P0 全面验收」

**一句话目标**：数据库连接池加固、API 限流上线、P0 收尾验收。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **数据库连接池优化** | pool_size/max_overflow 调优；连接空闲回收；慢查询日志（> 5s 告警）；连接泄漏检测 | 后端 | 3 |
| **API 限流上线** | 按用户/IP 限流（每分钟/每天）；超限 429 + 可读提示；配置化阈值 | 后端 | 2 |
| **BI Worker 回归 + 20 条 E2E** | 建立 20 条代表性中文业务问题的可重复 E2E 套件；每类失败有明确 failure_type + 安全摘要 | AI 域 | 4 |
| **P0 阶段性验收** | Chat 完整链路：登录→问数→Tool UI→artifact→报告→失败恢复→retry；全部安全边界验证 | 全体 | 3 |
| **前端 Chat 体验打磨** | Tool UI 动画、artifact 卡片样式、报告 Markdown 排版、Mermaid/ECharts 响应式 | 前端 | 3 |

**P0 里程碑验收标准**：
- Chat UI 完整闭环：Tool UI + Multi-Agent 可视化 + 报告 + 失败 recovery
- 同一会话内子 Agent 复用，不频繁创建/删除
- 12 轮对话上下文不爆；已有 SQL 快速筛选不重建 QueryPlan
- 跨表 JOIN 由知识图谱自动确定 join key
- 预编译 Query-Plan 20 个场景缓存可用
- Report Worker 激活，报告失败不影响 artifact
- 字段脱敏全链路覆盖；权限审计可查；API 限流生效
- 数据库连接池无泄漏、慢查询可发现
- 20 条业务问题 E2E 可重复；task_id 可追踪全链路
- 无 SQL/schema/raw rows 泄露

---

### Sprint 10：10/06 – 10/17 ⏳ P1 「能力扩展一：导出 + Worker + 接口」

**一句话目标**：数据导出上线、Python/Bash Worker 激活、接口发布。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **数据导出（Excel/CSV）** | artifact 支持 Excel/CSV 导出；前端下载按钮；10 万行内 30 秒完成 | 后端 + 前端 | 5 |
| **Python/Bash Worker 激活** | Python Worker（pandas + matplotlib）；Bash Worker 仅内部运维 | AI 域 | 3 |
| **接口发布完善** | OpenAPI/Swagger 完整注解；示例请求/响应；API 版本管理；使用示例 | 后端 | 3 |
| **前端性能优化** | 大 artifact 虚拟滚动；chat 消息懒加载；骨架屏 | 前端 | 3 |

**验收标准**：artifact 详情页可下载 Excel/CSV；Python Worker 可执行数据分析任务；API 文档覆盖率 ≥ 90%。

---

### Sprint 11：10/20 – 10/31 ⏳ P1 「监控告警 + 性能基线」

**一句话目标**：Prometheus/Grafana 告警体系就绪、全链路性能基线确立。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **Prometheus/Grafana 告警上线** | Dashboard（QPS/延迟/错误率/Token/成功率/并发）；告警规则：失败率 > 5%、P95 > 30s → 飞书通知 | 后端 | 4 |
| **业务指标补全** | 查询成功率、LLM Token 消耗趋势、P50/P95/P99 响应时间、Worker 并发数 | 后端 | 3 |
| **全链路性能基线** | 分段耗时报告；压测 50 并发 | 后端 | 3 |
| **前端 E2E 自动化** | Playwright：登录→问数→artifact→详情→retry；核心场景每天自动跑 | 前端 | 2 |
| **管理后台完善** | 数据集发布审批流；模型凭证管理 UX 优化；系统健康 Dashboard | 前端 | 3 |

**验收标准**：Grafana 可查看 7 天核心指标；告警通知可收到；性能基线报告包含分段耗时。

---

### Sprint 12：11/03 – 11/14 ⏳ P1→P2 「过渡：监控收尾 + 平台化设计」

**一句话目标**：OTel 全链路收尾，数据源定制平台和多租户技术方案完成。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **OTel 全链路观测完成** | 8 层 span 完整覆盖；trace_id 贯穿后端日志 + AgentScope span + 前端请求 | 后端 | 3 |
| **数据源定制平台技术方案** | REST API 数据源接入方案；schema 自动发现；查询执行器隔离 | 后端 | 3 |
| **多租户技术方案** | 租户边界定义；数据库方案评估；与 AgentScope Service 兼容分析 | 后端 | 3 |
| **SSO 技术选型与 POC** | 飞书/企微 OAuth2 集成评估；POC 对接 | 后端 | 2 |
| **管理后台 UX 优化** | 统一视觉风格；用户/数据集/数据源/凭证/健康 五大模块 | 前端 | 3 |

**验收标准**：数据源定制平台方案通过评审；多租户方案通过评审；SSO POC 可登录。

---

### Sprint 13：11/17 – 11/28 ⏳ P2 「平台化一：数据源平台 + 开放 API」

**一句话目标**：数据源定制平台初版可用、开放智能体能力暴露、多租户开发启动。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **数据源定制平台初版** | REST API 数据源接入；schema 自动发现与采样；受控查询执行器 | 后端 + 前端 | 5 |
| **开放智能体能力暴露** | RESTful API 暴露 BI Worker 查询能力（同步/异步）；API Token 鉴权；文档 | 后端 + AI | 5 |
| **多租户功能开发** | 数据源/数据集租户隔离；用户-租户绑定；tenant context 注入 | 后端 | 4 |
| **SSO 落地** | 至少完成飞书 SSO 对接；身份映射；首次登录自动创建账号 | 后端 | 2 |

**验收标准**：外部系统可通过 API Token 调用问数接口；REST API 数据源可接入并查询；多租户隔离生效。

---

### Sprint 14：12/01 – 12/12 ⏳ P2 「平台化二：本体建模 + 交付材料」

**一句话目标**：本体建模上线、交付材料补齐、安全审计通过。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **本体建模初版** | 基于 business_term + metric + dimension 建立业务本体；本体驱动问题理解；本体管理页面 | AI + 前端 | 5 |
| **多租户完善** | 租户管理页面；租户级配置（模型/限流/数据源）；自服务创建 | 后端 + 前端 | 3 |
| **交付材料补齐** | 产品介绍、架构图更新、用户引导、运维手册、安全白皮书 | 全体 | 4 |
| **安全审计** | pip-audit / npm audit；密钥轮转；最小权限审计；渗透测试 | 全体 | 3 |

**验收标准**：本体模型可消歧业务概念混淆；新成员可按运维手册独立部署；安全漏洞清零。

---

### Sprint 15：12/15 – 12/26 ⏳ 「缓冲收尾 + 2027 规划」

**一句话目标**：缓冲修复、红有达验收、2027 Q1 路线图。

| 交付项 | 说明 | 角色 | 人天 |
|--------|------|------|------|
| **缓冲与修复** | 遗留技术债务清理；pre-existing 测试修复；lint/build warning 清零；性能回归 | 全体 | 4 |
| **红有达部署验收** | 在红有达测试环境完成全功能部署验收 | 全体 | 4 |
| **2027 Q1 路线图** | 多数据集联合查询、指标市场、行级权限、企业 SSO 全量、K8s 部署 | 全体 | 2 |
| **项目记忆压缩** | `.codex/project-memory.md` 归档旧记录 | 全体 | 1 |

**验收标准**：`pytest tests/` 全绿；`npm run lint` 0/0；`npm run build` 无 warning；2027 Q1 计划通过评审。

---

## 四、里程碑检查点

| 检查点 | 日期 | 判定标准 |
|--------|------|----------|
| **M0** 基础链路 | 6/27 | 问数主链打通；多轮可用；Workbench 上线 |
| **M1** 主链迁移 | 7/25 | AgentScope Agent Team 主链替代旧 LangGraph；Chat UI 闭环 |
| **M2** 上下文可控 | 8/22 | 子 Agent 复用；12 轮不爆上下文；快速筛选可用；OTel 5 层 span |
| **M3** 核心能力 | 9/19 | Report Worker 激活；预编译 QP 20 场景；知识图谱上线 |
| **M4** 安全基座 | 10/03 | 脱敏/审计/限流/连接池/P0 全部验收 |
| **M5** P1 完成 | 10/31 | 导出/Worker/接口/监控/性能基线 |
| **M6** P2 + 收尾 | 12/26 | 数据源平台/多租户/SSO/开放 API/本体建模/审计通过 |

---

## 五、用户需求映射总表

| # | 用户需求 | 映射 Sprint | 优先级 |
|---|---------|------------|--------|
| 1 | BI Tools 渲染 | S4–S6 | P0 |
| 2 | 多轮对话快速筛选优化 | S6 | P0 |
| 3 | 子智能体会话级保留 | S4–S5 | P0 |
| 4 | 多租户 | S12–S14 | P2 |
| 5 | 字段级数据脱敏 | S8 | P0 |
| 6 | AgentScope 原生化、观测与运维 | S3–S5 + S11 | P0 |
| 7 | 认证、权限与管理后台 | S8 + S14 | P0→P1 |
| 8 | 数据源定制平台 | S12–S13 | P2 |
| 9 | Assistant-UI 聊天体验与多智能体可视化 | S4–S7 | P0 |
| 10 | 表关系维护（知识图谱） | S6–S8 | P0 |
| 11 | OTel 链路观测 | S6 + S12 | P0 |
| 12 | Report/Bash/Python Worker | S7 + S10 | P0→P1 |
| 13 | 接口发布 | S10–S11 | P1 |
| 14 | 权限审计 | S8 | P0 |
| 15 | 三方登录 SSO | S12–S13 | P2 |
| 16 | 开放智能体能力暴露 | S13 | P2 |
| 17 | 本体建模 | S14 | P2 |
| 18 | 预编译 Query-Plan | S7–S8 | P0 |
| 19 | 上下文压缩 | S5–S6 | P0 |
| 20 | 数据库连接池 | S9 | P0 |
| 21 | API 限流 | S9 | P0 |
| 22 | 数据导出 | S10 | P1 |
| 23 | 监控体系补全 | S11–S12 | P1 |
| 24 | 数据展示分页问题 | S3 ✅ | — |

---

## 六、3 人职责总览

| 角色 | P0 Sprint（S4-S9） | P1（S10-S11） | P2（S12-S15） |
|------|-------------------|---------------|---------------|
| **后端** | 子智能体池化/压缩/观测/知识图谱/脱敏/审计/限流/连接池 | 导出/接口发布/监控告警 | 数据源平台/多租户/SSO |
| **前端** | Tool UI/Multi-Agent UI/图表渲染/失败态retry/Chat UX | 导出 UI/性能/管理后台 | 本体页面/交付材料 |
| **AI 域** | 子智能体设计/压缩/预编译 QP/Report Worker/失败收敛/E2E | Python+Bash Worker | 本体建模/开放 API/文档 |

资源约束：3 人非全职（兼其他项目），每 Sprint 有效约 5–7 人天/人。

---

## 七、时间线总览

```
 6月           7月           8月           9月           10月          11月          12月
├─ S1-S3 ──────┤── S4-S9 (P0) ─────────────────────┤── S10-S11 (P1) ┤── S12-S15 (P2) ┤
│ 基础+多轮+    │  主链稳固 + 查询加固 + 安全基座     │  能力扩展      │  平台化+交付  │
│ AgentScope    │                                    │               │              │
│ 主链迁移 ✅   │                                    │               │              │
│               │                                    │               │              │
│ S1: 基础工程  │ S4: UI闭环+子Agent设计              │ S10: 导出+接口 │ S12: 设计过渡 │
│ S2: 多轮+     │ S5: 池化+观测基线                   │ S11: 监控告警  │ S13: 平台化一 │
│     Workbench │ S6: 压缩+OTel+知识图谱              │               │ S14: 平台化二 │
│ S3: 治理+     │ S7: Report+预编译QP                 │               │ S15: 收尾    │
│     主链迁移  │ S8: 脱敏+权限                       │               │              │
│               │ S9: 连接池+限流+P0验收              │               │              │
```

---

*本计划基于 Datalogue 项目 2026-06-02 至 2026-07-13 的实际提交历史和完成记录，结合用户手动 24 项需求和上传版工作规划整合而成。每个 Sprint 结束时应更新状态标记，季度末检查里程碑达标情况。*
