# 原 Documents 素材目录迁移记录

原 `assets/documents/` 混放了工作计划、调研、原型、脚本和旧架构图，现已拆分：

- `WORK_PLAN.md` 和旧设计方案进入 `docs/archive/2026-07-03-legacy-design-plans/`。
- 原型 HTML、Figma 脚本和旧架构 SVG 进入本归档的 `sources/`。
- 历史日报进入本归档的 `worklogs/`。
- 竞品调研进入 `docs/research/`。

本文件只保留作迁移记录，不作为当前文档入口。
