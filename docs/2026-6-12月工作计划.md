# 数语（Datalogue）2026 年 6–12 月工作计划

> 制定日期：2026-07-13  
> 当前日期：2026-07-13  
> 拆分粒度：最大两周（每 Sprint 约 10 个工作日）  
> 状态说明：✅ 已完成 ｜ 🔄 进行中 ｜ ⏳ 待启动

---

## 一、项目当前状态（截至 2026-07-13）

### 1.1 已交付的核心能力

| 模块 | 状态 | 说明 |
|------|------|------|
| AgentScope 主链 | ✅ 稳定 | Agent Team API (`/api/agent-team/tasks/stream`) 为统一入口；mirror 四表、Chat Session Bridge、Workbench View Model 已上线 |
| BI Worker 查询 | ✅ 稳定 | QueryPlan → DSL → SQL 编译/执行 → artifact 产物闭环；RepairPlan + RepairPatch 自动字段漂移修复已落地 |
| Workbench 工作台 | ✅ 稳定 | 右侧 Panel + 隐藏 `/workbench/:threadId` 路由；artifact 详情、timeline、受控 retry 已可用 |
| 前端目录治理 | ✅ 完成 | G055-G093 完成；`features/chat`、`shared`、`assistant-ui` 边界已划定；lint/build/test 通过 |
| 多轮对话 | ✅ 可用 | 首条消息懒创建、会话恢复、上下文槽位承接、数据集选择后恢复原问题 |
| 数据安全 | ✅ 基线 | 用户可见层禁止 SQL/schema/raw rows/query_plan/field patch；安全摘要 + 脱敏 artifact 已落地 |

### 1.2 已知的待办与债务

- **artifact store 产物持久化**：SQL 原始结果集和完整报告 markdown 需落地 artifact store，跨轮状态只保留 `result_ref` / `report_id`（`TODOS.md` 记录）
- **Phase B 目录语义迁移**：`graph/` → `core/`、`agents/` → `domains/bi/agent/`、`agentscope_service/` → `runtime/engine/` 等 7 个步骤已制定方案，待执行（`datalogue-api/docs/architecture/Phase-B-迁移计划.md`）
- **前端测试债务**：`settings.test.jsx` Router 上下文缺失、`assistant-ui/DatalogueMessage.test.jsx` echarts 依赖等 pre-existing 失败待治理
- **E2E 链路稳定化**：测试方案已制定，需覆盖真实浏览器五类证据（页面、Network、后端日志、数据库、artifact API）
- **lint 13 个 warnings + Vite chunk-size**：既有构建提示，需单独分包治理

---

## 二、整体路线图（三阶段）

```
┌─────────────────────────────────────────────────────────────────────┐
│  阶段一：产品化收口（6月–7月）  │  阶段二：能力扩展（8月–10月）  │  阶段三：生产就绪（11月–12月）  │
│  ───────────────────────────  │  ───────────────────────────  │  ───────────────────────────  │
│  核心目标：问数主链稳定可用      │  核心目标：高级查询 + 可视化    │  核心目标：性能 + 安全 + 部署    │
│  - AgentScope 主链替代 LangGraph │  - 多表联合查询 / 子查询        │  - 缓存与查询优化              │
│  - Workbench 工作台上线        │  - 图表可视化 + 报表导出        │  - 行级权限与数据脱敏          │
│  - Repair 自动修复               │  - 数据产物治理与上下文压缩      │  - 监控告警与运维可观测        │
│  - 目录治理与前端边界            │  - 智能推荐与语义增强          │  - 生产部署文档与灾难恢复      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 三、两周粒度 Sprint 计划

### Sprint 1：6/02–6/13 ✅ 基础工程与问数链路打通

**目标**：建立工程规范，打通问数基础链路。

| 交付项 | 说明 |
|--------|------|
| 工程规范 | 项目记忆、Python 文件头模板、AGENTS.md、编码规范 |
| 问数入口 | 蓝图分析、NL2DSL、语义资产、术语归一化 |
| SQL 安全 | SQL 校验与自动修复、回答解释、低置信确认 |
| 基础观测 | LiteLLM 配置化、Langfuse 本地观测、Trace 深链 |
| SubAgent 骨架 | Dataset SubAgent Manifest、LeadAgent Planner、渐进式披露 |

**验收标准**：后端 pytest 通过；前端 dev server 可访问；`docker compose up` 可启动。

---

### Sprint 2：6/16–6/27 ✅ 多轮对话与 Workbench 上线

**目标**：SubAgent 规划收口、多轮状态管理、Workbench 工作台交付。

| 交付项 | 说明 |
|--------|------|
| SubAgent v2 | 查询规划层 v2、语义资产渐进式披露、DSL 提示词 |
| Thread Memory | `QueryTaskCapsule`、`ConversationState`、多轮槽位承接 |
| C1 RepairPlan | 字段失败诊断、RepairPlan 契约、事件、Artifact refs |
| C2 RepairPatch | 离线 Patch Engine、字段候选、Tool validation、主链接入 |
| C3-P0 Workbench | mirror 四表、Chat Bridge、View Model API、Controlled Retry、Panel |

**验收标准**：真实问题"查询杨凯 2024 年工作日志"成功问数；Workbench 展示 timeline + artifact；历史会话可恢复。

---

### Sprint 3：6/30–7/11 ✅ 治理收口与体验修复（实际已推进至 7/13）

**目标**：Langfuse 移除、目录治理、前端体验修复、产物分页。

| 交付项 | 说明 |
|--------|------|
| Langfuse 移除 | 删除依赖、Docker 服务、API 挂载、前端 Trace 入口；改为本地日志 |
| 左侧功能栏 | `/api/navigation/counts` 真实数量；dashboard/history/datasets 等后端统计 |
| 产物分页 | 移除 `max_limit` 硬上限；后端 `REPORT_RESULT_MAX_ROWS=10000`；前端 `DataTable` 每页 100 行 |
| 历史回放 | `/chat/{id}` 后端聚合 `agentscope_message`；旧会话 `legacy` 回退 |
| Report Worker | Leader Prompt 强制触发规则；worker 注册表收敛为 `bi` + `report` 两类 |
| 目录治理 G055-G093 | facade-first 渐进迁移决策；前端 `features/chat/shared/assistant-ui` 边界；后端测试闸门；文档资产治理 |

**验收标准**：前端 `npm run test` 190+ passed；后端关键子集 189 passed；E2E 桌面 smoke 通过 `/chat`、`/datasets`、`/datasources`、Workbench。

---

### Sprint 4：7/14–7/25 🔄 产物治理与 Phase B 迁移（当前 Sprint）

**目标**：artifact store 落地、目录物理搬迁启动、E2E 测试稳定化。

| 交付项 | 说明 | 负责人/优先级 |
|--------|------|-------------|
| artifact store | SQL 原始结果集和完整报告 markdown 持久化；跨轮状态压缩为 `result_ref` / `report_id` | P0 |
| Phase B Step 1a | `graph/llm.py` → `core/llm.py`（fan-in 极小，3 个 import 点） | P1 |
| Phase B Step 1b | `agents/agentscope_model.py` → `core/`（fan-in=0） | P1 |
| E2E 测试自动化 | 按 `docs/superpowers/plans/2026-07-07-datalogue-e2e-chain-test-plan.md` 落地 API 级和浏览器级 E2E | P1 |
| 前端测试债务 | `settings.test.jsx` Router 上下文修复；`DatalogueMessage.test.jsx` echarts mock | P2 |
| lint warning 治理 | 13 个 pre-existing warnings 逐条修复或标注为 intentional | P2 |

**验收标准**：
- artifact store 新查询产物可跨轮引用，旧产物可只读回放
- Phase B Step 1a/1b 测试全绿，`app/graph/` 目录消失
- E2E 覆盖：主查询链路、artifact 详情、多轮追问、会话恢复、安全边界

**风险**：
- 若主工作区 `registry.py` 未提交改动与 Step 2/3 冲突，需先单独收口
- artifact store 涉及 DB  schema 变更，需 Alembic 迁移 + 中文注释

---

### Sprint 5：7/28–8/08 🔄 Phase B 深水区与 BI 领域收拢

**目标**：完成 Phase B 核心搬迁，BI 领域目录完整。

| 交付项 | 说明 | 优先级 |
|--------|------|--------|
| Phase B Step 2 | `agents/bi_agent/` → `domains/bi/agent/`（7 个 import 点） | P0 |
| Phase B Step 3 | `agentscope_service/bi_worker_*` → `domains/bi/worker/`（6 个文件） | P0 |
| Phase B Step 4a | 框架桥接 → `runtime/engine/`（8 个文件） | P0 |
| BI 领域完整测试 | `domains/bi/` 全链路测试覆盖：agent、worker、skill、toolkit | P1 |
| 前端 `src/app` 拆分 | `app-root.jsx`、`app-shell.jsx`、`routes.jsx` 薄 re-export 建立 | P2 |

**验收标准**：
- `app/agents/` 目录消失；`domains/bi/agent/` 成为 canonical 入口
- `app/agentscope_service/` 中 bi_worker 相关文件消失
- `pytest tests/` 全绿；前端 lint/build/test 通过

**风险**：Step 4a 改动面积最大（10+ import 点），需分 4a/4b/4c 子步骤，每步独立 commit 和回滚。

---

### Sprint 6：8/11–8/22 🔄 Agent Team 域收拢与产物上下文压缩

**目标**：完成 Phase B 剩余步骤，启动 artifact 上下文压缩。

| 交付项 | 说明 | 优先级 |
|--------|------|--------|
| Phase B Step 4b/4c | 业务层 → `domains/agent_team/`；删除 `agentscope_runtime/` | P0 |
| Phase B Step 5 | `services/` 残余清理：import 改新路径、facade 加 DeprecationWarning | P1 |
| Phase B Step 6 | `utils/` SQL 工具 → `domains/query_execution/`（8 个文件） | P1 |
| 产物上下文压缩 | `last_success_task` 白名单落地；`_thread` 上下文大小 guard；大产物不进入 LLM 承接态 | P1 |
| 前端 chat 测试迁移 | `src/assistant/*.test.js` → `src/features/chat/__tests__/` | P2 |

**验收标准**：
- `app/agentscope_service/` 和 `app/agentscope_runtime/` 目录消失
- `app/utils/` 只保留无领域归属工具（`think.py`、`token.py`、`json_utils.py`）
- 多轮对话时 LLM 上下文不超过安全阈值（如 128K token 的 80%）

---

### Sprint 7：8/25–9/05 ⏳ 多数据源联合查询与可视化增强

**目标**：支持跨数据集/跨表的复杂查询，增强可视化能力。

| 交付项 | 说明 | 优先级 |
|--------|------|--------|
| 多表联合查询 | 同数据集内多表 JOIN；QueryPlan 支持 `JoinRequirement` | P0 |
| 跨数据源查询 | 语义层统一：不同数据源（MySQL/PostgreSQL/Doris）在语义层视为同一逻辑数据集 | P1 |
| 图表可视化 | Chat 消息中支持折线图、柱状图、饼图；基于 echarts 的 `ChartBlock` 完善 | P1 |
| 报表导出 | artifact 支持导出 Excel/CSV；前端提供下载按钮 | P1 |
| Workbench 图表面板 | artifact 含图表数据时，Workbench 展示图表预览而非纯表格 | P2 |

**验收标准**：
- "统计 2026 年 4-5 月各地区各品类 GMV" 多表 JOIN 成功
- 图表 artifact 在 Chat 消息和 Workbench 中均可展示
- 导出 Excel 文件内容与页面展示一致

---

### Sprint 8：9/08–9/19 ⏳ 智能推荐与语义增强

**目标**：提升查询的智能化水平，降低用户精确表达成本。

| 交付项 | 说明 | 优先级 |
|--------|------|--------|
| 智能推荐 | 基于用户历史查询和数据源特征，推荐常见问题/指标 | P0 |
| 同义词/别名扩展 | 术语表支持同义词环；用户说"销售额"可匹配"GMV" | P0 |
| 意图识别增强 | 区分"查询明细"、"统计汇总"、"趋势分析"、"对比分析"四类意图 | P1 |
| 时间智能 | 自然语言时间解析："上月"、"本季度"、"同比"、"环比" | P1 |
| 数据洞察提示 | 查询结果异常时自动提示（如"某地区 GMV 环比下降 30%"） | P2 |

**验收标准**：
- 新用户首次进入 Chat 时，侧栏展示 5 条推荐问题
- "上季度环比"类查询时间范围解析正确
- 意图识别准确率 ≥ 80%（基于测试集评估）

---

### Sprint 9：9/22–10/03 ⏳ 权限体系与数据安全加固

**目标**：建立企业级权限体系，确保数据访问可控。

| 交付项 | 说明 | 优先级 |
|--------|------|--------|
| 行级权限 | 基于用户身份/部门的数据行过滤；SQL 执行前自动注入权限条件 | P0 |
| 字段级脱敏 | 敏感字段（如手机号、银行卡）按规则脱敏展示 | P0 |
| 数据访问审计 | 记录谁、何时、查询了什么数据、返回了多少行 | P1 |
| 数据集权限 | 数据集/数据源的可见性控制：用户只能看到授权的数据集 | P1 |
| 查询限流 | 单用户/全系统 QPS 限制；慢查询拦截与告警 | P2 |

**验收标准**：
- 非 admin 用户查询"员工信息"时，只能看到自己部门的记录
- 敏感字段在 Chat/Workbench/导出中均脱敏
- 审计日志可查询，保留 ≥ 90 天

---

### Sprint 10：10/06–10/17 ⏳ 性能优化与缓存体系

**目标**：提升查询响应速度和系统吞吐量。

| 交付项 | 说明 | 优先级 |
|--------|------|--------|
| 查询结果缓存 | 相同/相似 SQL 的查询结果缓存（Redis），TTL 按数据新鲜度配置 | P0 |
| 语义缓存 | 相同问题的语义 embedding 缓存，直接命中历史答案 | P0 |
| SQL 执行优化 | 大表 COUNT(*) 优化、分页查询优化、索引建议 | P1 |
| 并发控制 | BI Worker 并发数限制；队列积压时 graceful degradation | P1 |
| 前端性能 | 大产物虚拟滚动、懒加载、骨架屏 | P2 |

**验收标准**：
- 缓存命中率 ≥ 30%；缓存命中时响应时间 < 1s
- 并发 10 个查询时系统不崩溃，排队有序
- 10000 行产物前端滚动流畅，不卡顿

---

### Sprint 11：10/20–10/31 ⏳ 监控告警与运维可观测

**目标**：建立生产级监控和告警体系。

| 交付项 | 说明 | 优先级 |
|--------|------|--------|
| 系统指标 | 接入 Prometheus/Grafana：QPS、延迟、错误率、LLM token 消耗 | P0 |
| 业务指标 | 查询成功率、意图识别准确率、用户满意度（ thumbs up/down） | P0 |
| 告警规则 | 查询失败率 > 5%、平均延迟 > 30s、LLM 调用失败 时触发告警 | P1 |
| 日志聚合 | 结构化日志（JSON）输出；统一 trace_id 贯穿全链路 | P1 |
| 健康检查 | `/health` 扩展：数据库、Redis、AgentScope Service 子健康状态 | P2 |

**验收标准**：
- Grafana  dashboard 可查看过去 7 天核心指标
- 告警通道（钉钉/邮件）可正常接收
- 单个请求可在日志中通过 trace_id 追踪全链路

---

### Sprint 12：11/03–11/14 ⏳ 部署自动化与灾难恢复

**目标**：完善生产部署流程，确保系统可恢复。

| 交付项 | 说明 | 优先级 |
|--------|------|--------|
| Docker 生产镜像 | 多阶段构建、镜像瘦身、非 root 运行、健康检查 | P0 |
| K8s 部署模板 | Deployment、Service、ConfigMap、Secret、HPA 模板 | P0 |
| 数据库迁移 | Alembic 迁移自动化；生产迁移前备份与回滚策略 | P1 |
| 灾难恢复 | 数据库定期备份、RPO/RTO 目标定义、恢复演练 | P1 |
| 蓝绿部署 | 零停机发布：新版本并行启动，流量切换后旧版本下线 | P2 |

**验收标准**：
- `docker build` 产物镜像 < 500MB
- K8s 模板在测试集群可部署并通过健康检查
- 数据库迁移可在 5 分钟内完成并验证

---

### Sprint 13：11/17–11/28 ⏳ 文档完善与生产验收

**目标**：补齐生产级文档，完成内部验收。

| 交付项 | 说明 | 优先级 |
|--------|------|--------|
| 运维手册 | 部署指南、日常巡检、故障排查、扩容操作 | P0 |
| API 文档 | OpenAPI/Swagger 完整注解；示例请求/响应 | P0 |
| 用户手册 | 面向业务用户的问数指南、常见问题、最佳实践 | P1 |
| 安全白皮书 | 数据安全架构、权限模型、审计策略、合规说明 | P1 |
| 性能基线报告 | 压测结果、瓶颈分析、优化建议、容量规划 | P2 |

**验收标准**：
- 新团队成员可按运维手册独立部署一套环境
- API 文档覆盖率 ≥ 90%
- 压测报告包含至少 50 并发用户场景

---

### Sprint 14：12/01–12/12 ⏳ 缓冲与收尾

**目标**：处理前期 Sprint 遗留债务，预留时间应对突发需求。

| 交付项 | 说明 | 优先级 |
|--------|------|--------|
| 技术债务清理 | 未关闭的 TODO、pre-existing 测试失败、lint warning | P1 |
| 性能回归 | 全链路压测，确认无性能退化 | P1 |
| 安全审计 | 第三方依赖漏洞扫描、密钥轮转、最小权限原则检查 | P1 |
| 用户反馈闭环 | 收集早期用户反馈，修复高优先级问题 | P2 |
| 2027 Q1 规划 | 基于用户反馈和技术债务，制定下季度计划 | P2 |

**验收标准**：
- `pytest tests/` 全绿；`npm run lint` 0 errors、0 warnings；`npm run build` 无 warning
- 安全扫描无高危漏洞
- 下季度计划文档通过评审

---

## 四、关键依赖与风险矩阵

| 风险项 | 影响 | 缓解措施 | 负责人 |
|--------|------|----------|--------|
| Phase B 迁移与主工作区未提交 `registry.py` 冲突 | 高 | Sprint 4 前单独收口 `registry.py` 改动；每步独立 commit | 后端 owner |
| artifact store 涉及 DB schema 变更 | 中 | Alembic 迁移 + 回滚脚本；staging 环境预验证 | 后端 owner |
| 多数据源联合查询的 SQL 方言差异 | 中 | 先支持同数据集 JOIN；跨数据源用语义层统一 | BI 域 owner |
| 权限体系改动面大 | 高 | 按数据集粒度先做粗粒度；行级权限分阶段 | 安全 owner |
| LLM 模型成本波动 | 中 | 语义缓存降低重复调用；本地模型 fallback | 架构 owner |
| 前端人员变动导致目录治理中断 | 中 | 文档完善（G060-G062 README）；lint/build/test 门禁 | 前端 owner |

---

## 五、资源与协作约定

### 5.1 每周节奏

- **周一**：Sprint 站会，回顾上周进展，确认本周优先级
- **周三**：中期对齐，阻塞问题升级
- **周五**：Demo 与代码审查，更新 `.codex/project-memory.md`

### 5.2 代码质量门禁

| 检查项 | 提交前 | 合并前 |
|--------|--------|--------|
| 后端测试 | `pytest tests/ -q` 相关子集通过 | `pytest tests/` 全绿 |
| 前端测试 | `npm run test -- <相关文件>` 通过 | `npm run test` 全绿 |
| 静态检查 | `black + ruff + mypy` 通过 | `npm run lint` 0 errors |
| 构建验证 | `py_compile` | `npm run build` 成功 |
| 文档同步 | 新增功能补充中文注释 | 更新 `.codex/project-memory.md` |

### 5.3 里程碑检查点

| 检查点 | 日期 | 判定标准 |
|--------|------|----------|
| M1 产品化基线 | 7/25 | artifact store 可用；Phase B Step 1-3 完成；E2E 通过 |
| M2 架构收拢 | 8/22 | Phase B 全部完成；`utils/` 清理；上下文压缩落地 |
| M3 高级能力 | 9/19 | 多表 JOIN 可用；图表可视化上线；智能推荐上线 |
| M4 企业安全 | 10/17 | 行级权限/脱敏可用；缓存体系上线；监控告警可工作 |
| M5 生产就绪 | 11/28 | K8s 部署可用；灾难恢复演练通过；文档齐全 |
| M6 季度收尾 | 12/12 | 无 P0 债务；性能基线达标；2027 Q1 计划就绪 |

---

*本计划根据项目实际状态制定，后续可按市场/用户反馈动态调整。每完成一个 Sprint，需更新本文档状态并同步到 `.codex/project-memory.md`。*
