# 计划与待办

本目录记录“准备做什么”，不描述已经落地的系统事实。当前实现和稳定契约以 `docs/architecture/`、`docs/api/` 和代码为准。

## 当前入口

- `TODOS.md`：需要复核和推进的跨版本技术待办。
- `initiatives/`：围绕单一主题的专项落地方案。
- `roadmaps/`：仍有参考价值的中长期路线图。

同一计划产生多个草稿时，只在这里保留最终版本；被替代版本移入 `docs/archive/`。已完成的计划不继续作为当前入口。
